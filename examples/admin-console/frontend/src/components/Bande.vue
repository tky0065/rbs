<script setup lang="ts">
defineProps<{ titre?: string }>()
</script>

<!--
  La bande : l'unité de composition de ce monde. Une sur deux est encrée, comme le papier
  en continu qu'une imprimante ligne fait défiler — l'alternance se lit du rang de la
  bande, si bien qu'en insérer une au milieu ne demande de retoucher aucune des autres.
-->
<template>
  <section class="border-b border-border px-4 py-8 even:bg-muted last:border-b-0 sm:px-6">
    <h2 v-if="titre" class="mb-5 text-base uppercase tracking-[0.28em] text-muted-foreground">
      {{ titre }}
    </h2>
    <slot />
  </section>
</template>
