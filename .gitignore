/target

# Worktrees des agents : du travail en cours, jamais du contenu du dépôt.
/.claude/worktrees

# Site de documentation : la toolchain Node est isolée sous `docs/`.
/docs/node_modules
/docs/build
/docs/.docusaurus

# Espace de travail des sessions d'exécution de plan : un journal et des paquets de revue,
# régénérables, sans valeur une fois la branche fondue.
/.superpowers

# État local du skill de design : journaux de pages de décision, régénérables.
/.impeccable
