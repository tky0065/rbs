# Cinquième exemple `event-hub` — plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal :** un cinquième projet d'exemple, `examples/event-hub`, engendré par le CLI, qui
porte `webhooks`, `scheduler`, `audit`, `cors`, `docker`, `ci`, et dont les guides
`webhooks`, `scheduler` et `audit` citent le code au lieu de le recopier.

**Architecture :** l'exemple est engendré par les commandes de la spec, commité brut
(test de non-dérive vert), puis retouché à la main sur trois fichiers de `orders` : la
création d'une commande écrit sa trace d'audit et émet `order.created` dans la même
transaction. Les marqueurs `// region:` sont posés dans l'exemple ; les guides les citent
par `file=… region=…`, et les sorties de `rbs add` deviennent des `rbs:transcript`.

**Tech Stack :** Rust (Axum, SeaORM), `integration_examples.rs` et `integration_docs.rs`
(assert_cmd), Docusaurus et son plugin `remark-code-from-file.js`.

**Spec :** `docs/superpowers/specs/2026-09-13-cinquieme-exemple-event-hub-design.md`

## Global Constraints

- Ne toucher ni `crates/rbs-cli/templates/**`, ni le code du CLI, ni `CHANGELOG*.md`,
  `IMPROVE.md`, `TODO.md`, `ROADMAP.md`, `CLAUDE.md`.
- Commits Conventional Commits, sujet en français à l'impératif, sans majuscule initiale ni
  point final ; corps = pourquoi technique + `Vérifications :` avec résultats réels. Jamais de
  `Co-Authored-By`, `Claude-Session`, ni mention d'un assistant, d'un plan ou d'un backlog.
- Documentation bilingue : toute page anglaise modifiée l'est en français dans le même commit.
- Un commentaire dit le *pourquoi*, jamais le *quoi*. Le code de l'exemple est en français
  (projet `--lang fr`).
- Les champs réels sont `reference:string,amount:int` : la spec écrit `amount:integer`, type
  que le CLI refuse (`type inconnu « integer »`). Écart consigné.
- Pas de `rbs generate client --lang ts` : seul `hello-crud` le fait.
- Sortie longue → `/private/tmp/claude-501/-Users-yacoubakone-dev-rs/cdf33876-6963-4c94-bb34-990887cbdcf7/scratchpad/event-hub/`.
- Une passe Docker de `crates/rbs-cli/tests` ne tourne jamais en même temps qu'une autre
  suite qui nomme son projet `demo-api` (`target/rbs-integration` partagé).

---

### Task 1 : l'exemple brut, et sa non-dérive

**Files :**
- Create : `examples/event-hub/**` (déplacé depuis `event-hub/`, engendré à la racine du worktree)
- Modify : `crates/rbs-cli/tests/integration_examples.rs` (`EXEMPLES`, un test)

**Interfaces :**
- Produces : l'entrée `Exemple { nom: "event-hub", … }`, lue par le test des retouches (Task 2).

- [ ] **Step 1 : engendrer** (déjà fait à la racine du worktree, à refaire si absent)

```bash
./target/debug/rbs new event-hub --yes --core-path ./crates/rbs-core \
  --database-url 'postgres://rbs:rbs@localhost:5432/event_hub' --lang fr
# puis, pour f dans webhooks scheduler audit cors docker ci :
git -C event-hub add -A && git -C event-hub commit -q -m "before $f"
(cd event-hub && ../target/debug/rbs add "$f")
# enfin
(cd event-hub && ../target/debug/rbs generate crud orders --fields 'reference:string,amount:int' --force)
```

- [ ] **Step 2 : rendre l'exemple portable**

```bash
mv event-hub examples/event-hub && rm -rf examples/event-hub/.git
```
Dans `examples/event-hub/Cargo.toml`, la valeur `path` de `rbs-core` devient
`"../../crates/rbs-core"` (le reste de la ligne — `default-features`, `features` — intact).

- [ ] **Step 3 : l'entrée `EXEMPLES` et le test**, après `newsletter-queue` :

```rust
    Exemple {
        nom: "event-hub",
        database_url: "postgres://rbs:rbs@localhost:5432/event_hub",
        // `webhooks` tire `jobs` et `auth`, et par elle `mail` et `rate-limit` : les cinq
        // descendent d'un seul plan, dont les trois migrations portent le même horodatage.
        // C'est le seul exemple où l'ancre `migration_modules` doit trier des noms nés dans
        // la même seconde.
        features: &["webhooks", "scheduler", "audit", "cors", "docker", "ci"],
        crud: "orders",
        champs: "reference:string,amount:int",
        role: None,
        with_upload: false,
        edite_a_la_main: &[],
        engendre_a_part: &[],
    },
```

```rust
#[test]
fn event_hub_is_what_the_cli_produces_today() {
    assert_no_drift(example("event-hub"));
}
```

- [ ] **Step 4 : compiler, figer `Cargo.lock`, suivre `.env`**

```bash
(cd examples/event-hub && cargo fmt --check && cargo clippy --all-targets -- -D warnings)
git add examples/event-hub && git add -f examples/event-hub/.env
```
Attendu : fmt exit 0, clippy exit 0, `Cargo.lock` présent.

- [ ] **Step 5 : prouver**

```bash
cargo test -p rbs-cli --test integration_examples > $SP/examples-t1.log 2>&1
```
Attendu : tous verts, dont `event_hub_is_what_the_cli_produces_today`,
`each_example_passes_cargo_fmt`, `each_example_file_is_tracked_by_git`.

- [ ] **Step 6 : commit** `feat(examples): ajoute event-hub tel que le CLI l'engendre`

---

### Task 2 : la création transactionnelle, et les régions citées

**Files :**
- Modify : `examples/event-hub/src/orders/{repository,service,controller}.rs` (retouches)
- Modify (marqueurs seuls) : `examples/event-hub/src/modules/audit/mod.rs`,
  `src/modules/audit/tests.rs`, `src/modules/scheduler/mod.rs`,
  `src/modules/scheduler/ticker.rs`, `config/default.toml`
- Modify : `crates/rbs-cli/tests/integration_examples.rs`

**Interfaces :**
- Produces (régions, lues par les Tasks 3-5) :
  `src/orders/service.rs` `create` · `src/orders/repository.rs` `create` ·
  `src/orders/controller.rs` `create` · `src/modules/audit/mod.rs` `actions` ·
  `src/modules/audit/tests.rs` `history` · `src/modules/scheduler/mod.rs` `schedules`,
  `normaliser` · `src/modules/scheduler/ticker.rs` `reserve` ·
  `config/default.toml` `webhooks`, `scheduler` (marqueurs `# region:` en TOML).
- `service::create(db: &DatabaseConnection, input: CreateOrder, actor: &str) -> Result<OrderResponse>`

- [ ] **Step 1 : le test des retouches (rouge)**, après celui de `newsletter-queue` :

```rust
/// Ce que `event-hub` porte et qu'aucune commande n'écrit.
///
/// Les trois fichiers de `orders` sortent de la comparaison octet à octet. Sans ce test, la
/// transaction que trois guides citent pourrait se défaire en silence : l'exemple
/// compilerait encore, et les pages montreraient un contrat que plus rien ne tient.
#[test]
fn the_hand_edits_of_event_hub_are_in_place() {
    let racine = common::depot().join("examples").join("event-hub");
    let lire = |relatif: &str| {
        std::fs::read_to_string(racine.join(relatif))
            .unwrap_or_else(|erreur| panic!("{relatif} illisible : {erreur}"))
    };

    // Dans cet ordre, et non seulement présents : une trace ou une émission posée après le
    // commit survivrait au rollback qu'elle doit suivre, et c'est ce que les guides réfutent.
    let service = lire("src/orders/service.rs");
    let mut depuis = 0;
    for (raison, extrait) in [
        ("la transaction n'est pas ouverte", "db.begin().await?"),
        (
            "la commande ne s'écrit pas dans la transaction",
            "repository::create(&transaction, order)",
        ),
        (
            "la trace ne partage pas la transaction",
            "audit::record(\n        &transaction,",
        ),
        (
            "l'événement ne partage pas la transaction",
            "webhooks::emit(&transaction, \"order.created\", &order)",
        ),
        ("rien ne la commite", "transaction.commit().await?"),
    ] {
        let Some(position) = service[depuis..].find(extrait) else {
            panic!("src/orders/service.rs : {raison} — « {extrait} » absent ou hors d'ordre :\n{service}");
        };
        depuis += position + extrait.len();
    }

    // Générique sur la connexion : une transaction n'est pas un `DatabaseConnection`.
    let repository = lire("src/orders/repository.rs");
    assert!(
        repository.contains("pub async fn create<C: ConnectionTrait>(db: &C, order: ActiveModel)"),
        "src/orders/repository.rs : la création doit accepter une transaction :\n{repository}"
    );

    // L'acteur vient du jeton : sans lui, le journal ne dirait pas qui a créé la commande.
    let controller = lire("src/orders/controller.rs");
    assert!(
        controller.contains("service::create(state.core().db(), input, &identite.user_id)"),
        "src/orders/controller.rs : l'identité doit descendre jusqu'au journal :\n{controller}"
    );
}
```

Run : `cargo test -p rbs-cli --test integration_examples the_hand_edits_of_event_hub` → FAIL
(« la transaction n'est pas ouverte »).

- [ ] **Step 2 : `repository.rs`** — `create` devient générique ; ajouter `ConnectionTrait`
  à l'import `sea_orm::{…}` :

```rust
// region: create
// Générique sur la connexion, à la différence des autres portes : le service y passe la
// transaction qui porte aussi la trace d'audit et l'événement.
pub async fn create<C: ConnectionTrait>(db: &C, order: ActiveModel) -> Result<Model> {
    order.insert(db).await.map_err(conflict_on_duplicate)
}
// endregion: create
```

- [ ] **Step 3 : `service.rs`** — imports : `use sea_orm::{DatabaseConnection, TransactionTrait};`
  (remplace `use sea_orm::DatabaseConnection;`), `use serde_json::json;`,
  `use crate::modules::audit::{self, Entry};`, `use crate::modules::webhooks;`. `create` :

```rust
// region: create
/// Crée une commande, sa trace et son événement, ensemble ou pas du tout.
///
/// Les trois écritures partagent `transaction` : un rollback emporte la commande, l'entrée
/// du journal et les livraisons d'un même geste. Sur `db`, un `order.created` partirait pour
/// une commande que la base a refusée, et le journal tracerait une écriture qui n'a pas eu
/// lieu.
pub async fn create(
    db: &DatabaseConnection,
    input: CreateOrder,
    actor: &str,
) -> Result<OrderResponse> {
    let transaction = db.begin().await?;

    let order = ActiveModel {
        reference: Set(input.reference),
        amount: Set(input.amount),
        ..Default::default()
    };
    let order: OrderResponse = repository::create(&transaction, order).await?.into();

    audit::record(
        &transaction,
        Entry::new(audit::CREATE, "orders", order.id.to_string())
            .actor(actor)
            .changes(json!({ "reference": order.reference, "amount": order.amount })),
    )
    .await?;

    // Le DTO et non l'entité : le corps d'un webhook est un contrat public, et le coupler à
    // la table ferait de chaque colonne renommée une rupture chez tous les abonnés.
    webhooks::emit(&transaction, "order.created", &order).await?;

    transaction.commit().await?;

    Ok(order)
}
// endregion: create
```

- [ ] **Step 4 : `controller.rs`** — entre l'attribut `#[utoipa::path(…)]` de `create` et
  `pub async fn create(`, ouvrir `// region: create` ; la fermer après l'accolade finale. Le
  corps passe l'acteur :

```rust
    let order = service::create(state.core().db(), input, &identite.user_id).await?;
```

- [ ] **Step 5 : marqueurs dans les fichiers engendrés** (aucune autre ligne ne bouge) :
  - `src/modules/audit/mod.rs` : `// region: actions` avant le `///` qui précède
    `pub const CREATE`, `// endregion: actions` après `pub const DELETE`.
  - `src/modules/audit/tests.rs` : `// region: history` avant le commentaire « Le second tri
    n'est pas décoratif », `// endregion: history` après `.expect("lecture possible");`.
  - `src/modules/scheduler/mod.rs` : `// region: schedules` avant le `///` de `schedules()`,
    fermée après son accolade ; `// region: normaliser` avant le `///` de `normaliser`,
    fermée après son accolade.
  - `src/modules/scheduler/ticker.rs` : `// region: reserve` avant le commentaire « La
    condition porte sur `next_run_at` », fermée après `.await?;` du `update_many`.
  - `config/default.toml` : `# region: webhooks` avant `[webhooks]`, `# endregion: webhooks`
    après `timeout_secs = 10` ; idem `scheduler` autour de `[scheduler]`.

- [ ] **Step 6 : exclure les trois fichiers** — dans l'entrée `event-hub`,
  `edite_a_la_main: &["src/orders/repository.rs", "src/orders/service.rs", "src/orders/controller.rs"]`,
  précédé d'un commentaire : « La création d'une commande trace et émet dans sa propre
  transaction : c'est ce que ce projet montre, et `the_hand_edits_of_event_hub_are_in_place`
  en répond. »

- [ ] **Step 7 : prouver**

```bash
(cd examples/event-hub && cargo fmt && cargo fmt --check && cargo clippy --all-targets -- -D warnings)
cargo test -p rbs-cli --test integration_examples > $SP/examples-t2.log 2>&1
```
Attendu : tout vert. `cargo fmt` ne doit toucher que les trois fichiers retouchés.

- [ ] **Step 8 : commit** `feat(examples): trace et émet la création d'une commande dans sa transaction`

---

### Task 3 : le guide `audit` sur l'exemple

**Files :** `docs/docs/guides/audit.md`, `docs/i18n/fr/docusaurus-plugin-content-docs/current/guides/audit.md`

- [ ] **Step 1 :** le bloc `$ rbs add audit` devient gardé — la ligne qui précède le bloc :

```
{/* rbs:transcript cmd="rbs add audit" setup="rbs new demo --yes --database-url postgres://rbs:secret@localhost:5432/demo && git -c user.email=rbs@example.com -c user.name=rbs commit -q -m init" dans="demo" */}
```
Le bloc reprend la sortie réelle (`$SP/transcript-audit.txt`), `plan pour …/demo`.

- [ ] **Step 2 :** « L'écriture et sa trace sont indissociables » : le lien
  `examples/newsletter-queue` devient `examples/event-hub`, la phrase dit que l'exemple
  élargit la *création* ; le bloc `posts/repository.rs` devient
  ```` ```rust file=examples/event-hub/src/orders/repository.rs region=create ````, le bloc
  `posts/service.rs` devient ```` ```rust file=examples/event-hub/src/orders/service.rs region=create ````.
  La prose qui suit dit que la même transaction porte aussi `webhooks::emit` (lien vers
  [`webhooks`](./webhooks.md)).
- [ ] **Step 3 :** « L'acteur » : le bloc `Entry::new(audit::DELETE…)` devient
  ```` ```rust file=examples/event-hub/src/orders/controller.rs region=create ````, la phrase
  d'introduction dit que le handler passe `identite.user_id` au service.
- [ ] **Step 4 :** les trois constantes → ```` ```rust file=examples/event-hub/src/modules/audit/mod.rs region=actions ````.
  « Un avant/après par champ … c'est ce qu'écrit l'exemple ci-dessus » devient : l'exemple
  écrit les valeurs créées, un `UPDATE` écrirait un avant/après.
- [ ] **Step 5 :** la requête `history` → ```` ```rust file=examples/event-hub/src/modules/audit/tests.rs region=history ````,
  introduite par « la requête que le test livré rejoue » ; le paragraphe « Le second tri
  n'est pas décoratif » tombe, le commentaire de la région le porte déjà.
- [ ] **Step 6 :** mêmes changements en français ; `grep -c 'file=' …/audit.md` = 5 des deux côtés.
- [ ] **Step 7 : commit** `docs(guides): cite event-hub dans le guide audit`

### Task 4 : le guide `scheduler` sur l'exemple

**Files :** `docs/docs/guides/scheduler.md`, jumeau français.

- [ ] **Step 1 :** transcript gardé `cmd="rbs add scheduler"`, même `setup`/`dans` qu'en Task 3,
  sortie de `$SP/transcript-scheduler.txt`.
- [ ] **Step 2 :** `schedules()` → ```` ```rust file=examples/event-hub/src/modules/scheduler/mod.rs region=schedules ````,
  précédé d'une phrase : l'exemple garde l'échéance de démonstration telle que le fragment la pose.
- [ ] **Step 3 :** le message d'erreur en `text` → ```` ```rust file=examples/event-hub/src/modules/scheduler/mod.rs region=normaliser ````,
  « Toute autre longueur est refusée en nommant l'expression : ».
- [ ] **Step 4 :** le bloc `sql` → ```` ```rust file=examples/event-hub/src/modules/scheduler/ticker.rs region=reserve ````,
  « Une échéance se réserve par un `UPDATE` conditionnel — `WHERE kind = ? AND next_run_at <= ?` : ».
- [ ] **Step 5 :** `[scheduler]` → ```` ```toml file=examples/event-hub/config/default.toml region=scheduler ````.
- [ ] **Step 6 :** français ; `grep -c 'file='` = 4 des deux côtés.
- [ ] **Step 7 : commit** `docs(guides): cite event-hub dans le guide scheduler`

### Task 5 : le guide `webhooks` sur l'exemple, et sa sortie périmée

**Files :** `docs/docs/guides/webhooks.md`, jumeau français.

- [ ] **Step 1 :** le bloc `$ rbs add webhooks` devient
  `{/* rbs:transcript cmd="rbs add webhooks" setup="…" dans="demo" extrait="oui" */}` et
  reprend `$SP/transcript-webhooks.txt` : les deux premières lignes (`exige jobs, mail,
  rate-limit, auth`), `plan pour …/demo`, la première ligne de chaque fragment tiré, les
  onze lignes de `webhooks` et sa migration, `~ AGENTS.md`, `67 fichiers à écrire`,
  `✓ webhooks installée — 54 fichiers`, l'indication finale. Aucune ligne `…` (le mode
  extrait cherche chaque ligne du bloc dans la sortie). La phrase d'introduction dit
  « en voici un extrait ». Le premier paragraphe liste les fichiers réels (onze, `target.rs`
  compris) — vérifier « dix fichiers ».
- [ ] **Step 2 :** « Émettre un événement » : le bloc d'une ligne devient
  ```` ```rust file=examples/event-hub/src/orders/service.rs region=create ```` ; la prose
  parle d'`order.created` et renvoie au guide [audit](./audit.md) pour la trace qui partage
  la transaction.
- [ ] **Step 3 :** `[webhooks]` → ```` ```toml file=examples/event-hub/config/default.toml region=webhooks ````.
- [ ] **Step 4 :** les blocs `json` (corps d'abonnement, enveloppe), la formule `text` et le
  `js` du receveur restent : ce ne sont pas du code du projet.
- [ ] **Step 5 :** français ; `grep -c 'file='` = 2 des deux côtés.
- [ ] **Step 6 : commit** `docs(guides): cite event-hub dans le guide webhooks et corrige sa sortie`

### Task 6 : les README d'exemples, et « cinq »

**Files :** `examples/README.md`, `examples/README.fr.md`

- [ ] **Step 1 :** une ligne au tableau :
  EN `| \`event-hub\` | \`webhooks\`, \`scheduler\`, \`audit\`, \`cors\`, \`docker\` and \`ci\`: creating an order writes its audit entry and emits \`order.created\` inside the transaction that inserts it. |`
  FR `| \`event-hub\` | \`webhooks\`, \`scheduler\`, \`audit\`, \`cors\`, \`docker\` et \`ci\` : la création d'une commande écrit sa trace d'audit et émet \`order.created\` dans la transaction qui l'insère. |`
- [ ] **Step 2 :** une section `### \`event-hub\`` après `newsletter-queue`, avec la boucle de
  `file-drop` sur `webhooks scheduler audit cors docker ci` et
  `generate crud orders --fields 'reference:string,amount:int' --force` ; un paragraphe dit
  que `webhooks` tire `jobs`, `auth`, `mail`, `rate-limit`, que ses trois migrations naissent
  dans la même seconde, et que `.github/` et `Dockerfile` ne sont lus par aucune CI du dépôt.
- [ ] **Step 3 :** « Edits the CLI does not produce » : « all four projects » → « all five » ;
  un paragraphe `event-hub` pour ses trois retouches (repository générique, service
  transactionnel, contrôleur qui passe l'acteur), et le test qui en répond.
- [ ] **Step 4 :** « regenerates all four projects » → « all five » ; FR idem.
- [ ] **Step 5 : commit** `docs(examples): documente la régénération d'event-hub`

### Task 7 : vérification finale

- [ ] `git merge improve/lot-p2-doc-langue-exemple` ; `cargo build -p rbs-cli --bin rbs`.
- [ ] `cargo test -p rbs-cli --test integration_examples` — vert (régénérer si rouge).
- [ ] `cargo test -p rbs-cli --test integration_docs --no-fail-fast -- the_marked_transcripts_still_render_what_the_docs_show`.
- [ ] Exemple : `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, puis
  PostgreSQL 17 sur un port libre, `RBS_DATABASE__URL=… cargo run -p migration -- up`,
  `cargo test -- --include-ignored` ; arrêter le conteneur.
- [ ] `cd docs && npm run clear && npm run build` (code de sortie réel), `npm test`,
  `node scripts/parite.mjs`, tableaux comparés à la main.
- [ ] `grep -c 'file='` avant/après, EN et FR.
