# Lot P3 « CLI » — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal :** cinq corrections du binaire `rbs` — deux `allow(dead_code)` mensongers, message d'URL qui nomme le bon moteur, `doctor` qui ne compte plus deux fois une clé absente du `.env`, bilan « créés / modifiés » cohérent avec le plan, et contrat de codes de sortie 1/2/3.

**Architecture :** tout vit dans `crates/rbs-cli/src/` et dans `docs/` ; aucune template touchée. Vérification par tests unitaires, un binaire de tests d'intégration rapide, et `integration_docs` (transcriptions de la doc rejouées).

**Tech Stack :** Rust 2024, clap 4, thiserror, assert_cmd.

**Spec :** design approuvé en session le 2026-09-14 (tâches 52, 57, 59, 61, 65 d'`IMPROVE.md`, lire leurs lignes). Décisions : #57 inclut `doctor/base.rs` ; #59 = contrat complet sur toutes les commandes.

## Global Constraints

- Branche : `git checkout -b improve/p3-cli improve/p3-lot`. Avant la première ligne : `git rev-list --count HEAD..improve/p3-lot` doit rendre `0`.
- Ne jamais toucher `crates/rbs-cli/templates/`, `examples/`, `IMPROVE.md`, `TODO.md`, `CHANGELOG.md`, `CHANGELOG.fr.md` (le lot templates et l'orchestrateur s'en chargent).
- Commits : Conventional Commits, sujet français à l'impératif sans majuscule ni point final, **aucun identifiant de tâche**, aucune mention de backlog/plan, **aucune ligne `Co-Authored-By` ni `Claude-Session`**, corps = pourquoi technique + intertitre `Vérifications :` avec les commandes lancées et leur résultat réel.
- Commentaires : le *pourquoi*, jamais le *quoi*. Un `match` de classification est **exhaustif, sans `_`** : une variante ajoutée doit forcer une décision.
- Doc bilingue : toute page EN modifiée (`docs/docs/…`) l'est aussi en FR (`docs/i18n/fr/docusaurus-plugin-content-docs/current/…`) dans le même commit. Les blocs `{/* rbs:transcript … */}` sont rejoués caractère par caractère par `integration_docs` ; les blocs sans marqueur (tous les `guides/`) ne sont vérifiés par rien : les greper et les corriger à la main.
- Toute chaîne affichée qui change : la greper dans **tout** `crates/rbs-cli/` (`src/` et `tests/`) et `docs/` avant de conclure.
- Sorties longues : rediriger vers `/private/tmp/claude-501/-Users-yacoubakone-dev-rs/479da9f6-2d4c-4897-b574-61f82e638062/scratchpad/cli-<nom>.log` (préfixe `cli-` obligatoire : scratchpad partagé avec l'agent du lot templates). `integration_docs` sans `--ignored` dure plus de 360 s : `run_in_background`, sortie redirigée.
- Ordre des tâches imposé : 1 → 5 (la #59 en dernier, elle retouche tous les points de sortie de `lib.rs`).

---

### Task 1 : retirer deux `allow(dead_code)` devenus faux

**Files :** Modify `crates/rbs-cli/src/client/document.rs:41-43` et `:101-103`.

- [ ] **Step 1 :** supprimer les deux lignes de commentaire et le `#[allow(dead_code)]` au-dessus de `Parameter` (lu par `client/ts.rs`) et de `parse` (appelée par `client/mod.rs`).
- [ ] **Step 2 :** `cargo clippy --workspace --all-targets -- -D warnings` → aucune sortie ; `cargo test -p rbs-cli --lib client` → vert.
- [ ] **Step 3 : commit** — `refactor(client): retire deux allow(dead_code) que leurs appelants démentent`

### Task 2 : `rbs dev` nomme le moteur du projet dans l'erreur d'URL

**Files :** Modify `crates/rbs-cli/src/database.rs` (un libellé), `crates/rbs-cli/src/dev/mod.rs:66-69,127-130,192-196` ; Test dans `dev/mod.rs` (module `tests`).

**Interfaces :** Produit `Database::label(self) -> &'static str` (`"PostgreSQL"`, `"MySQL"`, `"SQLite"`) — sauf si un équivalent existe déjà (`grep -n '"PostgreSQL"' crates/rbs-cli/src/database.rs`), auquel cas l'employer. `dev::Error::UrlIllisible` devient `UrlIllisible { database: Database }`.

- [ ] **Step 1 : test** (reprendre la forme d'URL qu'un test existant emploie déjà pour provoquer `UrlIllisible`, `grep -n UrlIllisible crates/rbs-cli/src/dev/mod.rs`)

```rust
#[test]
fn an_unreadable_url_on_a_mysql_project_names_mysql() {
    let (_parent, root) = project_on(Database::Mysql, &[], "mysql://");

    let error = plan(&root).expect_err("une URL sans hôte ne se sonde pas");

    assert!(error.to_string().contains("URL MySQL"), "{error}");
    let remede = error.remedy().expect("l'erreur porte un remède");
    assert!(remede.contains("=mysql://"), "{remede}");
    assert!(!remede.contains("postgres"), "{remede}");
}
```

- [ ] **Step 2 :** `cargo test -p rbs-cli --lib dev::tests::an_unreadable_url` → FAIL (« URL PostgreSQL »).
- [ ] **Step 3 : correctif**

```rust
    /// L'URL de la base ne dit pas quel hôte joindre.
    #[error("{} n'est pas une URL {} exploitable", migrate::URL, .database.label())]
    UrlIllisible {
        /// Moteur que le manifeste déclare.
        database: Database,
    },
```

Remède : `format!("attendu : {}={}://utilisateur:motdepasse@hote:port/base", migrate::URL, database.schemes()[0])`. Dans `plan()`, calculer `let database = database_of(root);` une fois et construire `Error::UrlIllisible { database }` aux deux `ok_or`.

- [ ] **Step 4 :** test vert, puis `cargo test -p rbs-cli --lib` entier ; grep `URL PostgreSQL exploitable` dans `docs/` et `tests/` → mettre à jour (EN+FR).
- [ ] **Step 5 : commit** — `fix(dev): nomme le moteur du projet quand son URL est illisible`

### Task 3 : `doctor` ne signale qu'une fois une clé absente du `.env`

Règle : le contrôle `.env` a déjà nommé une clé, avec son remède, dès que `.env.example` la déclare et que le `.env` ne la porte pas (ou est illisible). Les contrôles `auth`, `mail` et `base` se taisent alors sur elle.

**Files :** Modify `crates/rbs-cli/src/doctor/env.rs` (une fonction), `doctor/auth.rs:40-60`, `doctor/mail.rs:34-60`, `doctor/base.rs:36-60` ; tests dans chacun et un test d'ensemble dans `doctor/mod.rs`.

**Interfaces :** Produit `pub(crate) fn signalee(root: &Path, cle: &str) -> bool` dans `doctor/env.rs`.

- [ ] **Step 1 : tests**
  - `env.rs` : `signalee` vrai quand la clé est dans l'exemple et absente du `.env` ; vrai si le `.env` manque ; faux si l'exemple ne la déclare pas ; faux si le `.env` la porte.
  - `mail.rs` : `a_password_missing_from_env_is_left_to_the_env_check` — projet `project_with_mail()`, ligne `RBS_MAIL__SMTP_PASSWORD` retirée du `.env` seul → `State::Bon`, détail qui contient `.env` ; `a_password_missing_from_both_files_is_still_named_by_mail` — retirée des deux fichiers → `State::Echec` (l'environnement passé à `check_with` ne la porte pas).
  - `auth.rs` : les deux mêmes avec `RBS_AUTH__SECRET`.
  - `base.rs` : `an_url_missing_from_env_is_left_to_the_env_check` — `RBS_DATABASE__URL` retirée du `.env` → `State::Avertissement` ; `.env` supprimé → `State::Avertissement`.
  - `mod.rs` : sur un projet avec `mail` dont le `.env` n'a plus `RBS_MAIL__SMTP_PASSWORD`, `run_with` rend exactement **un** contrôle en `Echec` dont le détail nomme la clé.
- [ ] **Step 2 :** `cargo test -p rbs-cli --lib doctor` → les nouveaux échouent.
- [ ] **Step 3 : correctif**

```rust
/// Le contrôle `.env` a-t-il déjà nommé `cle`, avec son remède ?
///
/// Il le fait dès que `.env.example` la déclare et que le `.env` ne la porte pas — ou ne
/// se lit pas. Un contrôle de feature qui la nommerait aussi compterait deux fautes pour
/// une ligne à écrire, avec deux remèdes différents.
pub(crate) fn signalee(root: &Path, cle: &str) -> bool {
    let Ok(attendues) = dotenv::read(&root.join(EXEMPLE)) else {
        return false;
    };
    dotenv::value(&attendues, cle).is_some()
        && dotenv::read(&root.join(FICHIER)).map_or(true, |presentes| dotenv::value(&presentes, cle).is_none())
}
```

`mail.rs` / `auth.rs` : dans le `match`, une branche `None if super::env::signalee(root, CLE) => renvoi = true,` avant `None => { … }` ; en fin de fonction, si `defauts` est vide et `renvoi`, `Check::ok(TITRE, format!("rien d'autre à signaler — {CLE} relève du contrôle .env"))` (`SECRET` pour `auth`).

`base.rs` : quand `migrate::project_variables` échoue **et** que le `.env` ne se lit pas, et quand `url(&variables)` rend `None` **et** `super::env::signalee(root, migrate::URL)`, rendre :

```rust
Check::warned(
    TITRE,
    format!("non vérifiée : {} manque au .env, ce que le contrôle .env signale", migrate::URL),
    "voir le contrôle .env",
)
```

(libellé adapté au `.env` illisible). Les autres cas gardent leur `Check::failed`.

- [ ] **Step 4 :** `cargo test -p rbs-cli --lib` entier. Des tests existants qui supposaient le double signalement changent d'assertion : les lister dans le corps du commit. Grep des messages `ni dans le .env ni dans l'environnement` dans `docs/` (EN+FR) et `tests/`.
- [ ] **Step 5 : commit** — `fix(doctor): laisse au contrôle .env la clé qu'il a déjà nommée`

### Task 4 : le bilan dit « créés » et « modifiés », comme le plan

**Files :** Modify `crates/rbs-cli/src/plan/mod.rs` (bilan), `plan/render.rs:128-150` (pied), `ui.rs:129-132` (libellé), `lib.rs` (succès de `add` ≈`:590`, `generate` ≈`:800`, `generate job` ≈`:926`), `generate/job.rs:75-108` (`Bilan::Ecrit`) ; les tests de chacun ; les transcriptions de `docs/`.

**Interfaces :** Produit `pub(crate) struct Bilan { pub crees: usize, pub modifies: usize }` et `Plan::bilan(&self, force: bool) -> Bilan` dans `plan/mod.rs` ; `ui::bilan(crees: usize, modifies: usize) -> String`. `generate::job::Bilan::Ecrit { crees, modifies, a_reporter }`.

- [ ] **Step 1 : tests**
  - `ui` : `(3, 6)` → `"3 créés, 6 modifiés"` ; `(1, 0)` → `"1 créé"` ; `(0, 1)` → `"1 modifié"` ; `(0, 0)` → `"aucun fichier"`.
  - `plan` : un plan de trois fichiers neufs, deux existants à modifier, un inchangé et un en conflit → `bilan(false) == Bilan { crees: 3, modifies: 2 }` ; `bilan(true)` compte le conflit (dans `modifies` s'il existe). **Lire `plan/application.rs` et reproduire exactement sa règle d'écriture** (quels statuts sont écrits, avec et sans `--force`).
  - `render` : le pied devient `"3 à créer, 2 à modifier, 1 inchangé, 1 en conflit"` (segments nuls omis, pluriels comme aujourd'hui).
- [ ] **Step 2 :** `cargo test -p rbs-cli --lib` → les nouveaux échouent.
- [ ] **Step 3 : correctif** — `ui::bilan` :

```rust
/// Ce qu'une commande a écrit, par ce qui est arrivé aux fichiers.
///
/// Le seul total mentait : `add cors` annonçait neuf fichiers à écrire puis se disait
/// installé en trois, ne comptant que ceux qu'il avait créés.
pub fn bilan(crees: usize, modifies: usize) -> String {
    let pluriel = |n: usize| if n > 1 { "s" } else { "" };
    let mut segments = Vec::new();
    if crees > 0 {
        segments.push(format!("{crees} créé{}", pluriel(crees)));
    }
    if modifies > 0 {
        segments.push(format!("{modifies} modifié{}", pluriel(modifies)));
    }
    if segments.is_empty() {
        return "aucun fichier".to_string();
    }
    segments.join(", ")
}
```

Succès : `"{feature} installée — {}"`, `"{feature} générée — {}"`, `"job {name} écrit — {}"` avec `ui::bilan(…)` tiré de `planned.plan.bilan(force)`. `ui::files` reste pour les lignes de `rbs new` (tout y est créé) ; le supprimer s'il n'a plus d'appelant. La sortie `--json` ne change pas.

- [ ] **Step 4 :** `cargo test -p rbs-cli --lib` entier ; grep `fichiers à écrire`, `à écrire,`, `installée —`, `générée —`, `écrit —` dans `crates/rbs-cli/src crates/rbs-cli/tests docs/docs docs/i18n` → corriger chaque occurrence (EN+FR, guides compris), en reprenant la sortie réelle de la commande. `cargo test -p rbs-cli --test integration_docs --no-fail-fast > …/cli-docs.log 2>&1` en arrière-plan → vert.
- [ ] **Step 5 : commit** — `fix(cli): distingue fichiers créés et modifiés dans le plan et le bilan`

### Task 5 : trois codes de sortie, un par nature d'échec

**Files :** Modify `crates/rbs-cli/src/errors.rs` (type et trait), `lib.rs` (tous les `process::exit`), les enums d'erreur de `new.rs`, `add/mod.rs`, `generate/command.rs`, `generate/job.rs`, `migrate/mod.rs`, `seed.rs`, `dev/mod.rs`, `openapi.rs`, `upgrade.rs`, `client/mod.rs`, `doctor/mod.rs` ; Create `crates/rbs-cli/tests/integration_sortie.rs` ; Doc `docs/docs/cli/doctor.md` (section des codes) et les phrases « exits with status 1 » de `cli/add.md`, `new.md`, `generate.md`, `migrate.md`, `seed.md`, `test.md`, `upgrade.md`, `doctor.md` + jumelles FR.

**Interfaces :** Produit dans `errors.rs` :

```rust
/// Ce que le code de sortie dit d'un échec, pour un script qui ne lit pas le message.
///
/// Trois familles plutôt qu'un code par erreur : un script ne branche que sur ce qu'il
/// peut faire — corriger le projet, corriger l'appel, ou réessayer quand l'environnement
/// le permettra. `rbs doctor` en a besoin pour distinguer une faute trouvée d'un
/// diagnostic qui n'a pas pu tourner.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub(crate) enum Sortie {
    /// Le projet porte une faute que la commande a trouvée, ou qui l'empêche.
    Faute,
    /// La commande est mal appelée, ou pas au bon endroit — le 2 des erreurs d'usage de clap.
    Usage,
    /// L'environnement a manqué : fichier illisible, outil introuvable, service injoignable.
    Environnement,
}

impl Sortie {
    pub(crate) fn code(self) -> i32 {
        match self {
            Self::Faute => 1,
            Self::Usage => 2,
            Self::Environnement => 3,
        }
    }
}

/// Une erreur qui sait à quelle famille de sortie elle appartient.
pub(crate) trait Classee {
    fn sortie(&self) -> Sortie;
}
```

**Classification** (F = Faute, U = Usage, E = Environnement ; « → » = déléguer à la `sortie()` de l'erreur enveloppée). Règle pour trancher un cas : U si le geste est de changer la ligne de commande ou l'endroit d'où on la lance (y compris « committez d'abord », « installez la feature prérequise ») ; E si rien dans le projet ni dans l'appel n'est en cause (IO, outil absent, service arrêté, CLI trop vieux) ; F sinon. Lire la doc de chaque variante non listée et appliquer la règle ; tout écart à ce tableau se justifie dans le corps du commit.

| Enum | U | E | F |
|---|---|---|---|
| `new::Error` | `NomInvalide`, `FeatureInconnue`, `UrlEtrangereAuMoteur`, `RepertoireOccupe`, `NoyauIntrouvable` | `Templates`, `Ecriture` | `Rendu`, `Agents` ; `Installation` → |
| `add::Error` | `PasUnProjet`, `Unknown`, `WorkingTreeSale` | `Acces`, `Application` | `SansManifeste`, `Manifest`, `Installation`, `Metadata`, `UrlIndecomposable`, `Plan` ; `Env` → |
| `generate::command::Error` | `PasUnProjet`, `Nom`, `Fields`, `DejaPresente`, `WorkingTreeSale`, `Relations`, `Homonyme`, `Absente`, `RoleSansAuth`, `UploadSansStorage`, `SoftDeleteColonneReservee`, `RoleInconnu`, `EnfantSansCle` | `Acces`, `Application` | `Rendu`, `Plan`, `Metadata`, `MigrationsAbsentes`, `UploadStorageHorsModules` |
| `generate::job::Error` | `PasUnProjet`, `WorkingTreeSale`, `Nom`, `SansJobs`, `SansScheduler`, `Cron`, `KindPris`, `EcheanceExistante` | `Acces`, `Application` | `Metadata`, `Rendu`, `Plan`, et selon la règle : `ModuleDeLaFile`, `NomDuDossier`, `NomDeCrate`, `HorsModules`, `ModuleDejaDeclare`, `FichierEtranger` |
| `migrate::Error` | `PasUnProjet` | `Cargo` | `Env`, `SansUrl`, `Migration`, `State`, `Fresh`, `Metadata` |
| `seed::Error` | `PasUnProjet`, `Production` | `Acces`, `Cargo` | `SansSeeds`, `Env`, `SansUrl`, `Seeds`, `Metadata` |
| `dev::Error` | `PasUnProjet` | `Injoignable`, `Docker`, `Compose`, `Cwd`, `Watch`, `Cargo` | `UrlIllisible`, `Metadata` ; `Env` → ; `Tests { code }` garde son code via `exit_code()` |
| `openapi::Error` | `PasUnProjet` | `Acces` | `Metadata`, `Obtention`, `Document` |
| `upgrade::Error` | `PasUnProjet`, `WorkingTreeSale` | `Acces`, `CliAnterieur`, `Application` | `Metadata`, `Plan`, `Agents` |
| `client::Error` | `PasUnProjet`, `WorkingTreeSale` | `Acces`, `Application` | `Openapi`, `Document`, `Rendu`, `Plan`, `Metadata` |
| `doctor::Error` | `PasUnProjet` | — | `Metadata` |

`rbs doctor` : `Ok(false)` sort en `Sortie::Faute.code()` (1, inchangé) ; `Err(e)` en `e.sortie().code()`. `dev::Error::exit_code()` rend `code` pour `Tests`, `self.sortie().code()` sinon. Si `SansSeeds` sort aujourd'hui en 0 (`docs/docs/cli/seed.md:52`), ne pas changer ce chemin.

- [ ] **Step 1 : tests unitaires** dans `errors.rs` : `Sortie::code` rend 1/2/3 ; un échantillon par enum, sur le modèle de `a_sample_of_errors_from_every_command_renders_a_snake_case_ascii_code` (`PasUnProjet` → `Usage`, une variante `Acces` → `Environnement`, une variante `Plan` → `Faute`, `add::Error::Env` délégué).
- [ ] **Step 2 : tests d'intégration rapides** — `crates/rbs-cli/tests/integration_sortie.rs`, sans Docker, avec `assert_cmd` comme les autres binaires (`grep -n cargo_bin crates/rbs-cli/tests/common/mod.rs`) :
  - `rbs doctor` dans un répertoire vide → code 2 ;
  - `rbs add inconnue` dans un projet frais (`rbs new … --yes` dans un tmpdir) → 2 ;
  - `rbs add cors` sur un projet dont on a retiré l'ancre `// <rbs:layers>` de `src/router.rs` (projet commité d'abord, sinon c'est `WorkingTreeSale` qui répond) → 1 ;
  - `rbs new demo` dans un répertoire en `0o555` (`#[cfg(unix)]`) → 3 ;
  - `rbs doctor` sur un projet frais sans base joignable → 1 (une faute trouvée).
- [ ] **Step 3 :** `cargo test -p rbs-cli --lib errors` et `cargo test -p rbs-cli --test integration_sortie` → échouent.
- [ ] **Step 4 : correctif** — implémenter `Classee` sur les onze enums (`match` exhaustifs), remplacer chaque `std::process::exit(1)` de `lib.rs` par `std::process::exit(error.sortie().code())`, ajouter `+ errors::Classee` aux bornes de `echec` et utiliser `error.sortie()` dans `echec_openapi`.
- [ ] **Step 5 :** tests verts ; `grep -rn 'code(1)\|code(Some(1))\|status.code()' crates/rbs-cli/tests crates/rbs-cli/src` → mettre à jour ce qui change (y compris dans les suites Docker, en le signalant : elles ne tourneront qu'à la passe finale de l'orchestrateur).
- [ ] **Step 6 : doc** — dans `cli/doctor.md` (EN+FR), la section des codes : 0 sain, 1 faute trouvée, 2 appel à corriger, 3 diagnostic empêché par l'environnement ; un paragraphe qui dit que le contrat vaut pour toutes les commandes. Corriger chaque « Each of these exits with status 1 » / « Exit status 1 » / « Exit code 1 » des pages listées selon la classification réelle de chaque cas cité (FR : même passage).
- [ ] **Step 7 : commit** — `feat(cli): distingue faute du projet, erreur d'usage et panne d'environnement par le code de sortie`

### Task 6 : passe finale

- [ ] `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings` : aucune sortie.
- [ ] `cargo test --workspace --no-fail-fast > …/cli-workspace.log 2>&1` en arrière-plan (plus de 600 s), total par `awk`.
- [ ] `cargo test -p rbs-cli --test integration_docs --no-fail-fast -- --ignored > …/cli-docs-ignored.log 2>&1` (Docker, transcriptions `base="oui"`).
- [ ] `node <chemin de parite.mjs>` (`find . -name parite.mjs -not -path '*/node_modules/*'`) : aucune paire en écart hors `IMPROVE_OLD.md`.
- [ ] `cd docs && npm ci && npm run build` : exit 0.
- [ ] Rapport final : par tâche, commit, test rouge vu (sortie citée), test vert ; chiffres de chaque suite ; tableau réel de classification des variantes tranchées « selon la règle » ; tout échec cité tel quel.
