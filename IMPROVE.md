# IMPROVE — backlog d'améliorations

**Scan du 2026-09-11**, sur le commit `b5c42fd` (`main`, arbre propre, v1.4.0 publiée).

**Méthode** — signaux mécaniques (`cargo fmt --check`, `clippy --all-targets -D warnings`,
`cargo test --workspace`, `cargo audit`, `npm audit` sur `docs/`, `parite.mjs`) puis six
agents de lecture parallèles : bugs & correctness, sécurité, dette & documentation,
performance & UX, qualité du code engendré, features. Le CLI a été exécuté (`rbs new`,
`add`, `generate`, `doctor`, `upgrade`) sur des projets jetables pour observer sa sortie
réelle. 75 findings bruts, dédupliqués et force-rankés. **Chaque tâche ci-dessous a été
vérifiée dans le code réel** — un finding sans `fichier:ligne` lu a été jeté.

**Rapport avec les autres fichiers** — `IMPROVE_OLD.md` est le backlog du scan précédent
(2026-08-30, v1.1.0) : ses 126 tâches sont toutes faites, aucune n'est reprise ici, et il
est déprécié. `TODO.md` reste le journal clos des cinq premiers jalons. `ROADMAP.md`
porte les jalons et le hors-périmètre.

**Exclusions** — hors périmètre du projet, jamais proposé ici : GraphQL, multi-tenancy,
WebSockets, gRPC, interface d'administration générée, gestion des paiements
(`ROADMAP.md:107-112`). Écartés après lecture : OAuth/OIDC et TOTP (après les clés d'API,
même famille), recherche plein texte (`guides/filtering.md:158` l'écarte : trois moteurs
divergents), `generate crud --from-table` (la spec §3.5 le renvoie à `sea-orm-cli`),
serveur MCP (les tâches 39, 40 et 44 donnent la même surface à un agent sans binaire
nouveau), `rbs new --from <URL>` (réseau dans un CLI qui n'en fait pas).

**Légende** — Tags : `[Bug]` `[Sécu]` `[Perf]` `[UX]` `[Dette]` `[Doc]` `[Feature]`
`[Arch]`. Difficulté : Easy (< 1 j) · Medium (1-3 j) · Hard (3-10 j). Les chemins
`templates/…` s'entendent sous `crates/rbs-cli/templates/`, les autres sous `crates/rbs-cli/src/`
ou tels quels depuis la racine.

---

## P0 — critique

- [x] **1. [Sécu] Tout compte auto-inscrit reçoit chez lui les événements de tout le projet, et peut couper les abonnements d'autrui** — `templates/features/webhooks/controller.rs.jinja:31-36,52-57,72-77` : les trois handlers prennent `_identite: Identity` sans `require_role`, `model.rs.jinja:6-19` n'a pas de colonne `owner`, et `/auth/register` est ouvert → un inconnu s'inscrit, `POST /webhooks/subscriptions {events:["*"]}` et reçoit signé `user.created` avec les adresses ; `DELETE …/{id}` révoque l'abonnement légitime. Fix : `require_role(Role::Admin)` par défaut sur les trois routes, comme le `--role admin` du CRUD. *(Easy)* — Fait le 2026-09-11 : `integration_webhooks -- --ignored` : 1 passé, 134,77 s, les sept tests livrés exigés nommément dont `a_user_role_is_refused_on_the_three_routes` ; `integration_docs` 13 passés.
- [x] **2. [Bug] L'image Docker engendrée n'embarque pas `templates/mail/` : aucun courriel ne part en conteneur** — `templates/features/docker/Dockerfile.jinja:23-27` ne copie que les deux binaires et `config/`, alors que `mail/config.rs.jinja:33` lit `templates/mail` à l'exécution (chemin relatif) → sous `docker compose --profile app up`, `register` journalise « envoi échoué » et `forgot-password` rend 202 sans jamais envoyer ; le preset `api` (auth + docker) est cassé par défaut. Fix : `COPY templates ./templates` quand `mail` est présent. *(Easy)* — Fait le 2026-09-11 : `cargo test -p rbs-cli --lib -- add::` 84 passés dont `the_dockerfile_ships_the_templates_directory` ; `docker build` de l'étage runtime rendu : `/app/templates/mail/bienvenue.html` présent avec `mail`, build réussi sans.
- [x] **3. [Bug] Un job réservé au moment d'un crash du worker reste `running` pour toujours** — `templates/features/jobs/queue.rs.jinja:30-38` : la réservation ne dépile que `WHERE status = 'pending'`, rien ne reprend une ligne `running` dont `updated_at` est ancien, et `worker.rs.jinja:50-70` n'a aucun bail → chaque rolling restart perd silencieusement les livraisons webhooks et les échéances du scheduler en cours (`ticker.rs.jinja:82-90` a déjà avancé `next_run_at`) ; `docs/guides/jobs.md:173-175` le tait. Fix : bail `UPDATE jobs SET status='pending' WHERE status='running' AND updated_at < now - lease` au démarrage et à chaque tour. *(Medium)* — Fait le 2026-09-11 : `lease_secs` (300 s) dans `[jobs]`, `queue::requeue_stale` rend `pending` ou condamne `failed` selon `max_attempts`, appelée à chaque tour du worker ; `integration_jobs -- --ignored` 3 passés, les sept tests livrés exigés nommément dont `a_job_left_running_past_the_lease_returns_to_the_queue` et `a_job_abandoned_on_its_last_attempt_is_failed_rather_than_requeued` ; `integration_examples` 19 passés, `integration_docs` 13 passés.

## P1 — impact utilisateur fort

- [x] **4. [Bug] `rbs add mail`, `redis`, et par transitivité `auth` et `webhooks`, échouent sur tout projet sans `docker-compose.yml`** — `anchors.rs:226-236` déclare `SERVICES` `optional: true` « pour un projet SQLite », mais `plan/mod.rs:186-192` lève `FichierAbsent` sans consulter le drapeau (`grep optional src/plan/` : zéro) → observé : `rbs new demo --database sqlite` puis `rbs add auth` → « docker-compose.yml est introuvable », sans remède ; les treize routes auth sont inaccessibles au preset SQLite. Fix : sur ancre optionnelle et fichier absent, sauter l'action et l'annoncer (« service mailpit à monter vous-même »). *(Medium)* — Fait le 2026-09-11 : `cargo test -p rbs-cli --lib` 1151 passés dont `inserting_into_the_missing_file_of_an_optional_anchor_is_skipped_and_recorded`, `an_optional_anchor_missing_from_a_present_file_stays_an_anchor_error` et `adding_auth_to_a_sqlite_project_without_a_compose_plans_and_names_the_service` ; `integration_add` 14 passés dont `adding_auth_to_a_sqlite_project_succeeds_and_names_the_service_left_to_mount` (`--dry-run` puis pose réelle, code 0, aucun compose, `auth` et `mail` inscrites) ; le banc SQLite d'`integration_auth` pose `auth` sans compose ni contournement : passé. `rbs new` relaie le même bloc (« docker-compose.yml absent : le bloc destiné à `# <rbs:services>` est à reporter vous-même », suivi du YAML).
- [x] **5. [Bug] `rbs new --with rate-limit,redis` et `--preset full` engendrent un compteur en mémoire alors que Redis est installé** — `add/mod.rs:284` passe au rendu `features => metadonnees.features.clone()` (ce qui est déjà sur le disque) et `new.rs:188-194` installe dans l'ordre alphabétique (`rate-limit` < `redis`) → observé : `counter.rs` engendré dit « le projet ne porte pas le fragment redis » avec `features = ["health","rate-limit","redis"]` ; chaque réplica compte seul, et `doctor` ne le voit pas. Même cause : `--database sqlite --with docker,auth` échoue, `auth` posée avant `docker`. Fix : passer `installées ∪ à poser` au contexte et ordonner par dépendances. *(Easy)* — Fait le 2026-09-11 : contexte `installées ∪ à poser`, tri topologique (`requires` + « écrit le fichier porteur d'une ancre visée »), `rbs new` en un seul pipeline ; `--lib` dont `rate_limit_planned_with_redis_renders_the_shared_counter_whatever_the_order`, `docker_is_laid_down_before_the_fragments_that_extend_its_compose` (`[docker, mail, rate-limit, auth]`), `two_equivalent_requests_render_the_same_project` et `rate_limit_created_with_redis_counts_in_redis` ; `integration_new --include-ignored` 10 passés ; `blog-auth` régénéré (ordre `mail, rate-limit, auth`), `integration_examples` 19 passés, `integration_docs` 13 passés après mise à jour de `cli/new.md` et `tutorials/auth.md` EN/FR.
- [x] **6. [Bug] Sur SQLite, un jeton de réinitialisation périmé reste consommable jusqu'à minuit UTC** — `templates/features/auth/repository/one_time_token.rs.jinja:65` (`consume`), `:105` (`purge_expired`) et `refresh_token.rs.jinja:89` comparent `expires_at` à `Expr::current_timestamp()` ; sqlx écrit la colonne en RFC 3339 (`2026-01-01T…`) et SQLite rend `CURRENT_TIMESTAMP` avec une espace → comparaison textuelle, `'T' > ' '`, toute échéance du jour est « future » (`sqlite3 ":memory:" "select '2026-01-01T00:00:00+00:00' > '2026-01-01 23:59:59'"` → 1) ; `reset_ttl_secs = 3600` vaut jusqu'à 24 h. Fix : lier `Utc::now()` en paramètre, comme `jobs/queue.rs.jinja:131` le fait déjà. *(Medium)* — Fait le 2026-09-11 : huit `Expr::current_timestamp()` des deux dépôts remplacés par un `Utc::now().fixed_offset()` lié, écritures comprises ; rouge prouvé sur SQLite avec les dépôts d'origine (58 passés, 3 échecs = exactement les tests neufs) ; `integration_auth --include-ignored` 15 passés dont le nouveau banc `the_auth_tests_of_the_generated_project_pass_on_sqlite` exigeant nommément `an_expired_reset_token_is_refused_by_consume_and_removed_by_the_purge` et `an_expired_session_is_no_longer_listed`. Au passage, `created_at` des jetons est posé depuis Rust : le défaut `CURRENT_TIMESTAMP` est à la seconde sur SQLite et le banc rendait `sessions[0]` indéterminé.
- [x] **7. [Sécu] Le rate-limit lit `X-Forwarded-For` au premier élément, celui que le client écrit lui-même** — `templates/features/rate-limit/mod.rs.jinja:108-118` : `.split(',').next()` ; nginx, Traefik, Caddy et les ALB *ajoutent* l'adresse du pair en fin de liste → derrière un proxy (`trust_forwarded_for = true` obligatoire), `X-Forwarded-For: <aléa>` par requête donne une clé neuve à chaque coup : la borne 5/min de `/auth/login` tombe, et avec elle la protection Argon2 de la tâche 12 de l'ancien backlog. Fix : prendre le dernier élément, ou le n-ième depuis la droite via `trusted_hops`. *(Easy)* — Fait le 2026-09-11 : dernier élément (`rsplit`), sans `trusted_hops` ; rouge 2 échecs sur `blog-auth` avec les seuls tests reportés (`left: 203.0.113.9`), puis 10 passés dont `a_forged_address_ahead_of_the_list_does_not_change_the_key` ; `integration_auth --include-ignored` 15 passés (les tests `rate_limit` du projet engendré compris) ; `cli/add.md` EN/FR dit l'élément lu ; `integration_examples` 19, `integration_docs` 13.
- [x] **8. [Bug] La CI engendrée monte PostgreSQL, migre, puis n'exécute aucun test de CRUD ni de fragment** — `templates/features/ci/.github/workflows/ci.yml.jinja:70-76` lance `cargo test --workspace` alors que tout test touchant la base est `#[ignore = "joint la base du projet"]` (`templates/feature/tests.rs.jinja:166,244`, idem `auth`, `jobs`, `storage`) ; `grep -c ignored ci.yml.jinja` → 0 → la CI de l'utilisateur est verte à vide, et `docs/guides/testing.md:62-64` prescrit la même commande. Fix : `cargo test --workspace -- --include-ignored` (+ `--no-fail-fast`) dans la CI engendrée et la doc. *(Easy)* — Fait le 2026-09-11 : `cargo test --workspace --no-fail-fast -- --include-ignored` dans `ci.yml.jinja`, même commande dans `guides/testing.md` EN/FR ; `the_workflow_runs_the_tests_that_reach_the_database` vu rouge (« le workflow laisse de côté les tests ignorés ») puis vert ; `integration_docs` 13 passés ; `docs` : `npm run typecheck` ok, `npm test` 16 passés ; aucun exemple ne porte `ci` (`grep -l '"ci"' examples/*/Cargo.toml` vide).
- [x] **73. [Bug] `rbs add webhooks` sur un projet d'avant 1.5.0 échoue à la compilation : `config` est déjà consommé quand l'ancre `state_init` est sous `core:`** — `anchors.rs:195` (`STATE_INIT.after = "Ok(Self {"`) et `templates/project/src/state.rs.jinja:15-20` posent l'ancre avant `core: CoreState::new(db, config)` depuis 1.5.0, mais un projet antérieur la porte après ; `add` insère sans regarder la position et le fragment écrit `Sender::from_config(&config)?` → `error[E0382]: borrow of moved value: config`, seul le CHANGELOG prévient. Fix : dans l'étape *vérifier* d'`add`, refuser quand `core: CoreState::new(` précède l'ancre, avec le bloc à déplacer affiché ; ou relocalisation par `upgrade`. *(Easy)* — Fait le 2026-09-12 : champ `before` sur les `[[anchors]]` d'un manifeste (`webhooks` seul le pose : `core: CoreState::new(`), `anchors::precedes` jugé sur le fichier tel qu'il est sur le disque, `plan::Error::MalPlacee`, remède « remontez ce bloc au-dessus de … » avec le bloc ; `cargo test -p rbs-cli --lib` 1157 passés dont `webhooks_refuses_a_state_init_anchor_left_below_core_and_shows_the_block_to_move` ; `integration_add` 15 passés dont `webhooks_on_a_project_whose_state_init_anchor_follows_core_refuses_and_shows_the_block` (sortie 1, arbre intact) ; `integration_docs` 13, `integration_examples` 19 ; clippy et fmt propres. La relocalisation par `upgrade` reste un candidat backlog.

## P2 — impact moyen

- [x] **9. [Sécu] SSRF par l'URL d'abonnement webhook : ni filtre de schéma ou d'IP, ni blocage des redirections** — `templates/features/webhooks/dto.rs.jinja:10` ne pose que `#[validate(url)]`, `delivery.rs.jinja:103-105` construit le `reqwest::Client` sans `redirect(Policy::none())` et `:122-131` POSTe vers l'URL telle quelle → `http://169.254.169.254/…` ou une URL publique en 302 vers l'interne, rejouée cinq fois avec un corps JSON. Fix : `https` seul hors développement, résolution DNS puis rejet des plages privées/loopback/link-local, redirections interdites. *(Medium)* — Fait le 2026-09-12 : `target.rs.jinja` (politique liée au profil, résolveur `reqwest::dns::Resolve` filtrant, `redirect::Policy::none()`, livraison interdite abandonnée sans réessai) ; `integration_webhooks -- --ignored` : 1 passé, 11 tests ordinaires et 11 sous conteneur exigés nommément dont `an_admin_subscribing_a_private_url_gets_400` et `a_delivery_to_a_blocked_target_is_abandoned_not_retried` ; l'ancre `state_init` passe avant `core:` dans le squelette (voir tâche 73).
- [x] **10. [Sécu] Un jeton de rafraîchissement déjà révoqué permet de déconnecter le compte à volonté pendant 30 jours** — `templates/features/auth/service/session.rs.jinja:71-94` : `find_refresh_token` ne filtre pas `revoked_at`, `consume` rend `false` et `revoke_sessions_of` ferme tout ; `repository/refresh_token.rs.jinja:62-67` admet qu'aucune colonne ne distingue une ligne *tournée* d'une ligne *révoquée* → l'attaquant expulsé par un reset rejoue son jeton mort à chaque reconnexion de la victime. Fix : `replaced_at` (rotation) distinct de `revoked_at` (fermeture), révocation de famille sur rotation seulement. *(Medium)* — Fait le 2026-09-12 : `refresh_tokens.replaced_at`, `rotate`/`close` dans `repository/refresh_token.rs.jinja`, la ligne rejouée fermée à son tour ; `integration_auth -- --ignored` : 8 passés, dont le banc SQLite, avec `a_refresh_closed_by_logout_when_replayed_leaves_the_other_sessions_open` et `a_replayed_token_presented_again_gets_401_and_closes_nothing_more`.
- [x] **11. [Sécu] Le jeton d'accès survit à toute révocation pendant `access_ttl_secs`, rôle figé compris** — `crates/rbs-core/src/extract.rs:42-56` : `Identity` ne fait que `jwt::verify` ; `service/password.rs.jinja:122` et `session.rs.jinja:83,164` n'écrivent que `refresh_tokens.revoked_at` ; `guard.rs.jinja:82-88` relit l'utilisateur sans rien comparer au jeton → après un reset-password « sessions révoquées », l'attaquant garde 15 min d'accès ; un admin rétrogradé reste admin. Fix : `users.sessions_revoked_at` comparé à `iat` dans une garde, ou `jti` vérifié en base dans `VerifiedIdentity`. *(Medium)* — Fait le 2026-09-12 : `HasAuth::accept` (méthode fournie, `rbs-core` 1.5.0, `extract.rs:51`), `users.sessions_revoked_at` estampillée par `close_every_session`, `issue` émet après la seconde de révocation, rôle comparé à la ligne ; `integration_auth -- --ignored` : 8 passés, dont `the_tests_of_a_crud_generated_under_auth_pass` (le `token()` engendré crée un compte réel) ; `cargo test -p rbs-core --all-features` : 138 passés.
- [x] **12. [Sécu] L'adresse électronique n'est jamais normalisée : pré-inscription d'un compte *vérifié* sous l'adresse d'autrui** — `templates/features/auth/dto.rs.jinja:11` `#[validate(email)]` seul, `repository/user.rs.jinja:16-21` compare `Column::Email.eq(email)` à l'octet près, `migration.rs.jinja:16-20` `unique_key()` sensible à la casse → l'attaquant inscrit `Victime@ex.fr`, la victime reçoit et clique le lien de vérification, le compte de l'attaquant porte son adresse vérifiée ; `forgot-password` sur l'autre casse rend 202 sans rien envoyer. Fix : `trim().to_lowercase()` avant `register`, `login`, `forgot`, `resend`. *(Easy)* — Fait le 2026-09-12 : `normalise` dans `service/mod.rs.jinja`, appliquée à `register`, `login`, `request_reset`, `verification::request` ; `integration_auth -- --ignored` : 8 passés, avec `login_ignores_the_case_of_the_address` et `an_address_taken_in_another_case_is_a_conflict` ; le `trim` est prouvé en unitaire, `#[validate(email)]` refusant les blancs avant le service.
- [x] **13. [Bug] `change-password` et `reset-password` enchaînent quatre écritures hors transaction** — `templates/features/auth/service/password.rs.jinja` : `reset` = `consume` → `set_password` → `invalidate_pending` → `revoke_sessions_of`, `grep transaction features/auth/` → zéro, tous les repos prennent `&DatabaseConnection` et non `&impl ConnectionTrait` (à l'inverse de `jobs::enqueue`, `queue.rs.jinja:86`) → un échec entre `consume` et `set_password` brûle le jeton sans changer le mot de passe ; un `change` qui échoue avant la révocation laisse ouvertes les sessions d'un compte peut-être compromis. Fix : `TransactionTrait` sur les deux services. *(Medium)* Revue du 2026-09-12 : `refresh` a le même trou entre `rotate` et `issue` — une erreur entre les deux tourne la session sans en rendre une neuve. — Fait le 2026-09-13 : les dépôts d'`auth`, `issue` et `close_every_session` prennent `&impl ConnectionTrait` ; `change`, `reset`, `refresh`, `verify` et `revoke_sessions` ouvrent une transaction (`db.begin()` → écritures → `commit`), sur `Replayed` la révocation se committe avant le 401 ; rouge prouvé par quatre `E0308` (`&DatabaseTransaction` là où `&DatabaseConnection` était attendu) ; `integration_auth -- --ignored` : 8 passés, 322,9 s, suite engendrée 72 passés dont `a_rolled_back_transaction_leaves_the_token_and_the_password_untouched` et `a_rolled_back_rotation_leaves_the_session_open_and_opens_no_other` ; `integration_examples` 19, `integration_docs` 14. Limite de la preuve : l'atomicité d'un service sous une panne injectée au milieu de sa suite n'est pas éprouvée de bout en bout (aucune contrainte ne fait échouer `set_password` à la demande) — elle s'arrête aux dépôts et à la forme des services.
- [x] **14. [Bug] Aucun arrêt gracieux, et `logs::shutdown()` n'est appelé par aucun projet engendré** — `templates/project/src/main.rs.jinja:28-33` : `axum::serve(...).await?` nu, `grep -rn 'graceful\|logs::shutdown' templates/project` → 0 alors que `rbs-core/src/logs/mod.rs:140` vide le lot OTLP → chaque `docker stop` coupe les requêtes en vol, perd le dernier lot de traces et interrompt un worker en pleine réservation (aggravé par la tâche 3). Fix : `tokio::signal` (Ctrl-C + SIGTERM) → `.with_graceful_shutdown()` → `logs::shutdown()` ; ancre optionnelle `<rbs:shutdown>` pour `jobs`/`scheduler`. *(Medium)* — Fait le 2026-09-13 : module `rbs_core::shutdown` (`CancellationToken` + `TaskTracker`, Ctrl-C et SIGTERM) porté par `CoreState::shutdown()`, sans quinzième ancre — le contenu de l'ancre `startup` ne change pas, un `add jobs` sur un projet antérieur compile toujours ; `main.rs.jinja` sert en `with_graceful_shutdown`, attend les tâches de fond au plus `[server] shutdown_timeout_secs` (30) puis appelle `logs::shutdown()` ; worker et ticker finissent le job en cours ; `integration_jobs -- --ignored` : rouge sur les templates d'avant (« le processus n'est pas sorti proprement après SIGTERM ») puis 4 passés, 146,6 s, dont `a_sigterm_lets_the_job_in_progress_finish_before_the_process_exits` ; `integration_scheduler` 2, `integration_new` 5, `integration_observability` 1, `integration_examples` 19, `integration_docs` 14 ; `rbs-core --all-features` 144. Non prouvé par test : le drainage d'une requête HTTP lente (aucune route engendrée ne l'est ; repose sur axum).
- [x] **15. [Perf] Le worker de jobs exécute strictement un job à la fois, sans concurrence ni backoff** — `templates/features/jobs/worker.rs.jinja:33-44` : `loop { reserver → execute(...).await }`, `config.rs.jinja` n'expose pas de `concurrency`, `queue.rs.jinja:264` : `retry_delay_secs` constant ; une livraison webhook attend jusqu'à 10 s (`webhooks/config.rs.jinja:16`) → `emit` vers 100 abonnés dont un tiers est lent bloque la file 5 min, mails et échéances derrière. Fix : `Semaphore`/`JoinSet` borné par `[jobs] concurrency`, délai doublé par tentative. *(Medium)* — Fait le 2026-09-13 : `[jobs] concurrency` (défaut 4) et `retry_max_delay_secs` (3600), `JoinSet` borné drainé à l'arrêt, délai `retry_delay_secs × 2^(tentative−1)` plafonné ; un job qui panique n'emporte plus le worker ; rouge sur le worker séquentiel (« la ligne est restée `running` ») puis `integration_jobs -- --ignored` 4 passés, 43,2 s, dont `jobs_run_side_by_side_up_to_the_configured_concurrency` ; `integration_examples` 19, `integration_docs` 14 ; guide `jobs` et page `doctor` EN/FR.
- [x] **16. [Bug] Une expression cron modifiée ne prend effet qu'après la prochaine occurrence de l'ancienne** — `templates/features/scheduler/sync.rs.jinja:44-47` : `if Entity::find_by_id(kind)…is_some() { continue; }`, l'expression n'est ni stockée ni comparée ; le test `tests.rs.jinja:199` ne rejoue que la même expression → passer de `0 3 1 * *` à `*/5 * * * *` laisse le prochain tick au 1er du mois, sans log. Fix : colonne `expression`, recalcul de `next_run_at` si elle diffère. *(Easy)* — Fait le 2026-09-12 : `integration_scheduler -- --ignored` : rouge avant (`a_changed_expression_moves_the_next_occurrence`, `left == right == 2026-10-01T03:00`), puis 2 passés en 228 s, les neuf tests sous conteneur exigés nommément sur PostgreSQL, MySQL et SQLite ; sans colonne ni migration, `next_run_at` est recalculé quand l'échéance à venir diverge de l'occurrence calculée ; guide `scheduler` EN/FR réécrit.
- [x] **17. [Bug] Client TypeScript : un `Option<Struct>` rend `unknown | T`, soit `unknown`** — `client/document.rs:268` + `client/ts.rs:101` : utoipa rend `Option<Struct>` en `oneOf: [{type:"null"}, {$ref}]`, `primitif("null")` tombe dans `_ => "unknown"` ; seul `type: ["string","null"]` est testé (`ts.rs:682`) → tout DTO à objet imbriqué optionnel perd son type côté client. Fix : `"null"` → `null`. *(Easy)* — Fait le 2026-09-12 : `cargo test -p rbs-cli --lib client::ts` : rouge (`unknown | S`) puis 30 passés, `an_optional_struct_is_a_union_of_null_and_its_reference` rend `null | S`.
- [x] **18. [Bug] `generate crud --with-upload` code `crate::modules::storage` mais ne vérifie que la présence de la feature** — `generate/command.rs:246-250` teste `features.iter().any(|f| f == "storage")`, `templates/feature/service.rs.jinja:54` écrit `use crate::modules::storage::…` → sur un projet d'avant 1.3.0 (`src/storage/`), la génération réussit et le projet ne compile plus ; la note 1.3.0 ne cite pas `generate`. Fix : tester `src/modules/storage/mod.rs`, ou résoudre le chemin comme `crate_path`. *(Easy)* — Fait le 2026-09-12 : `cargo test -p rbs-cli --lib generate::command` : rouge (la génération aboutissait) puis vert, `upload_on_a_storage_fragment_left_at_the_root_is_refused_and_names_the_expected_path` ; `integration_docs -- --include-ignored` 14 passés.
- [x] **19. [Bug] Le stockage fichiers écrit en place, sans fichier temporaire ni `rename`** — `templates/features/storage/files.rs.jinja:40` : `fs::write(&path, content)` → un `get` concurrent pendant le dépôt, ou un crash au milieu, sert un objet tronqué ; un nouveau `put` sur la même clé expose d'abord un fichier vide ; `available()` (`:71`) fait un `create_dir_all` à chaque `/health`. Fix : écrire `.partiel` puis `rename`. *(Medium)* — Fait le 2026-09-13 : `put` écrit `<cible>.<uuid>.partiel` dans le même répertoire, `sync_all` puis `rename`, temporaire retiré sur échec ; `FileStorage::new` crée la racine et rend `Result` ; `available()` ne fait plus que `fs::metadata` ; rouge 5/5 sur l'ancien `put` (corps de 0 à 512 octets servis à un lecteur concurrent) puis `a_put_on_an_existing_key_never_exposes_an_empty_object`, `a_put_leaves_no_temporary_file_behind` et `the_probe_reports_a_root_that_vanished` verts ; `integration_storage -- --ignored` 1 passé, 89,7 s ; `integration_examples` 19, `integration_docs` 14.
- [x] **20. [Bug] `--with-upload` livre trois routes de contenu sans aucun test engendré** — `templates/feature/tests.rs.jinja` : zéro occurrence de `with_upload` ; `examples/file-drop/src/uploads/controller.rs:154-213` porte `put_content`/`get_content`/`head_content`, `tests.rs` n'a aucun `/content` ; seul `tests/integration_crud.rs:775-809` les exerce, côté rbs → le `cargo test -- --ignored` de l'utilisateur ne verra jamais une régression sur PUT/GET/HEAD, 404 sans contenu, 401 sous auth. Fix : bloc `{% if with_upload %}` dans `tests.rs.jinja`. *(Medium)* — Fait le 2026-09-13 : bloc `{% if with_upload %}` de `tests.rs.jinja` : `the_content_round_trips_through_put_get_and_head`, `an_unknown_id_has_no_content`, `a_content_beyond_the_limit_returns_413`, et sous `auth` `an_anonymous_content_request_returns_401`, exigés nommément par `integration_crud` (1 passé, 43,6 s) et `integration_auth` (1 passé, 50,7 s) ; rouge sur trois tests de rendu avant le gabarit ; `file-drop` régénéré, `integration_examples` 19, `integration_docs` 14, `--lib` 1173.
- [x] **21. [Bug] `to_singular` rend `CreateNew` / `NotFound("new")` pour `news`, et tout pluriel invariable en `-s`** — `generate/feature.rs:287-311` : `FINALES_NON_PLURIELLES = ["ss","us","is"]`, tout autre `s` final tombe → observé : `rbs generate crud news` produit `pub struct CreateNew` et `let new = repository::find(…)`, schémas OpenAPI et interfaces TS publics compris. Fix : `--singular <nom>` sur `generate crud`, plus une courte liste d'invariables (`news`, `series`, `species`). *(Easy)* — Fait le 2026-09-12 : `--singular <NOM>` sur `crud` et `feature`, `INVARIABLES = [news, series, species]` ; `cargo test -p rbs-cli --lib` : rouge (`left: "new"`) puis 1168 passés dont `an_imposed_singular_overrides_the_heuristic` (`NewsItem`) ; transcriptions `--help` EN/FR régénérées, `integration_docs -- --include-ignored` 14 passés.
- [x] **22. [Bug] `modules` est accepté comme nom de feature et casse tous les `rbs add` suivants** — `generate/name.rs:30` : `MODULES_DU_SQUELETTE = ["main","router","openapi","state","health"]`, sans `modules` → observé : `rbs generate crud modules` réussit, `src/modules/mod.rs` devient un CRUD, puis `rbs add cors` → « ancre `<rbs:modules>` introuvable » avec un bloc à coller trompeur. Fix : ajouter `modules` (et `seeds`, `bin`) à la liste. *(Easy)* — Fait le 2026-09-12 : `cargo test -p rbs-cli --lib generate::name` : rouge (« modules » doit être refusé) puis 5 passés, `modules`, `seeds`, `bin` et `lib` refusés.
- [x] **23. [UX] Les réponses HTTP engendrées sont bilingues, et `--lang` ne couvre que `AGENTS.md`** — `crates/rbs-core/src/error.rs:104-105,116,144` : `title` anglais (`"Not Found"`, `"Validation failed"`) et `detail` français (`"{ressource} introuvable"`, `"une erreur interne est survenue"`), test `:257` le fige ; `cli.rs:47` : `--lang` = « Langue de l'AGENTS.md engendré » → un client d'un projet `--lang en` reçoit des `detail` en français. Fix : une langue dans `[server]` lue par `Error::into_response`, `detail` fourni par l'appelant sinon. *(Medium)* — Fait le 2026-09-13 : `rbs_core::Lang` et `[server] lang` (`Config::load` pose la langue, résolution paresseuse pour `bin/openapi`), `title`/`detail` et descriptions OpenAPI communes en fr|en, messages des gabarits rendus dans la langue que `add`/`generate` lisent dans `[server] lang` seul ; décisions du mainteneur : `title` français gardés (rupture écrite au CHANGELOG), règle de locale inchangée, textes OpenAPI par opération hors périmètre. `integration_lang` (projet `--lang en`, 409/422/404/429 entièrement anglais) 1 passé ; passe finale `cargo test --workspace --all-features --no-fail-fast -- --include-ignored` sur `b95f76d` : 30 binaires, 1514 passés, 0 échec.
- [x] **24. [UX] `rbs doctor` laisse cargo inonder le rapport de « Compiling … »** — `cargo.rs:28-33` ne pipe que `stdout`, `stderr` est hérité, et `migrate/mod.rs:163` lance `cargo run -p migration` sans `--quiet` → observé : 50 lignes `Updating / Locking / Compiling` avant un verdict de 8 lignes, y compris en `--json` sur stderr. Fix : `-q`, ou capturer `stderr` et ne le rejouer qu'en cas d'échec. *(Easy)* — Fait le 2026-09-12 : stderr capturée avec stdout et rejouée sur échec seul ; `cargo test -p rbs-cli --lib cargo::` : rouge (puits vide) puis 2 passés au faux `cargo` ; `integration_doctor -- --include-ignored` 7 passés ; `integration_docs` rouge sur `doctor.md:212` (lignes `Finished`/`Running` disparues du rendu réel) puis 14 passés après mise à jour des transcriptions.
- [x] **25. [Arch] Trois contrôleurs `auth` portent le même bloc d'orchestration courriel, et l'un dépasse 200 lignes** — `templates/features/auth/controller/session.rs.jinja:37-56`, `verification.rs.jinja:30-50`, `password.rs.jinja:56-80` enchaînent `service::request` → `state.mail().send_template_detached` → `tracing::error!` (≈20 lignes + 5 de commentaire, copiées trois fois) : le contrôleur orchestre deux services, ce que `CLAUDE.md` réserve au service ; `examples/blog-auth/src/auth/controller/session.rs` fait 208 lignes. Fix : remonter l'envoi dans le service, un helper `notify` unique. *(Medium)* — Fait le 2026-09-13 : helper privé `notify` dans `service/mod.rs`, `verification::send_link` et `password::send_reset_link`, contrôleurs réduits à un appel de service (ils passent `state.mail()` sans rien envoyer) ; `examples/blog-auth/src/auth/controller/session.rs` 208 → 178 lignes ; test `the_auth_mail_leaves_from_the_service_layer_only` rouge avant ; `integration_auth` 16 passés sur l'arbre fusionné, passe finale sur `b95f76d` 1514 passés, 0 échec.
- [x] **26. [Doc] `AGENTS.md` livré : compte de fichiers faux, recette périmée, renvoi de ligne faux, silence sur les routes fermées** — `templates/agents/fr.md.jinja:24,54` (idem `en.md.jinja`) « six fichiers » alors que la feature en a sept depuis `filter.rs` ; `:40` « `rbs generate feature webhooks` » alors que `webhooks` est un fragment `add` depuis 1.2 ; `:89` « la seconde ligne est celle qui compte » désigne `clippy`, pas `cargo test -- --ignored` ; `grep 'Identity\|require_role' agents/` → 0 → un agent qui ajoute une route à la main sur un projet `auth` ignore `Identity`, `security(("bearer"=[]))` et les 401/403. Fix : corriger les quatre points, FR et EN, et régénérer `examples/*/AGENTS.md`. *(Easy)* — Fait le 2026-09-13 : quatre points corrigés FR/EN — sept et huit fichiers tirés de `generate::command::fichiers`, recette `generate feature reports`, « quatrième ligne », paragraphe `Identity`/`require_role`/`security`/401-403 inconditionnel (`add` ne réécrit pas le guide) — puis ligne `--lang` = langue du projet ; `cargo test -p rbs-cli --lib agents::` 3 rouges → 39 passés, `integration_examples` 19 passés.
- [x] **27. [Doc] `README.md` et `README.fr.md` annoncent « Version 1.2.0 »** — `README.md:12`, `README.fr.md:12` vs `Cargo.toml:10` (1.4.0) et `CHANGELOG.md:13` ; le masque `masque_version` d'`integration_docs.rs:301` ne couvre que les blocs `rbs:transcript` → la vitrine GitHub ment de deux versions, et rien ne le gardera la prochaine fois. Fix : passer à 1.4.0 et garder la ligne par `integration_docs`. *(Easy)* — Fait le 2026-09-13 : `Version 1.5.0.` (la version avait encore bougé depuis le scan) ; test `the_readmes_announce_the_version_of_the_workspace` dans `integration_docs`, rouge avant, vert après.
- [x] **28. [Doc] `CLAUDE.md` périmé sur trois points, et `parite.mjs` réclame un `IMPROVE_OLD.fr.md`** — `CLAUDE.md:7` « cinq jalons, de la v0.1 à la v1.1 » (1.2 → 1.4 sont sorties), `:70` liste huit commandes sans `completions` (`cli.rs:119`), `:18,24` désignaient un `IMPROVE.md` absent jusqu'à ce scan ; `docs/scripts/parite.mjs:36` `RACINE_MONOLINGUE` ne connaît pas `IMPROVE_OLD.md` → observé : « 1 écart : IMPROVE_OLD.md : aucune version française ». Fix : réécrire « État du dépôt », ajouter `completions`, inscrire `IMPROVE_OLD.md` dans `RACINE_MONOLINGUE`. *(Easy)* — Fait le 2026-09-13 : `RACINE_MONOLINGUE` inscrit `IMPROVE_OLD.md` (`parite.mjs` : 1 écart → 0) ; « État du dépôt » réécrit (six jalons, 1.2 à 1.4 publiées, 1.5.0 prête, cinq projets d'exemple), `completions` et `filter` ajoutés ; le point « `IMPROVE.md` absent » était déjà obsolète.
- [x] **29. [Doc] `ROADMAP.md` ignore tout ce qui a été livré depuis 1.1** — `ROADMAP.md:121-128` : tableau arrêté à « v1.1 Agents » ; `grep -cE 'webhook|scheduler|audit|client|curseur|soft' ROADMAP.md` → 0 → le document censé porter les jalons ne connaît ni webhooks, ni scheduler, ni audit, ni le client TS. Fix : trois lignes 1.2 / 1.3 / 1.4 dans le tableau et un paragraphe par jalon. *(Easy)* — Fait le 2026-09-13 : sections v1.2 à v1.5 et quatre lignes au tableau d'état (1.5 « prête, non publiée ») ; `grep -cE 'webhook|scheduler|audit|client' ROADMAP.md` 0 → 4.
- [x] **30. [Doc] La page `completions` transcrit à la main onze fragments, il y en a treize** — `docs/docs/cli/completions.md:88-96` et FR `:92-100` (« The eleven names… ») listent `audit … redis storage` sans `scheduler` ni `webhooks` ; sortie réelle de `rbs completions bash` : `… redis scheduler storage webhooks` ; aucun `rbs:transcript` dans la page, donc `integration_docs` ne la voit pas. Fix : bloc transcrit et gardé. *(Easy)* — Fait le 2026-09-13 : blocs `rbs:transcript cmd="rbs completions bash|zsh" extrait="oui"`, prose sans compte ; `integration_docs` 14 passés, contre-épreuve (`webhooks` retiré du bloc) rouge.
- [x] **31. [Doc] Les guides `webhooks`, `scheduler` et `audit` sont écrits à la main, sans extrait tiré d'`examples/`** — `grep -c 'file=' docs/docs/guides/{webhooks,scheduler,audit}.md` → 0, 0, 0 contre 13 pour `auth.md` (14/12/12 fences manuelles), aucun exemple ne porte ces fragments ni `cors`, `docker`, `ci` (`examples/*/Cargo.toml:14`) → contradiction avec `CLAUDE.md` (« aucune ligne écrite à la main »), même dette que la tâche 99 de l'ancien backlog. Fix : cinquième exemple portant les six fragments manquants, `README` des exemples, guides réécrits sur `file=… region=…`. *(Medium)* — Fait le 2026-09-13 : `examples/event-hub` (six fragments + CRUD `orders` dont la création écrit l'audit et émet `order.created` dans sa transaction) ; `integration_examples` 22 passés, tests de l'exemple contre PostgreSQL 17 : 145 passés ; `grep -c 'file='` webhooks 0 → 2, scheduler 0 → 4, audit 0 → 5, EN et FR ; `npm run build` 0, `npm test` 16/16, parité 0 écart, `integration_docs --include-ignored` 15 passés. A exigé un correctif hors backlog : l'ancre `migration_modules` triée, sans quoi `add webhooks` rendait `cargo fmt --check` rouge.
- [x] **32. [Dette] `argon2` et `jsonwebtoken` ont une majeure disponible** — `Cargo.toml:18` `argon2 = "0.5.3"` → 0.6.0, `:32` `jsonwebtoken = "10.3.0"` → 11.0.0 (`cargo update --dry-run --verbose` : « available: v11.0.0 »), toutes deux sous la feature `auth` de `rbs-core` ; côté engendré, `redis = "1.6"` (1.7.0) et `aws-sdk-s3 = "1.144"` (1.146.0). Fix : monter les deux majeures avec leur note, et les deux mineures dans les `feature.toml`. *(Medium)* — Fait le 2026-09-13 : argon2 0.6.0, jsonwebtoken 11.0.0, `redis = "1.7"`, `aws-sdk-s3 = "1.146"` ; fixtures d'un hash argon2 0.5.3 et d'un jeton signé par jsonwebtoken 10 toujours acceptés ; `rsa` toujours tiré par `rust_crypto`, l'exception `RUSTSEC-2023-0071` reste ; `cargo audit --deny warnings` 0, `cargo deny` ok, `cargo semver-checks` sans rupture ; passe finale sur `b95f76d` 1514 passés, 0 échec.
- [x] **33. [Dette] Aucune veille sur les dépendances npm de `docs/`, ni Dependabot, ni décision écrite sur `cargo-deny`** — `.github/workflows/docs.yml:45-51` : `npm ci`, `typecheck`, `test`, pas d'`npm audit` ; pas de `.github/dependabot.yml` ; `grep -rniE 'cargo.deny|deny\.toml' .` → vide (et `cargo deny check` échoue sur sa config par défaut, faute de `deny.toml`) → observé : `npm audit --omit=dev` remonte des *high* (image-size, js-yaml, qs, serialize-javascript) que la CI ne verra jamais. Fix : `npm audit --audit-level=high` dans `docs.yml`, Dependabot cargo + npm, et un `deny.toml` ou une ligne qui l'écarte. *(Medium)* — Fait le 2026-09-13 : `npm audit fix` (js-yaml 4.3.2) + override serialize-javascript 7.1.1, `audit-ci@7.1.0` bloquant à `high` avec `docs/audit-ci.jsonc` (image-size, sans correctif publié) — vert, rouge si une exception manque ; `deny.toml` (11 licences, crates.io seul, doublons en warn) + job `deny` — `bans ok, licenses ok, sources ok`, rouge sans MIT ; `.github/dependabot.yml` cargo, npm, actions. Les deux jobs ne sont éprouvés sur GitHub qu'au premier push.

### Features P2 — fort ratio valeur/effort

- [x] **34. [Feature] `rbs test` : services, migrations, puis `cargo test -- --include-ignored`** — `dev/mod.rs:199-217` fait déjà compose → attente base → `migrate up` ; `docs/guides/testing.md:62-64` prescrit trois commandes à la main → une seule commande, la même que la CI engendrée après la tâche 8, `--no-fail-fast` inclus. *(Medium)* — Fait le 2026-09-14 : `integration_test --include-ignored` 2 passés (projet engendré vert, puis rouge avec le code 101 de `cargo test` propagé) ; passe finale sur `806ef9a` : workspace 1636 passés, 28 binaires d'intégration à 0 échec.
- [x] **35. [Feature] `/health/live` et `/health/ready` distincts** — `templates/project/src/health/controller.rs.jinja:7-22` : une seule route qui sonde la base ; `grep -rniE 'ready|live' templates/project/src` → 0 → une liveness qui dépend de la base fait redémarrer l'API en boucle quand c'est la base qui tombe. Fix : `/health/live` = 200 sans dépendance, `/health` inchangé, healthchecks Docker sur `/live`. *(Easy)* — Fait le 2026-09-14 : `integration_new --include-ignored` 11 passés dont `the_liveness_route_outlives_the_database` (base arrêtée : `/health` 503, `/health/live` 200) ; `integration_routes` 2 passés (`/health/live` en `public`) ; conteneur réel `--with docker` `healthy`, et toujours `healthy` 40 s après `docker stop` de la base ; `integration_examples` 22 passés.
- [x] **36. [Feature] `rbs generate job <nom> [--every "<cron>"]`** — trait `Job` (`templates/features/jobs/mod.rs.jinja:32-37`), ancre `<rbs:jobs>` déjà employée par `webhooks/feature.toml:70`, `Schedule::every` (`scheduler/mod.rs.jinja:119-120`) ; `cli.rs:138-206` ne connaît que `crud`, `feature`, `client` → un job s'écrit en copiant `demo.rs`, ce que `doctor` signale ensuite comme « hors CLI » (`doctor/agents.rs:140`). Reste à créer : une ancre pour le `pub mod` (`jobs/mod.rs.jinja:1-5` en dur) et une dans `schedules()`. *(Medium)* — Fait le 2026-09-14 : `integration_scheduler --include-ignored` 4 passés dont `a_generated_job_with_its_schedule_compiles_and_passes_the_project_tests` (fmt, clippy et tests du projet verts) ; `integration_json` 10 passés dont `generate job --json` ; `integration_examples` 22 passés ; ancres 14 → 16 ; sur un calendrier encore en `vec![]`, `doctor --fix` ne repose pas `schedules` et `doctor` n'avertit que (documenté).
- [x] **37. [Feature] Contrôles `doctor` pour les six fragments qui n'en ont aucun** — `doctor/mod.rs:303` `FEATURE_CHECKS: [(&str, Controle); 7]` = auth ×2, redis, mail, storage, jobs, observability ; rien pour `cors` (origines vides), `rate-limit`, `scheduler` (cron illisible = arrêt au boot), `webhooks` (ligne absente de `<rbs:jobs>` = ne compile pas), `audit`, `docker`/`ci` (`config/production.toml` absent) ; `doctor::section_check` fabrique un contrôle en quelques lignes. *(Easy)* — Fait le 2026-09-14 : sept contrôles, `ci` visant `.github/workflows/ci.yml` puisque le fragment ne pose pas `config/production.toml` ; validateur `src/cron.rs` croisé avec les tests du fragment ; `integration_doctor --include-ignored` 7 passés ; workspace 1636 passés.
- [x] **38. [Feature] `rbs generate crud --cursor`** — `rbs-core/src/pagination.rs:103,201` livre `Cursor`/`CursorPage` depuis 1.2.0, ids UUIDv7 ; `grep -rn Cursor templates examples` → 0 : la primitive publiée n'est rendue par aucun gabarit ni témoin engendré. Fix : le flag rend `list` en `CursorPage`, tests et client TS suivent. *(Medium)* — Fait le 2026-09-14 : `integration_cursor --include-ignored` 1 passé (entité `--cursor --soft-delete --with-upload --role admin`, 13 tests engendrés dont la marche par curseur sans doublon) ; `integration_client` 6 passés ; rendu par défaut inchangé (`integration_examples` 22 passés). Le client TS ne passe pas `tsc --strict`, à cause du bug préexistant des unions (hors lot).
- [x] **39. [Feature] `rbs routes [--json]` et `rbs openapi export [--out]`** — `templates/project/src/bin/openapi.rs.jinja` rend le document sans serveur et `client/document.rs:15-104` le parse déjà (chemins, méthodes, `operation_id`, `security`) ; `grep -n 'Routes\|Openapi' cli.rs` → rien → voir méthode / chemin / garde par route en une commande, `--json` pour un agent. *(Easy)* — Fait le 2026-09-14 : `integration_routes --include-ignored` 2 passés (`--json` : `/health` et `/health/live` en `public`, routes d'auth en `bearer`) ; `integration_client` 6 passés sur l'obtention factorisée ; table réelle citée dans `cli/routes.md`.
- [x] **40. [Feature] `--json` sur les plans d'`add`, `generate`, `upgrade` (avec `--dry-run`) et sur les erreurs d'ancre** — `doctor --json` existe (`cli.rs:105`, `doctor/json.rs`), `plan::Action` est un enum fermé à 9 variantes (`plan/action.rs:20-80`) sans `Serialize` ; `grep -rn json src/plan/ src/ui.rs` → 0 → critère de sortie v1.1 « non éprouvé sur un agent réel » : un agent parse aujourd'hui de l'ANSI. *(Medium)* — Fait le 2026-09-14 : `integration_json` 10 passés (plans d'`add`, `generate crud`, `generate job` et `upgrade`, appliqués ou non ; refus `ancre_absente` et `jobs_absent` ; `applique` à `false` sur une relance sans effet) ; codes stables énumérés dans `guides/agents.md` ; workspace 1636 passés.
- [ ] **41. [Feature] Binaires GitHub Releases + `cargo-binstall`** — `.github/workflows/release.yml` ne publie que sur crates.io ; `grep -rn binstall Cargo.toml README.md` → 0 ; `README.md:24-33` n'offre que `cargo install rbs-cli` → installer rbs sur un runner CI (pour `doctor --json`) coûte plusieurs minutes de compilation. Fix : `cargo-dist` ou `upload-rust-binary-action`, `[package.metadata.binstall]`. *(Medium)* — PARTIEL (2026-09-14) : jobs `release-github` et `binaires` (cinq cibles, `rbs` et `rbs-cli`, sommes sha256) et `[package.metadata.binstall]` livrés ; `actionlint` sans remarque, `parse-changelog CHANGELOG.md 1.5.0` extrait la section, `cargo package --list` sans avertissement ; la publication des archives et `cargo binstall rbs-cli` ne se prouvent qu'au prochain tag.
- [x] **42. [Feature] `rbs new` écrit un `CLAUDE.md` d'une ligne (`@AGENTS.md`)** — `grep -rn 'CLAUDE.md' templates new.rs agents.rs` → 0 ; `docs/guides/agents.md:15` affirme que Claude Code lit `AGENTS.md` de lui-même, ce qui n'est vrai que par l'import `@fichier` d'un `CLAUDE.md` → ferme le trou pour l'agent le plus probable sans dupliquer le guide. *(Easy)* — Fait le 2026-09-14 : tests `a_new_project_carries_a_claude_file_importing_its_agents_file` et les trois d'`upgrade` (créé si absent, jamais réécrit), dans le workspace à 1636 passés ; `integration_upgrade` 7 et `integration_new` 11 passés ; `CLAUDE.md` dans les cinq exemples (`integration_examples` 22 passés).

## P3 — polish et dette basse

- [x] **43. [Sécu] `POST /auth/register` reste un oracle d'énumération par le code de statut** — `templates/features/auth/service/session.rs.jinja:26-28` → `Error::Conflict` → 409 contre 201 ; la tâche 13 de l'ancien backlog a retiré l'adresse du message, pas le statut ; depuis 1.4 le courriel de vérification permet la réponse uniforme. Fix : 201 indiscernable, conflit signalé par courriel. *(Easy)* — Fait le 2026-09-14 : 202 sans corps à toute inscription, Argon2 dans les deux branches, `create` qui rend `None` sur violation d'unicité, courriel `inscription.html` au titulaire d'une adresse prise, `ADRESSE_PRISE` retiré ; `integration_auth --include-ignored` 16 passés sur l'arbre fusionné, qui exige désormais par leur nom `registration_returns_202_without_a_body`, `a_taken_address_returns_the_same_202_and_creates_nothing`, `a_taken_address_keeps_its_password` et `a_taken_address_costs_the_same_time_as_a_new_one` (ce dernier vu rouge sur l'ancien code) ; `integration_lang` 1 passé. Reste l'oracle en deux requêtes `register` puis `login`, assumé et documenté : tâche 76.
- [x] **44. [Sécu] `forgot-password` et `resend-verification` : le temps de réponse trahit encore l'existence du compte** — `templates/features/auth/controller/password.rs.jinja:56-59`, `verification.rs.jinja:29-32` : la branche « compte existe » fait UPDATE + INSERT + rendu minijinja + `parse::<Mailbox>` avant de répondre, l'autre un SELECT ; seul le SMTP est détaché (`:36-38`). Fix : `SELECT` seul dans la requête, tout le reste dans la tâche détachée. *(Easy)* — Fait le 2026-09-14 : la requête ne fait plus que `normalise` et un `SELECT` ; purge, invalidation, émission du jeton et rendu du courriel partent dans `detach` (`service/mod.rs.jinja`), leurs échecs au journal ; les tests HTTP qui comptent les jetons attendent par `eventually` ; `integration_auth --include-ignored` 16 passés sur l'arbre fusionné. Deux jetons vivants restent possibles pour deux demandes entrelacées — course antérieure, que le détachement élargit.
- [x] **45. [Sécu] Le mot de passe Redis atteint le journal au démarrage** — `templates/features/redis/mod.rs.jinja:39` et `rate-limit/counter.rs.jinja:28` : `format!("pool Redis inconstructible pour `{}`", config.url)` alors que `rbs-core/src/db.rs:77-84` masque celui de la base → Redis absent → secret sur stderr et dans le collecteur. Fix : même masquage, ou l'hôte seul. *(Easy)* — Fait le 2026-09-14 : `rbs_core::db::redact_url` (le masquage de `db::connect`, rendu public) cite l'URL dans `redis/mod.rs.jinja` et la branche redis de `rate-limit/counter.rs.jinja` ; sur l'arbre fusionné, `cargo test -p rbs-core` 134 passés dont `a_redis_url_loses_its_password`, test de rendu `the_redis_pool_error_masks_the_url_password` (vu rouge avant), `integration_redis --include-ignored` 1 passé, `integration_examples` 22 (`file-drop` suivi). Les angles morts de ce masquage textuel — mot de passe en query string, `@` non encodé — restent ceux qu'avait déjà `db::connect`.
- [x] **46. [Sécu] Les jetons de réinitialisation et de vérification voyagent en query string** — `templates/features/auth/config.rs.jinja:41-46` : `format!("{}/{path}?token={token}")` → journaux d'accès du front, historique, `Referer`. Fix : fragment `#token=` ou segment de chemin, consommation par POST inchangée. *(Easy)* — Fait le 2026-09-14 : `FlowConfig::link` rend `…/<chemin>#token=…` ; test engendré `a_link_carries_its_token_in_the_fragment`, exigé par son nom dans `integration_auth` (16 passés sur l'arbre fusionné) ; plus aucun `?token=` dans les templates, la doc ni les exemples, hors la note de montée qui décrit l'ancien comportement ; `npm run build` et `npm run parite` (45/45) verts.
- [x] **47. [Sécu] `.env` est écrit avec les droits du umask alors qu'il porte le secret JWT** — `new.rs:423` `fs::write` pour tout le squelette dont `.env` (`:623`), `plan/application.rs:87` idem pour le secret tiré par `add/installation.rs:194-211` ; aucun `set_permissions` hors tests → 0644 sur machine partagée. Fix : `OpenOptions::mode(0o600)` sous `cfg(unix)` pour `.env`. *(Easy)* — Fait le 2026-09-14 : `secret::write` pose 0600 sur le descripteur avant le contenu, pour `rbs new`, tout plan et sa restauration, `.env` préexistant en 0644 compris ; tests Unix `the_env_file_is_readable_by_its_owner_only`, `an_existing_env_rewritten_by_a_plan_ends_up_owner_only`, `an_env_restored_by_a_rollback_stays_owner_only` (vus rouges à 0o644) dans `--lib` 1394 passés sur l'arbre fusionné ; `integration_new --include-ignored` 11 passés. Hors Unix : non testé.
- [x] **48. [Sécu] La CI engendrée épingle ses actions par tag, non par SHA** — `templates/features/ci/.github/workflows/ci.yml.jinja:56,58,62` : `actions/checkout@v7`, `dtolnay/rust-toolchain@stable`, `Swatinem/rust-cache@v2` ; `permissions: contents: read` (`:8-9`) borne déjà le rayon. Fix : SHA complet + commentaire de version. *(Easy)* — Fait le 2026-09-14 : `checkout@3d3c42e… # v7.0.1`, `rust-toolchain@6bed076… # stable` avec `toolchain: stable` explicite, `rust-cache@6323deb… # v2.9.2` (SHA relus par `gh api`), et `.github/dependabot.yml` (github-actions, hebdomadaire) déposé par le fragment ; test `every_action_of_the_generated_ci_is_pinned_by_a_sha` (vu rouge avant) dans `--lib` 1394 ; `event-hub` suivi, `integration_examples` 22. Non prouvé : l'exécution sur GitHub, et que Dependabot sache monter `rust-toolchain`, épinglé sur un commit de la branche `stable` qu'aucun tag ne marque (le seul tag est `v1`).
- [x] **49. [Bug] `resend-verification` et `verify-email` acceptent un compte déjà vérifié et rajeunissent `email_verified_at`** — `templates/features/auth/service/verification.rs.jinja:14-37,47-58` : `request()` ne lit pas `email_verified_at`, `repository/user.rs.jinja:69` `mark_verified` pose `CURRENT_TIMESTAMP` sans condition → la date censée servir à « redemander une preuve aux plus anciennes » est réécrivable à volonté. Fix : 202 sans jeton si déjà vérifié, `WHERE email_verified_at IS NULL`. *(Easy)* — Fait le 2026-09-14 : `request` rend `None` pour une adresse déjà vérifiée (202, aucun jeton), `mark_verified` filtre `email_verified_at IS NULL` ; tests engendrés `a_verified_address_is_not_sent_a_new_token` et `verifying_again_keeps_the_first_date`, exigés par leur nom dans `integration_auth` (16 passés sur l'arbre fusionné).
- [x] **50. [Bug] `one_time_tokens` ne se purge jamais** — `templates/features/auth/repository/one_time_token.rs.jinja:102-103` : `#[allow(dead_code)] pub async fn purge_expired`, aucun appelant dans `templates/`, `src/` ni `examples/blog-auth` → seule table qui croît avec le trafic anonyme (une ligne par `register`, `forgot`, `resend`). Fix : une `Schedule` conditionnelle quand `scheduler` est présent, sinon purge à l'émission. *(Easy)* — Fait le 2026-09-14 : `purge_expired` est appelée par `open_reset` et `open` à chaque émission, dans la tâche détachée, et perd son `#[allow(dead_code)]` ; index `idx_one_time_tokens_expires_at` dans la migration, à créer à la main sur un projet déjà migré (note 1.5.0) ; test engendré `an_emission_purges_the_expired_tokens_of_every_account`, exigé par son nom dans `integration_auth` (16 passés). Garde faible : joué en parallèle, un test voisin purge aussi la table, et son rouge n'a été vu qu'en `--exact`.
- [x] **51. [Bug] Le commentaire de `filter.rs` affirme que `contains` échappe la valeur — faux** — `templates/feature/filter.rs.jinja:116-120` vs sea-orm `column.rs:241` : `format!("%{}%", s)` sans échappement → `{"title":{"contains":"%"}}` retourne tout, `_` est un joker ; paramètre lié, donc pas d'injection, mais un contrat documenté qui ment. Fix : échapper `%`, `_`, `\` avec `ESCAPE`. *(Easy)* — Fait le 2026-09-14 : `contains` échappe `!`, `%` et `_` et rend `LIKE … ESCAPE '!'` (`ce25306`) ; test engendré `contains_reads_percent_and_underscore_literally`, exigé par son nom dans `integration_crud` : rouge sur `hello-crud` et PostgreSQL 14 (chaque motif rendait les quatre lignes), puis vert ; arbre fusionné : `integration_crud` 8/0, `integration_examples` 22/0 ; la note 1.5.0 donne la fonction à coller.
- [x] **52. [Bug] `rbs dev` sur un projet MySQL nomme PostgreSQL dans son erreur d'URL** — `dev/mod.rs:66,115` : `UrlIllisible` dit « n'est pas une URL PostgreSQL exploitable », remède `postgres://…`, alors que `plan()` accepte MySQL via `a_un_serveur()`. Fix : message par moteur. *(Easy)* — Fait le 2026-09-14 : `UrlIllisible { database }` nomme le moteur du manifeste et donne son schéma (`b468743`) ; test `an_unreadable_url_on_a_mysql_project_names_mysql` rouge (« URL PostgreSQL ») puis vert ; arbre fusionné : `--lib` 1420 passés, 0 échec.
- [x] **53. [Perf] Compteur mémoire du rate-limit : au-delà de 10 000 clés vivantes, `retain` O(n) sous mutex à chaque requête** — `templates/features/rate-limit/counter.rs.jinja:74,106-107` : le balayage se déclenche sur `len() >= SEUIL` mais ne retire que les fenêtres échues → sous la charge que la limite doit absorber, chaque `hit` parcourt la table. Fix : balayer au plus une fois par intervalle. *(Easy)* — Fait le 2026-09-14 : `prochain_seuil` : le balayage suivant attend que la table ait doublé (`039c0b7`) ; rouge = non-compilation dans `blog-auth`, puis 12/12 dans `blog-auth` et `event-hub` ; arbre fusionné : `integration_auth` 16/0 (compteur mémoire), `integration_examples` 22/0.
- [x] **54. [Perf] Aucune compression ni cache HTTP dans le squelette engendré** — `templates/project/src/router.rs.jinja` n'empile que `TimeoutLayer`, `Cargo.toml.jinja:39` `tower-http` avec `["timeout"]` seul ; `grep -rn CompressionLayer templates` → 0 → `/openapi.json` (plusieurs centaines de Ko depuis les six schémas de filtres) part non compressé. Fix : `CompressionLayer` dans `<rbs:layers>`, feature `compression-gzip`. *(Easy)* — Fait le 2026-09-14 : `CompressionLayer` dans `<rbs:layers>`, `tower-http` en `compression-gzip` (`16e2594`) ; test engendré `the_list_travels_compressed_when_the_client_accepts_it` rouge (`content-encoding` absent) puis vert dans `hello-crud` ; arbre fusionné : `integration_crud` 8/0, `integration_new` 11/0, `integration_examples` 22/0 ; note 1.5.0 pour les projets antérieurs.
- [ ] **55. [Perf] `storage` charge chaque objet entièrement en mémoire, et le copie une fois de plus** — `templates/features/storage/mod.rs.jinja` : `put(content: Vec<u8>)`, `get() -> Vec<u8>` ; `s3.rs.jinja:80` `collect().to_vec()` ; `templates/feature/controller.rs.jinja:297-306` `Bytes` → `to_vec()` → 20 GET concurrents de 10 Mio = 400 Mo résidents ; un objet déposé par une autre voie n'est borné par rien. Fix : `Body::from_stream` en lecture, `Bytes` de bout en bout. *(Medium)*
- [x] **56. [Perf] `mark_done` / `retry_or_fail` réécrivent la ligne entière, payload compris, pour trois colonnes** — `templates/features/jobs/queue.rs.jinja:238,267` : `job.clone().into()` puis `.update(db)`, alors que `auth/repository/user.rs.jinja:51-63` explique pourquoi il fait l'inverse (`update_many().col_expr`) → deux fragments du même binaire se contredisent sur la règle. Fix : `update_many` ciblé. *(Easy)* — Fait le 2026-09-14 : `mark_done` et `retry_or_fail` en `update_many` ciblé (`2c86533`) ; diagnostic corrigé : `update()` n'écrivait que les colonnes `Set` (sea-orm `query/update.rs:108-117`), le coût était la relecture de la ligne, payload compris (`RETURNING` de toutes les colonnes, `SELECT` de plus sur MySQL) ; comportement inchangé, donc pas de rouge ; arbre fusionné : `integration_jobs` 5/0, `integration_webhooks` 2/0 ; `newsletter-queue` 15/15 sur PostgreSQL.
- [x] **57. [UX] `doctor` compte deux fois la même absence de `RBS_MAIL__SMTP_PASSWORD`, avec deux remèdes différents** — observé en `--json` : check `.env` « absente du .env » puis check `mail` (`doctor/mail.rs:45`) « ni dans le .env ni dans l'environnement », même patron pour le secret JWT (`doctor/auth.rs:52`) → deux ✗ pour une ligne à écrire. Fix : le contrôle de feature se tait quand celui du `.env` a parlé. *(Easy)* — Fait le 2026-09-14 : `doctor::env::signalee` : `auth` et `mail` se taisent sur la clé que le contrôle `.env` nomme déjà, `base` avertit au lieu d'échouer (`c4b77d3`) ; 7 tests rouges puis verts, `rbs doctor` réel : un seul ✗ pour `RBS_MAIL__SMTP_PASSWORD` ; arbre fusionné : `--lib` 1420 passés, 0 échec.
- [ ] **58. [UX] L'aide et les erreurs d'usage mêlent l'anglais de clap au français du CLI** — observé : `rbs --help` → « Print help », « Print version », « help  Print this message… » ; `cli.rs:8-13` n'a ni `help_template` ni `mut_arg("help")`. Fix : `disable_help_flag`/`disable_version_flag` + args `-h`/`-V` en français, `help_template` francisé (les erreurs d'usage exigent un `Command::error` maison : Medium). *(Easy)*
- [x] **59. [UX] Un seul code de sortie pour tous les échecs** — `lib.rs:77,92,147,159,173,183,197,204,218,221` : `process::exit(1)` pour « pas un projet », « working tree sale », « ancre absente », « doctor non sain », erreur d'accès fichier → un script CI ne distingue pas « doctor a trouvé une faute » de « doctor n'a pas pu tourner ». Fix : 1 = faute du projet, 2 = usage, 3 = environnement/IO, documenté dans `cli/doctor.md`. *(Easy)* — Fait le 2026-09-14 : `errors::Sortie` (1 faute du projet, 2 usage, 3 environnement) et trait `Classee`, `match` exhaustifs sur les onze enums de commande, erreurs enveloppées classées par leur cause (`e35a046`, `af4ac1a`) ; `integration_sortie` 5/5 (3 rouges avant), `integration_json` 10/10, `integration_dev` 2/2, `--lib` 1420/0 sur l'arbre fusionné ; contrat documenté dans `cli/doctor.md` EN+FR.
- [ ] **60. [UX] `rbs dev` n'a aucune option** — observé : `rbs dev --help` → `Usage: rbs dev` ; pipeline figé dans `dev/mod.rs:199-217` (compose → 30 s d'attente → `migrate up` → `cargo run`, `watch.rs:98-103`) → impossible de relancer sans rejouer les migrations ni de passer des arguments au serveur. Fix : `--no-migrate`, `--no-compose`, `-- <args>`. *(Easy)*
- [x] **61. [UX] Le bilan d'une commande contredit son plan sur le nombre de fichiers** — observé : `rbs add cors` → « 9 fichiers à écrire » puis « ✓ cors installée — 3 fichiers » ; `lib.rs:290` affiche `pose.files` (créés) là où `:425,609` affichent `planned.files.len()` (créés + modifiés) ; les commits `c78fbfd` et `22e6bfb` montrent que la doc s'y perd aussi. Fix : « 3 créés, 6 modifiés » partout. *(Easy)* — Fait le 2026-09-14 : `Plan::bilan` et `ui::bilan` : pied « 3 à créer, 6 à modifier, 2 inchangés », bilan « 3 créés, 6 modifiés » pour `add`, `generate` et `generate job` (`2add68a`) ; 5 tests rouges puis verts ; transcriptions EN+FR recalculées ; arbre fusionné : `integration_docs` 14/0 et 1/0 sous Docker, `--lib` 1420/0.
- [ ] **62. [Dette] `Uuid::parse_str(&identite.user_id).map_err(|_| Unauthorized)` copié six fois ; le noyau n'offre que `user_id: String`** — `rbs-core/src/extract.rs:31-36` ; occurrences `auth/controller/session.rs.jinja:134,154,177,199`, `password.rs.jinja:31`, `guard.rs.jinja:78`, commentaire dupliqué → chaque CRUD qui voudra l'auteur d'une écriture (`audit::Entry::actor`) recopiera la ligne. Fix : `Identity::uuid(&self) -> Result<Uuid>` dans `rbs-core` (additif, ne varie pas d'un projet à l'autre). *(Easy)*
- [ ] **63. [Dette] Manifeste engendré sans `rust-version` ni `[profile.release]`, aucun `rust-toolchain.toml`/`rustfmt.toml`/`.editorconfig`, Dockerfile sans cache de dépendances** — `templates/project/Cargo.toml.jinja` (51 lignes, `edition = "2024"` sans MSRV ni profil) ; `docker/Dockerfile.jinja:11-14` : `COPY . .` puis `cargo build --release`, image `rust:1-slim-trixie` flottante → chaque commit recompile sea-orm/axum dans l'image, binaire ni strippé ni LTO, toolchain trop vieille signalée par l'édition et non par la MSRV. Fix : `rust-version = "1.94"`, profil release (`lto`, `codegen-units=1`, `strip`), couche deps ou `cargo-chef`. *(Easy)*
- [ ] **64. [Dette] Fragment `webhooks` : clone gratuit dans `emit`, révocation non atomique, validation des motifs hors DTO** — `templates/features/webhooks/service.rs.jinja:38` `abonnement.events.clone()` sur un modèle possédé ; `repository.rs.jinja` (rendu `modules/webhooks/repository.rs:63-79`) fait `find` → test `revoked_at` → `update` là où `auth` fait `UPDATE … WHERE revoked_at IS NULL` ; motifs vides validés dans le service (`service.rs:66-72`) plutôt que par `#[validate]`. *(Easy)*
- [x] **65. [Dette] `#[allow(dead_code)]` et commentaires mensongers dans `client/document.rs`** — `client/document.rs:101-103` : « Aucun appelant tant que `generate client` n'existe pas » au-dessus de `parse`, appelée par `client/mod.rs:183` ; idem `:41-43` sur `Parameter`, lu par `ts.rs:506,536,560,565` → deux lints désarmés et un *pourquoi* faux. Fix : retirer les deux `allow` et les commentaires. *(Easy)* — Fait le 2026-09-14 : les deux `allow(dead_code)` retirés, avec `Parameter::description` que rien ne lisait (`0f06d1c`) ; `cargo clippy --workspace --all-targets -- -D warnings` sans sortie sur l'arbre fusionné.
- [ ] **66. [Dette] Les features `redis`/`mail`/`storage` de `rbs-core` sont vides, activées par rien, et le README les dit « planned »** — `crates/rbs-core/Cargo.toml:34-36` `redis = []`, `mail = []`, `storage = []` ; `crates/rbs-core/README.md:48-49` « reserve the names for planned extensions » alors que les trois fragments vivent côté engendré depuis la v0.3 ; aucun `feature.toml` ni `examples/*/Cargo.toml` ne les active → API publiée pour rien, README contraire à la frontière noyau/généré. Fix : corriger le README (Easy) ; retirer les features à la prochaine mineure avec sa note (Medium). *(Easy)*
- [ ] **67. [Dette] L'exception `RUSTSEC-2026-0235` d'`audit.toml` a atteint sa propre condition de sortie** — `.cargo/audit.toml:16-20` : « à retirer quand `rust_decimal` passera à 0.8 » ; `cargo update --dry-run` : `rust_decimal v1.42.1 -> v1.43.0`, `Removing rkyv v0.7.46` ; 75 paquets du lock en retard → une exception qui devait vivre le temps d'un correctif lui survit. Fix : `cargo update`, retirer l'entrée. *(Easy)*
- [ ] **68. [Dette] Quinze templates dépassent la règle des ~200 lignes du `CLAUDE.md`** — `wc -l` : `features/auth/tests/session.rs.jinja` 919, `feature/tests.rs.jinja` 469, `feature/controller.rs.jinja` 380, `auth/tests/password.rs.jinja` 339, `scheduler/tests.rs.jinja` 338, `jobs/tests.rs.jinja` 286, `webhooks/tests.rs.jinja` 278, `jobs/queue.rs.jinja` 275, … → le fichier de tests que `add auth` dépose chez l'utilisateur fait 919 lignes, alors que 1.4.0 a justement scindé `src/auth/` par couche. Fix : scinder les tests par route ou par couche, comme le code. *(Medium)*
- [ ] **76. [Sécu] `register` puis `login` distingue encore une adresse inscrite d'une libre** — `templates/features/auth/service/session.rs.jinja:87` : `login` ne lit pas `email_verified_at`, seule la garde `guard.rs.jinja:86` le fait ; depuis que `register` rend 202 à toute adresse, `register {a, P}` puis `login {a, P}` rend 200 si l'adresse était libre, 401 si elle était prise → oracle en deux requêtes, borné à 10 inscriptions par heure et par client (`rate-limit/feature.toml:82`), assumé et documenté (`guides/auth.md`, section des routes). Fix : une clé `[auth]` qui refuse la connexion d'un compte non vérifié par le même 401 que le mauvais mot de passe — au prix de la connexion immédiate après inscription, et des tests engendrés qui la supposent. *(Medium)*

### Features P3 — ambitieuses

- [ ] **69. [Feature] `rbs generate migration <nom> --add-column <table> --fields "…"`** — parseur `--fields` (`generate/fields.rs:12-23`), rendu des `ColumnDef` (`generate/migration.rs`), `migrate new` par le plan (`migrate/fresh.rs`) ; `grep -n 'alter\|add_column' generate/migration.rs` → 0 → toute évolution de schéma après le premier `generate crud` est manuelle ; le CLI rend `alter_table().add_column()` + `drop_column` en `down`, et **affiche** la ligne à coller dans `model.rs`/`dto.rs`. *(Medium)*
- [ ] **70. [Feature] `--fields` : type `enum(a,b,c)`, plus `date` et `decimal`** — `FieldType` à sept variantes (`generate/fields.rs:12-23`), `fields.rs:751` teste `parse("decimal") → None`, aucun `enum` dans la grammaire → `status:enum(draft,published)` est le champ le plus fréquent après `string` ; `DeriveActiveEnum` en `string(N)` + CHECK pour tenir les trois moteurs. *(Medium)*
- [ ] **71. [Feature] `rbs add api-keys`** — `rbs-core/src/token.rs:23,36` (`random`, `fingerprint`), extracteur `Identity` limité à `Authorization: Bearer` (`extract.rs:31,64`), table `one_time_tokens` comme moule, `requires = ["auth"]` ; `grep -rniE 'api.key|x-api-key' crates docs` → 0 → intégrations machine sans cycle refresh/login ; point dur : `Identity` figée en 1.x, donc chemin `X-Api-Key` additif ou extracteur engendré. *(Hard)*
- [ ] **72. [Feature] `rbs remove <fragment>`** — `feature.toml` énumère tout ce qu'`add` pose, `metadata::record_feature` (`metadata.rs:292`) et le parcours des `requires` (`add/mod.rs:439`) sont à inverser, `plan::Builder` restaure ; `grep -n Remove cli.rs` → rien, la spec §4.4 renvoie à `git checkout`, valable le jour même → points durs : migration déjà appliquée (exiger `migrate down`), fichier modifié par l'utilisateur (conflit, comme `client`). *(Hard)*
- [ ] **74. [Perf] Deux résolutions DNS par livraison webhook, dans tous les profils** — `templates/features/webhooks/target.rs.jinja` : `Policy::resolve` (avant l'envoi, pour nommer le refus) puis `Resolver::resolve` (à la connexion, pour fermer le rebinding) résolvent le même hôte ; la première n'existe que pour distinguer `Blocked` d'une erreur de transport. Fix : faire porter le refus par une erreur typée que le résolveur du client remonte, et ne résoudre qu'une fois. *(Easy)*
- [ ] **75. [Dette] Sur une route gardée par `VerifiedIdentity`, le compte est relu deux fois par requête** — `templates/features/auth/mod.rs.jinja:31-52` (`accept`) puis `guard.rs.jinja:82-84` (`find`) lisent la même ligne ; `accept` pourrait la conserver dans les extensions de la requête pour que le garde la reprenne. *(Easy)*

---

## Écarté / obsolète

**Vérifié sain, avec son témoin** — ces points ont été contrôlés et n'ont donné aucune tâche :

- `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings` (rustc 1.98.1, celle de la CI) : aucune sortie, exit 0. `cargo audit` : exit 0 (536 dépendances, deux exceptions justifiées — l'une périmée, tâche 67).
- `cargo test --workspace` : **1346 passés, 0 échec, 55 ignorés** (suite Docker, exercée en CI par `-- --ignored`).
- Parité FR/EN : `parite.mjs` 42/42 paires au même dernier commit, 207 liens résolus ; seul écart `IMPROVE_OLD.md` (tâche 28). `CHANGELOG.md` et `CHANGELOG.fr.md` portent les mêmes sections et le même nombre d'items par version (1.4.0 : 9/9).
- Aucune dépendance déclarée sans usage : les 16 de `rbs-cli` et les 24 de `rbs-core` sont greppées dans `src/` ; aucune `pub fn` de `rbs-cli` sans appelant hors tests (281 passées au crible) ; chaque fichier de `templates/features/*/` est déclaré dans son `feature.toml` et chaque `source =` existe ; aucun `TODO`/`FIXME` dans `crates/`.
- `--help` réel = doc CLI pour `new`, `add`, `generate crud|feature|client`, `doctor`, `upgrade`, `seed`, `dev` ; `add.md:48` « twenty-one files » = 21 destinations dans `auth/feature.toml`. Le « quatorze ancres » du `CLAUDE.md` est exact (`anchors.rs:308`).
- Login sans oracle de temps : hash témoin Argon2 même sans compte (`auth/service/session.rs.jinja:21-23,45-51`), même 401 pour les deux cas. Jetons à usage unique : 32 octets `SysRng`, empreinte SHA-256 seule en base, `purpose` dans la recherche, consommation et péremption dans la condition de l'`UPDATE` (`one_time_token.rs.jinja:57-70`), test `a_token_is_consumed_once_even_under_concurrency`. Rotation du refresh atomique (`refresh_token.rs.jinja:51-60`), `change-password` exige l'ancien mot de passe (`password.rs.jinja:32-34`).
- Sessions cloisonnées : `user_id` dans la condition de l'`UPDATE` (`refresh_token.rs.jinja:100-110`), 404 plutôt que 403, aucun `token_hash` dans `SessionResponse`, `Cache-Control: no-store` sur la paire. `require_role` par seuil bien ordonné (`Role` `User < Admin`, `PartialOrd`), CRUD engendré sous auth : lectures `User`, écritures `--role` (`templates/feature/controller.rs.jinja:8-9`).
- Mail : autoescape minijinja sur `*.html`, destinataire analysé en `Mailbox` (pas d'injection d'en-têtes). CORS : origines vides par défaut, joker refusé avec `credentials`. Webhooks : HMAC-SHA256 sur `<t>.<corps>` avec secret par abonnement, jamais rendu par la liste. Upload borné par les 2 Mio d'axum sur `Bytes`. Client TS : identifiants assainis, `*/` neutralisé. Mot de passe de la base masqué (`db.rs:77-84`).
- OpenAPI complet et sécurisé : 13 routes auth + 6 CRUD dans `<rbs:openapi>` (`examples/blog-auth/src/openapi.rs:15-33`), chaque route fermée déclare `security(("bearer"=[]))`, 401 et 403 ; test engendré `the_openapi_document_carries_the_thirteen_auth_operations`.
- Gardes de `--fields` : mots-clés Rust, `id/created_at/updated_at`, doublon, snake_case, `string(N)` inconnu, `deleted_at` sous `--soft-delete`, `--role` sans auth, `--with-upload` sans storage — tous refusés avant écriture. Tests CRUD engendrés : cycle complet, 404, 400, filtre, 422, et sous auth 401 lecture/écriture, 403 non-admin. Aucun `unwrap()` hors tests dans un projet `--preset full`.
- `rbs add` avec `requires` : un seul plan pour la demandée et ses entraînées (`add/mod.rs:389-406`), cycle gardé, restauration par `Log::undo` testée (`a_failure_on_the_fourth_action_rolls_back_the_first_three`) ; `anchors::insert` idempotent. Scheduler : réservation d'échéance et enfilage du job dans une même transaction ; `emit` webhooks dans la transaction du métier (test `an_emission_rolled_back_with_its_transaction_enqueues_nothing`). `upgrade` ne touche que `Cargo.toml` et les zones d'`AGENTS.md`, refuse de redescendre.
- UX du CLI : démarrage 10 ms, `rbs new` 47 ms ; non-interactif refusé avec le remède (`prompts.rs:105-113`) ; `NO_COLOR` et tube sans ANSI ; `Broken pipe` absorbé ; idempotence annoncée sur `add`, `generate`, `upgrade` ; CRLF suivi (`anchors.rs:438-448`) ; messages d'usage actionnables (feature inconnue → liste, répertoire existant, hors projet).
- Perf du code engendré : liste CRUD en `try_join!` page + count, `per_page` clampé 1..100 ; file de jobs en un seul `UPDATE … FOR UPDATE SKIP LOCKED … RETURNING` ; mail détaché de la réponse avec timeout SMTP 10 s ; client HTTP webhooks avec timeout 10 s ; tri du filtre fermé par un `match`, colonne inconnue → 400.

**Déjà implémenté** — idées envisagées puis écartées, avec le fichier qui le prouve : `doctor --fix` et `--json` (`cli.rs:105-110`), `doctor` nomme le code hors CLI (`doctor/agents.rs:24,140`), `migrate status` (`cli.rs:216`), `--soft-delete` (`cli.rs:172`), Prometheus `/metrics` + OTLP (`observability/metrics.rs.jinja`), presets (`preset.rs:10-14`), `--dry-run` sur `add`/`upgrade`, sondes de santé par fragment (`<rbs:health_probes>`), `--template-dir`, tri et filtres typés, notes de migration à l'`upgrade` (`notes.rs`), `upgrade --check` (couvert par `doctor --json` contrôle `versions`).

**Ancien backlog** — les 126 tâches d'`IMPROVE_OLD.md` sont faites et aucune n'est reprise ; deux y renvoient parce qu'elles rouvrent un sujet clos sous un autre angle (tâche 7 → ex-#12, tâche 43 → ex-#13).
