# IMPROVE — backlog d'améliorations

**Scan du 2026-08-30**, sur le commit `c5486c2` (`main`, arbre propre, v1.1.0 publiée).

**Méthode** — signaux mécaniques (`cargo fmt`, `clippy`, `test`, `audit`, `tree`) puis six
agents de lecture parallèles : bugs & correctness, sécurité, dette & documentation,
performance & UX du CLI, qualité du code généré, nouvelles features. 82 findings bruts,
dédupliqués et force-rankés. **Chaque tâche ci-dessous a été vérifiée dans le code réel** —
un finding sans `fichier:ligne` lu a été jeté.

**Rapport avec `TODO.md`** — `TODO.md` est clos : 147 cases sur 147, les cinq jalons v0.1 →
v1.1 livrés. C'est un journal de preuves daté, pas un backlog concurrent, et il n'est pas
déprécié. `IMPROVE.md` est le backlog ouvert, il ne reprend aucune de ses lignes.

**Exclusions** — hors périmètre du projet, jamais proposé ici : GraphQL, multi-tenancy,
WebSockets, gRPC, interface d'administration générée, gestion des paiements
(`ROADMAP.md:111`).

**Légende** — Tags : `[Bug]` `[Sécu]` `[Perf]` `[UX]` `[Dette]` `[Doc]` `[Feature]`
`[Arch]`. Difficulté : Easy (< 1 j) · Medium (1-3 j) · Hard (3-10 j).

---

## P0 — critique

- [x] **1. [Sécu] `rbs add auth` fait signer les JWT avec un secret publié dans la crate** — `crates/rbs-cli/src/lib.rs:329` dit « recopiez `RBS_AUTH__SECRET` de .env.example vers votre .env », et `templates/features/auth/feature.toml:71` y écrit `changez-moi-par-un-secret-…` (61 octets, donc accepté par `config.rs:158`) → qui suit l'instruction du CLI laisse forger un token `role:"admin"` à quiconque a `cargo install rbs-cli`. Fix : `add auth` tire le secret au hasard directement dans `.env`. *(Easy)* — Fait le 2026-08-30 : `[[env]]` porte un champ `secret`, `add` dépose 64 hexadécimaux tirés de `SysRng` dans le `.env` gitignoré et laisse le placeholder à `.env.example` ; `cargo test --workspace` 787 + 77, 0 échec, suite Docker `--ignored` en exit 0, clippy et fmt propres.
- [x] **2. [Bug] Un commentaire en fin de ligne dans `.env` désarme le refus de semer en production** — `crates/rbs-cli/src/dotenv.rs:55` rend `unquote(value.trim())` sans traiter le `#` inline, et `seed.rs:172` compare en égalité stricte → `RBS_ENV=production # ne jamais semer` est lu `"production # ne jamais semer"`, le garde-fou ne se prononce pas et `rbs seed` s'exécute sur la base de production. Fix : couper au `#` non échappé hors guillemets, comme `dotenvy`. *(Easy)* — Fait le 2026-08-30 : `strip_comment` applique la règle `dotenvy` — un `#` ne coupe que précédé d'un blanc, un guillemet échappé ne ferme pas la valeur ; `s3#cret` et `"{\"k\":1}"` survivent, `production # …` rend `production` et `seed` refuse ; `cargo test --workspace` 787 + 77, 0 échec, clippy et fmt propres.

## P1 — impact utilisateur fort

- [x] **3. [Bug] Le corps HTTP des 500 et le document OpenAPI de tout projet généré portent du français cassé** — `crates/rbs-core/src/error.rs:144` renvoie `detail: "une error interne est survenue"`, `error.rs:66,70` `"error base de données"` / `"error interne"`, `openapi.rs:65-66` `"détaillé par field"` / `"error interne"`, `config.rs:128,135` `"file `.env` illisible"` / `"32 bytes"`, `logs/mod.rs:30` `"une value invalide … (expected …)"` : le renommage des identifiants vers l'anglais est passé sur les littéraux. Fix : corriger les 9 chaînes, publier un patch — `rbs-core` 1.1.0 est déjà sur crates.io. *(Easy)* — Fait le 2026-08-31 : les 9 chaînes livrées corrigées, plus les littéraux de test touchés par le même renommage (`body`, `line`, `name`, `value`, `profile`) — 55 lignes sur 14 fichiers, aucun identifiant Rust ni titre HTTP modifié ; `cargo test -p rbs-core` 80 passés (77 avant), `--features auth` 100 passés, clippy et fmt propres.
- [x] **4. [Doc] `CLAUDE.md` décrit un dépôt vide et un backlog ouvert** — `CLAUDE.md:7-10,17,35` : « Aucun code applicatif n'existe encore », « `src/main.rs` », « les 52 tâches de la v0.1 », « ces commandes ne fonctionneront qu'une fois `A1` faite » → 35 000 lignes existent, `TODO.md` est à 147/147 et `src/main.rs` n'existe plus ; tout agent démarre sur une fausse carte. Fix : réécrire les sections « État du dépôt » et « Commandes ». *(Easy)* — Fait le 2026-08-31 : « État du dépôt » et « Commandes » réécrites sur le dépôt réel — workspace en place, cinq jalons livrés, `IMPROVE.md` désigné backlog ouvert et `TODO.md` requalifié en journal clos ; `parite.mjs` 26/26 paires.
- [x] **5. [Doc] `CLAUDE.md` et l'`AGENTS.md` livré affirment l'inverse du code généré** — `CLAUDE.md:84` et `templates/agents/fr.md.jinja:50` (idem `en.md.jinja:51`) : « un `service` n'accède jamais à `DatabaseConnection` », or `templates/feature/service.rs.jinja:4` l'importe et ses cinq fonctions la prennent en paramètre ; la spec écrivait « jamais **directement** » (`specs/2026-08-25-rbs-design.md:118`), le mot a sauté → l'agent de l'utilisateur « corrige » du code correct. Fix : restaurer la formulation de la spec aux trois endroits. *(Easy)* — Fait le 2026-08-31 : « jamais *directement* » restauré aux trois endroits — `CLAUDE.md`, `agents/fr.md.jinja`, `agents/en.md.jinja` — avec la mécanique explicitée (le service reçoit la connexion et la passe au repository) ; `cargo test -p rbs-cli --lib agents` 42 passés.
- [x] **6. [Doc] `CLAUDE.md` liste 5 commandes sur 8 et 4 ancres sur 10** — `CLAUDE.md:59,88` omet `seed`, `dev` et `upgrade` (`cli.rs:78,85,88`) et six ancres sur les dix de `anchors.rs:198` (`STATE_CHAMPS`, `STATE_INIT`, `STARTUP`, `SEEDS`, `SERVICES`, `MIGRATION_MODULES`) → une feature écrite sur cette liste ignore la moitié des points d'insertion. Fix : régénérer les deux inventaires depuis `cli.rs` et `anchors.rs`. *(Easy)* — Fait le 2026-08-31 : les 8 commandes inventoriées, et les ancres passées de 4 citées sur 10 à un tableau des 11 (l'ancre `layers` incluse) renvoyant à `ANCRES` comme autorité ; les comptes des six pages de doc suivis, EN et FR.
- [x] **7. [Doc] Toute la documentation publiée annonce la version précédente** — `README.md:12` et `README.fr.md:12` disent « Version 1.0.0. », `docs/docs/getting-started.md:43` montre `rbs 1.0.0`, et `cli/new.md:155`, `cli/add.md:170`, `cli/generate.md:278`, `cli/upgrade.md:46-137` impriment `version = "1.0.0"` — `Cargo.toml:10` est en 1.1.0, et `getting-started.md:386` affiche déjà 1.1.0, donc la page se contredit. Fix : passer les captures à 1.1.0, FR et EN. *(Easy)* — Fait le 2026-08-31 : README (×2), `getting-started`, `cli/new` et `cli/generate` passés en 1.1.0, et le README corrigé de cinq à six jalons livrés. `compatibility.md` et `cli/upgrade.md` gardent leurs 1.0.0 — l'un explique la lecture d'un numéro de version, l'autre raconte un saut daté ; `parite.mjs` 26/26.
- [x] **8. [Bug] `--fields "author_id:uuid,author:references:users"` engendre un projet qui ne compile pas** — `crates/rbs-cli/src/generate/fields.rs:308` déduplique sur `field.name` alors que `column_name()` (`:155`) suffixe `_id` aux références, et le garde-fou de `:376` ne refuse qu'un *nom de référence* en `_id` → deux `pub author_id` dans le `Model`, deux `ColumnDef` et une variante `DeriveIden` en double. Fix : dédupliquer sur `column_name()`. *(Easy)* — Fait le 2026-08-31 : la déduplication porte sur `column_name()`, et l'erreur dit *pourquoi* deux noms distincts collident (une référence suffixe sa colonne en `_id`) ; test de `parse` sur `author_id:uuid,author:references:users`.
- [x] **9. [Bug] Deux relations qui se singularisent pareil produisent deux variantes `Relation` identiques** — `crates/rbs-cli/src/generate/relations.rs:92` fait `to_pascal_case(to_singular(...))`, et `to_singular` (`feature.rs:221`) retire le `s` final : `author` et `authors` donnent tous deux `Author`, émis une fois par champ (`model.rs.jinja:26-34`) → `enum Relation` porte deux fois `Author,`, le projet ne compile pas et rien ne le détecte. Fix : refuser la collision de variantes dans `resolve`. *(Easy)* — Fait le 2026-08-31 : `resolve` refuse la collision de variantes, réservée avant la résolution de la cible pour que `author`/`authors` se signalent même quand les deux cibles sont bonnes ; `Error::Targets` devenu `Error::Relations(Vec<ResolveError>)`.
- [x] **10. [Sécu] Un mot de passe contenant `/` part en clair dans l'erreur de connexion** — `crates/rbs-core/src/db.rs:92-99` : `password()` coupe l'autorité au premier `/` puis cherche le `@` avant la coupe ; pour `postgres://alice:s3c/r3t@host/app` le `rfind('@')` rend `None` et `strip` laisse l'URL intacte → le secret est affiché et journalisé. Le test `the_password_does_not_appear_in_the_error_message` (`db.rs:190`) n'exerce qu'un mot de passe sans `/`. Fix : découper sur `://` puis sur le dernier `@` de l'autorité. *(Easy)* — Fait le 2026-08-31 : `fin_du_userinfo` tranche le segment précédant le premier `/` entre un port (suffixe numérique) et un mot de passe coupé en deux ; 2 tests écrits d'abord et vus rouges sur `postgres://alice:s3c/r3t@host/app`, plus les cas limites (`@` brut et `%40`, port et query, sqlite). Reste non masqué le seul cas textuellement indécidable — un mot de passe entièrement numérique portant un `/` et sans chemin, indiscernable de `postgres://localhost:5432/app@v2`.
- [x] **11. [Sécu] Tout déploiement Docker généré sert `/docs` et l'OpenAPI complet publiquement** — `templates/project/config/default.toml.jinja:13-15` pose `swagger_ui = true` pour tous les profils, le service `api` de `features/docker/feature.toml:38-47` ne pose ni `RBS_ENV` ni `RBS_DOCS__*`, et `config/` n'a pas de `production.toml.jinja` → aucun endroit où la décision de production puisse s'écrire. Fix : engendrer un `config/production.toml` qui coupe les docs, et poser `RBS_ENV=production` dans le service `api`. *(Easy)* — Fait le 2026-08-31 : `config/production.toml` engendré coupe les deux documents, et le service `api` du compose pose `RBS_ENV=production` (dans le contenu d'ancre *et* dans le compose de repli). Le fragment `docker` dépose aussi le profil, faute de quoi `add docker` sur un projet ancien aurait pointé `RBS_ENV` vers un fichier absent, docs publiées ; `cargo test -p rbs-cli --lib` 828 passés.
- [x] **12. [Sécu] `/auth/login` n'a aucune limite de débit et coûte un Argon2 par requête anonyme** — `templates/project/src/router.rs.jinja:16-18` n'empile que `trace` et `request_id` (aucun `governor` dans tout le dépôt), et `features/auth/service.rs.jinja:47-53` hache délibérément même pour un email inconnu → bourrinage d'identifiants libre, et N connexions simultanées épinglent N × 19 Mio. Fix : `rbs add rate-limit` (tâche 54) posé d'office par `add auth`. *(Medium)* — Fait le 2026-08-31 avec les tâches 53 et 54 : `[feature] requires` résout `auth → rate-limit` en un plan unique, annoncé à l'utilisateur avant application. `/auth/login` est limité à 5 tentatives par minute contre 120 en global — ce qui borne aussi la mémoire qu'un Argon2 par requête anonyme peut imposer.
- [x] **13. [Sécu] `POST /auth/register` est un oracle d'énumération qui répète l'adresse** — `templates/features/auth/service.rs.jinja:25-30` et `repository.rs.jinja:39-44` renvoient tous deux `Error::Conflict("l'adresse {} est déjà inscrite")`, soit un 409 portant l'email → défait la protection que le même fichier construit soigneusement pour le login (le hash témoin de `service.rs.jinja:14-22`). Fix : réponse indifférenciée, confirmation par mail ou 202 systématique. *(Easy)* — Fait le 2026-08-31 : le 409 ne répète plus l'adresse, message porté par une constante partagée entre le service et le repository plutôt que par deux littéraux qui pouvaient diverger ; un test balaie tous les `Error::Conflict` du fragment et y interdit `email`, vu rouge d'abord.
- [x] **14. [Sécu] Un refresh token rejoué ne révoque pas la famille de sessions** — `templates/features/auth/repository.rs.jinja:84-92` : `consume` est un `UPDATE … WHERE revoked_at IS NULL` qui rend 401 au second usage, mais rien ne révoque les sessions sœurs de l'utilisateur → un jeton volé et utilisé avant la rotation légitime laisse l'attaquant avec une paire valide indéfiniment, en silence. Fix : sur réutilisation détectée, révoquer toute la famille. *(Medium)* — Fait le 2026-08-31 : à réutilisation détectée, `revoke_sessions_of` révoque toutes les sessions du compte, avec un `tracing::warn!` sans donnée personnelle. Toutes les sessions plutôt que la seule chaîne : isoler la famille demanderait une colonne de plus, qu'un projet ayant déjà appliqué la migration ne recevrait jamais — le correctif serait inopérant là où un déploiement ancien en a besoin.
- [x] **15. [Bug] Le test généré pour un champ `references` viole la clé étrangère à sa première exécution** — `crates/rbs-cli/src/generate/tests_http.rs:67` rend `Uuid::new_v4().to_string()` pour toute `Reference` (`fields.rs:202`), POSTé dans une colonne sous `ForeignKeyAction::Restrict` → 500 au lieu de 201. Le générateur de seeds a résolu ce cas (`seed.rs:119 is_seedable`), `command.rs:436` n'a pas la garde équivalente, et aucun exemple ne porte de `references`. Fix : aligner `tests_http` sur `is_seedable`. *(Medium)* — Fait le 2026-08-31 : `Feature::required_reference()` devient le prédicat partagé avec `is_seedable`. Référence optionnelle → `Value::Null` au lieu d'un UUID tiré au hasard, comme le seed ; référence requise → les scénarios qui créent ne sont pas engendrés, un bandeau nomme la référence bloquante et le CLI le dit. Prouvé sur un projet réellement engendré (`cargo check --all-targets` et `clippy -D warnings` verts).
- [x] **16. [Bug] Une violation de contrainte `unique` rend 500 sur un CRUD généré, 409 sur `auth`** — `templates/feature/repository.rs.jinja:31-33` fait `Ok(x.insert(db).await?)`, la `DbErr` remonte en `Error::Database` ; `features/auth/repository.rs.jinja:39-44` fait exactement l'inverse au même endroit. `--fields "email:string:unique"` est documenté → second POST = 500 non documenté, masqué par le suffixe aléatoire du test (`tests.rs.jinja:81`). Fix : porter le `match error.sql_err()` d'`auth` dans le gabarit générique. *(Easy)* — Fait le 2026-08-31 : `conflict_on_duplicate` traduit la violation d'unicité en 409 sur `create` et `update`, message générique — le gabarit ne connaît pas la colonne. Le contrat OpenAPI déclare le statut, sans quoi ce correctif aurait creusé la tâche 47 ; `cargo test -p rbs-cli --lib controller` 13 passés.
- [x] **17. [UX] `rbs dev` reste muet jusqu'à trente secondes en attendant la base** — `crates/rbs-cli/src/dev/mod.rs:238-251` : `wait_for` boucle toutes les 250 ms jusqu'à `ATTENTE_APRES_COMPOSE = 30s` sans écrire une ligne, le plan ayant déjà été affiché → l'utilisateur ne sait pas si rbs attend PostgreSQL ou s'est planté. Fix : une ligne « en attente de la base (host:port) », puis un point par seconde. *(Easy)* — Fait le 2026-08-31 : une ligne « en attente de la base (host:port) » puis un point par seconde, cadencé sur `Instant` et non sur les tours de boucle ; rien ne s'affiche quand la base répond du premier coup. `wait_for` reste testable, l'affichage passant par un paramètre.
- [x] **18. [Perf] Le CRUD généré fait un `COUNT(*)` non borné avant chaque page, en série** — `templates/feature/repository.rs.jinja:14` : `let total = Entity::find().count(db).await?;` précède la requête paginée, jamais conditionnel ni parallélisé → deux allers-retours par `GET /items`, dont un scan complet de table sur PostgreSQL, dans *tout* projet engendré. Fix : `tokio::try_join!` des deux requêtes. *(Medium)* — Fait le 2026-08-31 : `tokio::try_join!` lance la page et le `COUNT(*)` ensemble. `try_join!` n'exige que la feature `macros`, déjà déclarée par le manifeste engendré — rien à ajouter ; le commentaire du gabarit dit pourquoi le pool en sert deux à la fois.

## P2 — impact moyen

- [x] **19. [Bug] `doctor` et `generate` n'ont pas le même prédicat de présence d'une ancre** — `crates/rbs-cli/src/doctor/anchors.rs:67` fait `source.contains(...)` n'importe où, y compris dans une chaîne ou un commentaire, quand `anchors.rs:310` exige `line.trim() == balise` → `doctor` annonce « les 10 points d'insertion sont en place » et `generate` répond « ancre introuvable », exactement le cas que `doctor` existe pour prévenir. Fix : `doctor` appelle `line_of`. *(Easy)* — Fait le 2026-08-31 : `anchors::marks` porte seule la règle `line.trim() == balise` et est appelée par `line_of`, par `doctor/anchors` et par `doctor/relations`, qui répétait la même duplication ; `cargo test --workspace --no-fail-fast` 840 + 80, 0 échec ; suite Docker `--ignored` en exit 0, les seize binaires d'intégration dont `integration_examples` ; clippy et fmt propres.
- [x] **20. [Bug] Un `Cargo.toml` au TOML invalide fait dire « aucun projet rbs ici »** — `crates/rbs-cli/src/metadata.rs:18-23` : `project_root` remonte les ancêtres sur `read(...).is_ok()`, avalant `Syntaxe`, `Field` et `MoteurInconnu` → une virgule oubliée rend `add`, `generate`, `upgrade`, `migrate` et `seed` inutilisables sans nommer la faute, et peut faire viser un projet situé plus haut. Fix : distinguer « pas de manifeste » de « manifeste illisible ». *(Medium)* — Fait le 2026-08-31 : `project_root` rend `Result<PathBuf, RootError>` : un manifeste absent ou sans section rbs laisse continuer la remontée, un manifeste cassé l'arrête en nommant la faute ; les sept appelants convertissent par `From` ; `cargo test --workspace --no-fail-fast` 840 + 80, 0 échec ; suite Docker `--ignored` en exit 0, les seize binaires d'intégration dont `integration_examples` ; clippy et fmt propres.
- [x] **21. [Bug] `doctor` retombe silencieusement sur PostgreSQL quand le manifeste est illisible** — `crates/rbs-cli/src/doctor/base.rs:152-155` : `metadata::read(...).map(|m| m.database).unwrap_or_default()` → un projet MySQL ou SQLite dont le manifeste est cassé est diagnostiqué en PostgreSQL, sonde réseau, port et message d'écart compris. Fix : propager l'erreur en contrôle rouge. *(Easy)* — Fait le 2026-08-31 : `database_of` rend `Result<Database, metadata::Error>` et le diagnostic pose un contrôle rouge nommant le manifeste au lieu de sonder PostgreSQL ; `cargo test --workspace --no-fail-fast` 840 + 80, 0 échec ; suite Docker `--ignored` en exit 0, les seize binaires d'intégration dont `integration_examples` ; clippy et fmt propres.
- [x] **22. [Bug] `rbs add` invente des identifiants de base quand le `.env` du projet est illisible** — `crates/rbs-cli/src/add/mod.rs:194-197` : `.ok()` jette l'erreur d'accès et retombe sur `database.default_url(...)`, alors que le commentaire voisin exige « les identifiants que le projet interroge » → le compose écrit des identifiants faux, le service monte et l'application ne s'y connecte pas. Fix : remonter l'erreur. *(Easy)* — Fait le 2026-08-31 : un `.env` présent mais illisible remonte l'erreur ; absent, ou sans `DATABASE_URL`, le repli sur `default_url` demeure, verrouillé par un test ; `cargo test --workspace --no-fail-fast` 840 + 80, 0 échec ; suite Docker `--ignored` en exit 0, les seize binaires d'intégration dont `integration_examples` ; clippy et fmt propres.
- [x] **23. [Bug] `rbs migrate new` écrit deux fichiers sans restauration** — `crates/rbs-cli/src/migrate/fresh.rs:90-91` enchaîne deux `write` en contournant `plan::Builder`, seul porteur du journal de rollback → un échec sur le second laisse une migration qui n'est ni déclarée en `mod` ni inscrite au `Migrator` : le projet ne compile plus. Fix : passer la commande par le plan. *(Medium)* — Fait le 2026-08-31 : les deux écritures passent par `plan::Builder` : un `lib.rs` non inscriptible ne laisse plus de migration orpheline ; `cargo test --workspace --no-fail-fast` 840 + 80, 0 échec ; suite Docker `--ignored` en exit 0, les seize binaires d'intégration dont `integration_examples` ; clippy et fmt propres.
- [x] **24. [Bug] `bio:text:unique` engendre une migration que MySQL 8 refuse** — `templates/feature/migration.rs.jinja:21` concatène `.text()` et `.unique_key()`, et `fields.rs:860` teste explicitement que c'est accepté sans réserve → erreur 1170 au `rbs migrate up`, longtemps après la commande fautive, alors que MySQL est un moteur de premier rang (`docker-compose.yml.jinja:24`). Fix : refuser `unique`/`index` sur `text`, ou exiger une longueur de préfixe. *(Easy)* — Fait le 2026-08-31 : `ErrorKind::IndexOnText` refuse `unique` et `index` sur un champ `text`, tous moteurs, documentation bilingue suivie ; `cargo test --workspace --no-fail-fast` 840 + 80, 0 échec ; suite Docker `--ignored` en exit 0, les seize binaires d'intégration dont `integration_examples` ; clippy et fmt propres.
- [x] **25. [Sécu] Le compose généré porte le mot de passe de la base en clair et sans échappement** — `templates/project/docker-compose.yml.jinja:9,26,30,40` interpole `{@ database_password @}` — issu du `--database-url` réel — dans des scalaires YAML non quotés et dans un `CMD-SHELL` MySQL ; `.gitignore.jinja` n'ignore que `/target` et `.env` → le mot de passe est committé au premier `git add .`, et un caractère `'` ou `$(` casse le fichier. Fix : `${POSTGRES_PASSWORD}` lu du `.env`. *(Easy)* — Fait le 2026-08-31 : les deux composes — celui du projet et celui du fragment `docker`, que le diagnostic ne citait pas — passent aux `"${POSTGRES_PASSWORD}"` cités, `$$` protégeant les `CMD-SHELL` d'un mot de passe portant `$(` ; `rbs new` écrit les clés dans le `.env` gitignoré, et `add docker` les y dépose sur un projet antérieur grâce à un `project_value` et un `when` ajoutés au manifeste de fragment ; `RBS_DATABASE__URL` du compose ne porte plus d'identifiants ; `cargo test --workspace --no-fail-fast` 874 + 84, 0 échec ; suite Docker `--ignored` en exit 0, seize binaires d'intégration ; `integration_examples` 17/17 ; clippy et fmt propres.
- [x] **26. [Sécu] Les routes CRUD générées sont publiques en écriture, sans moyen de demander un garde** — `templates/feature/controller.rs.jinja:39-100` : `create`, `update` et `delete` ne prennent ni `Identity` ni `require_role`, et `cli.rs:100-115` n'expose aucun `--protected` → `rbs new --with auth` puis `rbs generate crud articles` donne écriture et suppression anonymes. Le choix est documenté (`guides/auth.md:121`), mais `doctor` ne dit rien. Fix : flag `--role`, ou avertissement `doctor`. *(Medium)* — Fait le 2026-08-31 : `--role <ROLE>` pose `Identity` + `require_role` sur `create`, `update` et `delete`, `list` et `show` restant publics ; le contrat OpenAPI annonce 401 et 403, les statuts que le garde produit réellement ; `--role` sans `auth` et un rôle non déclaré sont refusés avant écriture ; `doctor` avertit en orange sur un CRUD anonyme dans un projet portant `auth` ; `cargo test --workspace --no-fail-fast` 874 + 84, 0 échec ; suite Docker `--ignored` en exit 0, seize binaires d'intégration ; `integration_examples` 17/17 ; clippy et fmt propres.
- [x] **27. [Sécu] Aucun `TimeoutLayer` : `/health` et toute route peuvent pendre indéfiniment** — `crates/rbs-core/src/health.rs:75` attend `ping()` sans `tokio::time::timeout`, et `templates/project/src/router.rs.jinja:16-18` n'empile aucune borne (`tower-http` n'est même pas dépendance du `Cargo.toml.jinja` généré) → une base qui accepte le TCP sans répondre fait pendre la sonde, donc les redémarrages orchestrés. Fix : `TimeoutLayer` dans le routeur et borne sur le ping. *(Medium)* — Fait le 2026-08-31 : `tower-http` câblé, `TimeoutLayer::with_status_code` posé dans `<rbs:layers>` donc intérieur à `trace` et `request_id`, durée `RBS_SERVER__TIMEOUT_SECS` à 30 s par défaut ; le ping de `/health` est borné à 2 s et rend le verdict d'un ping en échec ; `cargo test --workspace --no-fail-fast` 874 + 84, 0 échec ; suite Docker `--ignored` en exit 0, seize binaires d'intégration ; `integration_examples` 17/17 ; clippy et fmt propres.
- [x] **28. [Doc] `docs/cli/generate.md` publie une sortie `--help` dont `--has-many` a disparu** — `docs/docs/cli/generate.md:55-69` (et sa version FR) annonce en `:12-15` que chaque bloc est capturé en lançant la commande, mais n'y liste pas `--has-many`, pourtant déclaré non caché en `cli.rs:119` et documenté en `guides/relations.md:174`. Fix : recapturer les deux blocs. *(Easy)* — Fait le 2026-08-31 : les deux blocs recapturés en lançant le binaire — ils gagnent `--has-many`, absent depuis toujours, `--role`, et la correction de `<NOM>` en `<NAME>` que clap rend réellement ; tableau des flags mis en accord, EN et FR ; `cargo test --workspace --no-fail-fast` 874 + 84, 0 échec ; suite Docker `--ignored` en exit 0, seize binaires d'intégration ; `integration_examples` 17/17 ; clippy et fmt propres.
- [x] **29. [UX] La question « Nom du projet ? » ne peut pas se poser, et la doc annonce trois questions** — `crates/rbs-cli/src/cli.rs:32` fait de `name` un positionnel requis et `lib.rs:175` passe toujours `Some(name)` : la branche de `prompts.rs:153` est inatteignable hors tests, alors que `docs/docs/cli/new.md:191-195` documente trois questions et que `README.md:57` en annonce deux. Fix : rendre `name` optionnel — le code d'assistant existe et est testé. *(Easy)* — Fait le 2026-08-31 : `name` devient `Option<String>` et la branche d'assistant de `prompts.rs` cesse d'être inatteignable ; `--yes` sans nom rend une erreur explicite plutôt qu'un nom inventé ; `cargo test --workspace --no-fail-fast` 874 + 84, 0 échec ; suite Docker `--ignored` en exit 0, seize binaires d'intégration ; `integration_examples` 17/17 ; clippy et fmt propres.
- [x] **30. [UX] `rbs add` et `rbs upgrade` n'offrent pas de `--dry-run`** — `crates/rbs-cli/src/cli.rs:56-63` : les deux commandes n'ont que `--force`, quand `generate crud` et `generate feature` portent `--dry-run` (`cli.rs:115,133`), et `lib.rs:288-292` imprime le plan puis applique aussitôt → aucun moyen de prévisualiser `rbs add auth`, la commande qui touche pourtant le plus de fichiers. Fix : `--dry-run` sur `Add` et `Upgrade`. *(Easy)* — Fait le 2026-08-31 : `--dry-run` sur `add` et `upgrade`, sur le modèle de `generate crud` — plan affiché, projet identique octet pour octet, sortie 0 ; `cargo test --workspace --no-fail-fast` 874 + 84, 0 échec ; suite Docker `--ignored` en exit 0, seize binaires d'intégration ; `integration_examples` 17/17 ; clippy et fmt propres.
- [x] **31. [UX] `--yes` et `--template-dir` sont globaux mais ignorés par six sous-commandes sur sept** — `crates/rbs-cli/src/cli.rs:19-24` les déclare `global = true`, mais `lib.rs:55` ne transmet `yes` qu'à `create_project` et `template_dir` qu'à `New` et `Add` ; `generate` rend depuis des `include_str!` constants → `rbs generate crud --template-dir ./mes-templates` est accepté sans effet ni avertissement. Fix : câbler, ou retirer `global`. *(Medium)* — Fait le 2026-09-01 : `global` retiré des deux — `--yes` descend sur `new`, seule commande interactive, `--template-dir` sur `new` et `add`, les deux qui l'honorent. `rbs generate crud --template-dir …` est désormais une erreur de clap et non un drapeau pris puis ignoré. Cinq appelants ont suivi — les deux derniers, `rbs add auth --yes` et `rbs add jobs --yes` dans la suite Docker, n'ont été rattrapés qu'après la fusion : un `cargo test --workspace` ne les exécute pas, seule la suite `--ignored` les voit. Vingt-deux blocs `--help` régénérés et six pages de prose corrigées, EN et FR.
- [x] **32. [UX] `rbs doctor` n'a aucune sortie machine-lisible malgré un code de sortie pensé pour les scripts** — `crates/rbs-cli/src/lib.rs:145-154` sort en 1 avec le commentaire « un script doit pouvoir le distinguer », mais `doctor/render.rs:27-31` ne rend que du texte ANSI à glyphes `✓ ! ✗` → une CI ne peut savoir *quel* contrôle a échoué. Fix : `--json`. *(Medium)* — Fait le 2026-09-01 : `--json` rend `{ "sain", "checks" }`, chaque contrôle portant `name`, `status` (`ok`/`avertissement`/`echec`) et `detail`, plus `remede` quand il existe. Prouvé sur le binaire : 870 octets sur stdout, stderr vide, JSON valide, exit 1 sur projet en échec ; le rendu texte est inchangé.
- [x] **33. [UX] `rbs doctor` déclenche une compilation de plusieurs minutes sans l'annoncer** — `crates/rbs-cli/src/doctor/base.rs:242` appelle `migrate::launch(root, "version", …)`, soit `cargo run -p migration -- version` (`migrate/mod.rs:158`), alors que l'aide annonce « Diagnostique le projet : ancres, .env, base joignable, versions » (`cli.rs:88`) → un diagnostic présumé instantané bloque le temps de bâtir la crate `migration`. Fix : annoncer l'étape, ou la passer derrière un flag. *(Medium)* — Fait le 2026-09-01 : l'annonce est portée par le contrôle `base` et sort **avant** la compilation, ce qui a imposé de rendre le rapport au fil des contrôles — il s'assemblait en `Vec<String>` affiché en bloc, incompatible avec une annonce émise pendant le travail. `--json` ne porte jamais l'annonce. Bloc de documentation capturé sur une base réellement joignable, et non rédigé de mémoire.
- [x] **34. [Sécu] Les mots de passe n'ont aucune borne supérieure, et `LoginRequest` aucune contrainte** — `templates/features/auth/dto.rs.jinja:10-18` : `RegisterRequest.password` porte `length(min = 8)` sans maximum et `LoginRequest.password` rien du tout → `/auth/login` hache en Argon2 un corps de plusieurs mégaoctets, chemin d'amplification le moins cher vers le service (cf. tâche 12). Fix : `length(min = 12, max = 128)` sur les deux. *(Easy)* — Fait le 2026-08-31 : `length(min = 12, max = 128)` sur `RegisterRequest` et `LoginRequest`, documentation `auth` alignée dans les deux langues ; test de rendu vu échouer (0 borne sur 2) puis vert.
- [x] **35. [Perf] `rbs doctor` reparse le même `Cargo.toml` une dizaine de fois et `default.toml` jusqu'à huit** — `crates/rbs-cli/src/doctor/mod.rs:182-185` refait un `metadata::read` complet par entrée de `FEATURE_CHECKS`, et `mod.rs:156-179` (`section`/`field`) refait `read_to_string` + `parse` à chaque interrogation — `storage.rs:50-59` en enchaîne trois. Fix : lire une fois, passer en paramètre. *(Easy)* — Fait le 2026-08-31 : une lecture du manifeste avant la boucle des features, un `Config` analysé une fois et passé en paramètre. **PARTIEL, l'agent le signale lui-même** : `doctor` lit encore le `Cargo.toml` trois fois hors de cette boucle (`agents`, `base`, `versions`), une fois chacun — ce n'est pas le défaut décrit, mais ce n'est pas la lecture unique que le titre laisse espérer.
- [x] **36. [Perf] Le commentaire « une seule lecture pour toute la fonction » est faux à deux endroits** — `crates/rbs-cli/src/generate/command.rs:194` lit le manifeste avec ce commentaire, puis `:239` refait `metadata::package_name(...)` ; `add/mod.rs:147` porte le même commentaire et relit en `:186` → le nom du paquet est déjà dans le document parsé. Fix : l'exposer sur `Metadata`. *(Easy)* — Fait le 2026-08-31 : `Metadata::package` expose le nom déjà analysé, la fonction libre `metadata::package_name` a disparu. Le champ reste optionnel à dessein, pour préserver la décision d'`upgrade` de ne pas échouer sur un `[package] name` illisible dont il n'a pas besoin.
- [x] **37. [Dette] La fixture de test `fn project()` existe en 18 copies, dont 8 identiques au caractère** — `crates/rbs-cli/src/generate/command.rs:530-547` et `migrate/fresh.rs:151-168` sont identiques ; `seed.rs:199`, `migrate/mod.rs:177` et les six modules de `doctor/` répètent le même `new::create(&Options { name: "demo-api", … })`, soit ~320 lignes. Fix : un `#[cfg(test)] pub(crate) fn project(opts)` partagé. *(Medium)* — Fait le 2026-08-31 : `crates/rbs-cli/src/fixtures.rs`, `#[cfg(test)]`, constructeur en chaîne pour qu'un appelant ne nomme que ce qui le concerne ; aucune valeur de fixture n'a bougé, vérifié champ par champ sur les 21 blocs d'origine.
- [x] **38. [Dette] Trois variantes d'erreur et leur constructeur sont recopiés dans huit modules** — `crates/rbs-cli/src/generate/command.rs:95-101` et `add/mod.rs:64-70` : sur les 18 `enum Error` de `src/`, **8** déclarent le même `Acces { path, source }` au message identique, **6** le même `PasUnProjet`, **3** `WorkingTreeSale` ; `fn access(...)` (`command.rs:476` / `add/mod.rs:276`) est identique au caractère. Fix : un module d'erreurs communes. *(Medium)* — Fait le 2026-08-31 : `crates/rbs-cli/src/errors.rs` porte `Acces` et `WorkingTreeSale` en `#[error(transparent)]`, la constante `PAS_UN_PROJET` partagée par les quatre modules au message identique, et une macro pour les sept `impl From<metadata::RootError>`. **Laissés dehors à dessein** : les `PasUnProjet` d'`add`, `generate` et `upgrade` nomment leur commande — les aligner aurait changé le texte rendu.
- [x] **39. [Dette] Le préambule `plan_for` et le rituel afficher-puis-appliquer sont écrits trois à quatre fois** — `crates/rbs-cli/src/generate/command.rs:184-203` et `add/mod.rs:140-177` (troisième en `upgrade.rs:116-127`) partagent le même `canonicalize → project_root → metadata::read` **jusqu'au commentaire explicatif**, suivi du même garde `git::modified_files` ; `lib.rs:288-292` et `:446-450` sont les mêmes cinq lignes. Fix : une fonction d'entrée commune. *(Medium)* — Fait le 2026-08-31 : `metadata::cible` (lire), `git::garde` (vérifier), `lib.rs::appliquer` (appliquer). `cible` ne garde ni n'affiche : chaque commande garde la maîtrise de son ordre, et les trois ordres sont inchangés — `add` juge son idempotence entre lecture et garde, `upgrade` ne garde que si `!deja_a_jour && !force`.
- [x] **40. [Dette] La carte de résolution des ancres est réécrite quatre fois, dont deux dans le même fichier** — `crates/rbs-cli/src/agents.rs:248-256` et `agents.rs:410-418` portent le même `ANCRES.iter().map(...)` à 160 lignes d'écart, repris en `doctor/anchors.rs:19-28` et `templates.rs:413-419` → ajouter une ancre demande de toucher quatre endroits. Fix : une seule fonction de résolution. *(Easy)* — Fait le 2026-08-31 : `anchors::resolve` porte seule la règle et `anchors::resolved` rend le registre résolu en n'interrogeant le disque qu'une fois pour les onze ancres, au lieu d'une fois par ancre ; les quatre sites s'y raccordent. Deux tests ajoutés l'y verrouillent.
- [x] **41. [Dette] `doctor/redis.rs` et `doctor/jobs.rs` sont le même module deux fois** — `crates/rbs-cli/src/doctor/redis.rs:46-65` et `doctor/jobs.rs:47-66` : ~110 lignes sur 117 et 133 diffèrent seulement par `SECTION` et la chaîne de remède, doc-commentaire partagé au mot près. Fix : un `section_check(titre, section, remède)`. *(Easy)* — Fait le 2026-08-31 : `doctor::section_check` porte la règle commune. **`mail.rs` et `storage.rs` restent dehors, et c'est documenté** : ils lisent le `.env` en plus de la configuration, accumulent plusieurs défauts et ne rendent pas un verdict sur la seule présence d'une section.
- [x] **42. [Dette] La version de PostgreSQL livrée n'est jamais celle qui est testée** — `crates/rbs-cli/tests/common/mod.rs:160` et `generate/bench.rs:41` lancent `("postgres", "17")` alors que tout ce qui est livré épingle `postgres:18-alpine` (`docker-compose.yml.jinja:6`, les quatre `examples/*`, `getting-started.md:128`) — et `bench.rs:48` dit `.expect("PostgreSQL 18 doit démarrer")`. Fix : aligner le harnais sur 18. *(Easy)* — Fait le 2026-08-31 : le harnais démarre la 18 livrée par défaut et la 14 — plancher que `rbs doctor` fait respecter — sous `RBS_TEST_PG`, lue à l'exécution et non par `option_env!` qui la figerait à la compilation. **Le `"17"` n'était pas un oubli** : un commentaire le disait délibéré ; le diagnostic ne voyait que deux des quatre sites, `integration_crud` et `integration_auth` redéclarant leur propre `IMAGE`. Les trois constantes sont remplacées par un point de décision unique, partagé par `#[path]`. Suite Docker verte sous les deux versions (42 passed chacune), et rien n'a cédé sous 14 : l'affirmation qui fonde le plancher est désormais éprouvée plutôt qu'avancée.
- [x] **43. [Sécu] Aucun `cargo audit` en CI, et deux advisories dorment dans `Cargo.lock`** — `.github/workflows/ci.yml` ne contient ni `audit` ni `deny` (grep à vide), et `cargo audit` remonte `RUSTSEC-2023-0071` (`rsa` 0.9.10, via `jsonwebtoken`, sans correctif amont) et `RUSTSEC-2026-0235` (`rkyv` 0.7.46, entrée orpheline via `rust_decimal`, non compilée). Fix : ajouter l'étape, et `cargo update` pour purger `rkyv`. *(Easy)* — Fait le 2026-08-31 : job `audit` en CI, et les deux advisories ignorées dans `.cargo/audit.toml`, chacune avec la commande qui la lèvera. **La purge de `rkyv` annoncée par le diagnostic est impossible** : `cargo update -p rkyv` ne bouge rien, `rust_decimal` 1.42.1 — dernière version parue — épingle `^0.7.46` quand l'advisory demande `>= 0.8.17` ; `cargo tree -i rkyv` rend « nothing to print », l'entrée du lock est orpheline. `cargo audit` exit 0.
- [x] **44. [Dette] La MSRV 1.85 est déclarée et n'est éprouvée nulle part** — `Cargo.toml:12` pose `rust-version = "1.85"`, mais les trois jobs de `.github/workflows/ci.yml` utilisent `dtolnay/rust-toolchain@stable` et le dépôt n'a pas de `rust-toolchain.toml` → une dépendance qui monte sa MSRV casse les utilisateurs sans que la CI le voie. Fix : un job épinglé sur 1.85. *(Easy)* — Fait le 2026-08-31 : **la 1.85 déclarée était fausse**, `sea-orm` 2.0.2 et la famille `sqlx` 0.9 exigent 1.94 — vérifié par `cargo metadata` ; `rust-version` porté à 1.94, job `msrv` épinglé, six phrases de documentation corrigées dont un `CONTRIBUTING` qui affirmait que la CI la tenait. Effet de bord traité : `collapsible_if` étant gagé sur la MSRV, trois `if` imbriqués sont passés en chaînes `let`.
- [x] **45. [Bug] Les tests CRUD générés laissent deux lignes orphelines en base à chaque exécution** — `templates/feature/tests.rs.jinja:179-197` : `two_creations_in_a_row_carry_increasing_ids` fait deux POST sans DELETE, contrairement au test de cycle complet qui nettoie (`:167`), et les tests tournent sur la base de développement du `.env` sans transaction → la table enfle de deux lignes par `cargo test`. Fix : nettoyer, ou tout passer en transaction annulée. *(Easy)* — Fait le 2026-08-31 : nettoyage en fin de scénario, et non transaction annulée — une `DatabaseTransaction` SeaORM n'est pas le `DatabaseConnection` que porte l'`AppState` engendré, l'adopter aurait retouché `state.rs`, les services et les repositories du projet de l'utilisateur.
- [x] **46. [Bug] Les tests générés ne couvrent ni le 422 ni le 409** — `templates/feature/tests.rs.jinja:199-225` ne porte que `an_unknown_id_returns_404` et `an_unreadable_body_returns_400` : aucun corps lisible-mais-invalide alors que le DTO porte `#[validate(email)]` (`dto.rs.jinja:16`), aucun rejeu de valeur `unique` → le chemin de validation, raison d'être de `ValidatedJson`, n'est jamais exercé. Fix : deux tests de plus au gabarit. *(Easy)* — Fait le 2026-08-31 : `an_invalid_email_returns_422` et `a_replayed_unique_value_returns_409`, émis conditionnellement selon les champs demandés ; statut lu dans le code et non supposé — `ValidatedJson` rend `Error::Validation`, soit 422, le 400 restant au corps illisible. Les quatre combinaisons de rendu vérifiées.
- [x] **47. [Doc] Le contrat OpenAPI généré tait trois statuts réellement atteignables** — `templates/feature/controller.rs.jinja:20` ne déclare que 200 pour `list`, alors que `Pagination::from_request_parts` (`rbs-core/src/pagination.rs:82`) rend 400 sur `per_page=abc` ; `create` et `update` ne déclarent aucun 409 bien que `Conflict` soit prêt dans `components/responses` (`openapi.rs:56`). Fix : compléter les annotations `responses`. *(Easy)* — Fait le 2026-08-31, **PARTIEL car diagnostic à demi périmé** : `create` et `update` déclaraient déjà leur 409 sur `main`, seul le 400 de `list` manquait — ajouté sur la forme des autres statuts du fichier. Les entrées de `components/responses` n'étant adressables que par nom, sans type implémentant `ToResponse`, `#[utoipa::path]` ne peut pas les référencer.
- [x] **48. [Arch] `auth` est le seul fragment dont les tests exigeant un service externe ne sont pas `#[ignore]`** — `templates/features/auth/tests.rs.jinja:113` : les ~30 tests joignent la base sans marqueur, quand `jobs` (`:125`), `redis` et `storage` posent tous `#[ignore = "joint …"]` avec le commentaire de convention → `rbs new --with auth && cargo test` échoue tant que PostgreSQL n'est pas démarré *et* migré, là où `--with jobs` passe. Fix : appliquer la convention. *(Easy)* — Fait le 2026-08-31 : les 22 tests d'`auth` prennent le marqueur, aucun n'étant unitaire ; projet engendré pointé sur un port fermé → compile, puis `9 passed; 0 failed; 22 ignored`.
- [x] **49. [Arch] `PUT` généré est une fusion, jamais un remplacement** — `templates/feature/service.rs.jinja:52-58` : le commentaire admet qu'« un champ absent du corps garde sa valeur », donc un champ optionnel ne peut plus repasser à NULL, mais `controller.rs.jinja:67-77` annonce un `put` sans nuance → le client qui envoie un corps partiel croit remplacer la ressource. Fix : `PATCH`, ou `Option<Option<T>>` sur les champs nullables. *(Medium)* — Fait le 2026-08-31 : route et annotation en `PATCH`, sans alias `put`, jusque dans les quatre exemples ; le `PUT /uploads/{id}/content` de `file-drop` est conservé, il remplace réellement un contenu binaire. Le commentaire du service est réduit à ce que `PATCH` ne dit pas de lui-même.
- [x] **50. [Dette] Deux dépendances du workspace ne servent à rien** — `Cargo.toml:43` (`tower-http`) et `:48` (`utoipa-swagger-ui`) ne sont référencés par aucun manifeste de membre et par aucun `use` ; elles viennent de `specs/2026-08-25-rbs-design.md:407-408`, qui les annonçait pour `rbs-core`. Fix : les retirer. *(Easy)* — Note du 2026-08-31 : l'alternative « câbler `tower-http` pour la tâche 27 » est périmée, la borne de temps ayant été posée dans la template du projet généré et non dans une crate du workspace ; `grep -rn tower_http crates/*/src` reste à vide. Seul le retrait subsiste. — Fait le 2026-08-31 : les deux retirées de `[workspace.dependencies]` ; absentes du code, des manifestes des membres et de `Cargo.lock`, qui reste identique après `cargo metadata`. Les templates, qui décrivent le manifeste d'un projet engendré, ne sont pas touchées.

### Features — P2

- [x] **51. [Feature] `rbs completions <bash|zsh|fish|powershell>`** — `crates/rbs-cli/src/cli.rs:14` : `Cli` dérive déjà `Parser` et `CommandFactory` est déjà utilisé en test (`cli.rs:159`), `clap_complete::generate` prend le `Command` tel quel. Valeur : complétion des sous-commandes et des sept noms de features à la tabulation. *(Easy)* — Fait le 2026-09-01 : `rbs completions bash|elvish|fish|powershell|zsh` engendré depuis la `Command` de clap, enrichie du catalogue des fragments **pour la seule complétion** — le parseur réel accepte toujours un nom hors catalogue, sans quoi `rbs add ma-feature --template-dir …` deviendrait impossible. Seuls bash et zsh descendent jusqu'aux valeurs d'un positionnel, vérifié sur les scripts rendus.
- [x] **52. [Feature] `rbs doctor --fix`** — `crates/rbs-cli/src/generate/command.rs:317` : `plan_repair` sait déjà reposer une ancre, et `doctor/anchors.rs` sait déjà les détecter. Valeur : une ancre supprimée par mégarde se répare par une commande au lieu d'un copier-coller manuel. *(Easy)* — Fait le 2026-09-01 : chaque ancre déclare sa ligne d'accroche ; `--fix` repose les absentes par le plan, donc avec restauration, et s'abstient en nommant la raison quand l'accroche manque ou paraît deux fois — une ancre posée au mauvais endroit coûte plus cher qu'une ancre absente. La colonne se déduit de l'accroche plus un cran quand elle ouvre un bloc, éprouvé sur les onze. `--force` n'est accepté qu'avec `--fix`.
- [x] **53. [Feature] `rbs add cors` et l'ancre `// <rbs:layers>`** — `templates/project/src/router.rs.jinja:16-18` a ses `.layer()` en dur ; l'ancre manquante est le préalable commun des tâches 12, 27 et 54. Valeur : tout front qui appelle l'API bute aujourd'hui sur CORS et doit éditer `router.rs` à la main. *(Medium)* — Fait le 2026-08-31 : ancre `layers` posée entre `.merge(docs)` et `.layer(trace)` — la seule position qui donne au middleware ajouté le `request_id` et fasse entrer ses réponses courtes dans la trace ; `ANCRES` passe à 11, `doctor` la réclame, et `rbs add cors` arrive avec elle.
- [x] **54. [Feature] `rbs add rate-limit`** — s'appuie sur l'ancre `layers` de la tâche 53 et sur le fragment `redis` (`features/redis/feature.toml:14-19`, `state_champs`/`state_init`) pour le compteur distribué, repli mémoire sinon. Valeur : c'est le correctif de la tâche 12. *(Medium)* — Fait le 2026-08-31 : fragment `rate-limit`, compteur Redis (pipeline `SET NX EX` + `INCR`) quand le fragment `redis` est là, fenêtre fixe en mémoire sinon — le choix se fait à la pose, et le fichier engendré dit lequel. 429 au format du projet avec `Retry-After`, et la réponse enregistrée dans `components/responses` du noyau.
- [x] **55. [Feature] Contrôles de santé par feature installée** — `crates/rbs-core/src/health.rs:44-49` : `Checks` est `#[non_exhaustive]` et son commentaire annonce déjà « cache, file, stockage » ; la boucle `FEATURE_CHECKS` de `doctor/mod.rs:127` sait quelles features sont là. Valeur : `/health` répond `ok` alors que Redis ou S3 sont morts. *(Medium)* — Fait le 2026-09-01 : `health::report` mène les sondes de front, chacune bornée à 2 s, et `Checks` porte les extras en `#[serde(flatten)]` — `utoipa` en rend `additionalProperties`, lu sur le schéma réel et non supposé. Douzième ancre `health_probes` ; `redis` et `storage` inscrivent leur sonde, `mail` et `jobs` n'en ont pas — SMTP trop coûteux à sonder à chaque appel, file déjà couverte par la base. `Health`/`Checks` perdent `Copy` : lint semver déclassé et justifié dans le manifeste.
- [x] **56. [Feature] `rbs add observability` (OTLP + `/metrics`)** — `crates/rbs-core/src/trace.rs` pose déjà le span, et l'ancre `STARTUP` (`anchors.rs:140`) accueille l'initialisation de l'exporteur. Valeur : une API générée est aujourd'hui muette en production au-delà de ses logs JSON. *(Medium)* — Fait le 2026-09-01 : feature cargo `observability` sur `rbs-core`, `logs::init()` greffant la couche OTLP — seul endroit possible, l'abonné global étant posé avant l'ancre `startup`. Endpoint et nom de service par les variables standard `OTEL_*` ; absentes, le fragment installé reste inerte. Le fragment engendre le middleware de comptage — gabarit de route et jamais l'URL, sans quoi le collecteur tomberait — et sert `/metrics` sur un listener séparé, 9090 par défaut, jamais sur le routeur public.
- [x] **57. [Feature] Filtres et tri depuis la query string dans le CRUD généré** — `templates/feature/repository.rs.jinja:17-22` a `order_by_desc(Column::Id)` en dur, et `generate/fields.rs:23` connaît le type de chaque champ à la génération. Valeur : `POST /articles/filter` avec un body  sans écrire une ligne. *(Hard)* — Fait le 2026-09-01 : `POST /<module>/filter` porte les conditions dans son corps, la pagination restant en chaîne de requête. Les opérateurs vivent au noyau (`rbs-core/src/filter.rs` — `Comparison<T>`, `TextMatch`, `Sort`), les colonnes dans un septième fichier engendré dont `repository.rs` est le seul client : un nom reçu du client n'est jamais qu'un bras de `match` écrit à la génération, et une colonne de tri inconnue rend 400 en nommant celles qui sont acceptées. `list` devient le filtre vide et le total ne compte plus que les lignes retenues. `cargo test --workspace` 1101 passés, 0 échec ; suite Docker `-- --ignored` exit 0, 45 passés, 0 échec, dont `the_filter_narrows_the_list ... ok` et `an_unknown_sort_column_returns_400 ... ok` joués contre PostgreSQL par `integration_crud` ; `integration_examples` 17 passés et `cargo check` propre sur les quatre exemples ; `npm run build` dans `docs/` exit 0, les trois extraits injectés dans les deux langues ; clippy et fmt sans sortie. Écart assumé sur la conception : `contains` rend un `LIKE` dont la casse suit la collation du moteur — `ILIKE` n'existe dans sea-orm que par `PgExpr`, et le CLI engendre aussi pour MySQL et SQLite.

## P3 — polish et dette basse

- [x] **58. [Bug] `anchors::insert` écrit toujours des fins de ligne `\n`, quelles que soient celles du fichier hôte** — `crates/rbs-cli/src/anchors.rs:275` : `format!("{indentation}{line}\n")`, alors que `line_of` accepte un fichier CRLF (`line.trim()` absorbe le `\r`) → sur un dépôt en `core.autocrlf=true`, chaque insertion pose des lignes LF au milieu d'un fichier CRLF, et le `cargo fmt --check` du workflow `ci` généré peut échouer sur ce que le CLI a écrit. *(Easy)* — Fait le 2026-09-01 : `eol()` lit la fin de ligne du fichier hôte — le premier saut décide, un fichier mélangé n'est pas normalisé — et les trois sites d'écriture la reprennent. Le troisième était hors de la lettre du backlog et portait le défaut en pire : `repose` faisait `lines().join("\n")`, ce que le test rouge a montré convertir le fichier *entier* en LF, pas la seule ligne posée. Trois tests CRLF vus rouges (branche simple, branche triée, `repose`) ; `cargo test --workspace` 1104 passés 0 échec, suite Docker `--ignored` en exit 0 (45 passés), clippy et fmt propres.
- [x] **59. [Bug] Les zones de l'`AGENTS.md` se repèrent au premier marqueur du document, où qu'il soit** — `crates/rbs-cli/src/agents.rs:131-138` : `bounds` fait un `source.find("<!-- rbs:{zone}")` sans exiger que le marqueur soit seul sur sa ligne, et `splice` (`:150`) réécrit tout jusqu'à la fermeture → un développeur qui *cite* `<!-- rbs:inventory -->` dans sa propre section voit `rbs upgrade` effacer le texte intermédiaire, dans un fichier décrit comme le sien. *(Easy)* — Fait le 2026-09-01 : `marker_line` impose qu'un marqueur soit seul sur sa ligne, aux blancs près, ouverture comme fermeture — la règle qu'`anchors::marks` portait déjà pour les ancres du code, et dont l'`AGENTS.md` était le seul mécanisme d'insertion exempté. Le nom de zone doit finir où le marqueur le dit, sans quoi `inventory` reconnaîtrait l'ouverture d'un `inventory-extra`. Trois tests vus rouges (citation de l'ouverture, citation de la fermeture, lecture du corps) ; `cargo test --workspace` 1107 passés 0 échec, suite Docker `--ignored` en exit 0 (45 passés), clippy et fmt propres.
- [x] **60. [Bug] L'inventaire des entités compte les accolades des chaînes et des commentaires** — `crates/rbs-cli/src/generate/entities.rs:91-92` fait `line.matches('{').count()` sur toutes les lignes → un `model.rs` contenant `format!("{{")` ou un doc-commentaire déséquilibré décale `closes_at` et rattache les entités suivantes au mauvais module : `relations::resolve` écrit alors un `belongs_to` faux, là où le module se dit « faillible du côté du refus ». *(Medium)* — Fait le 2026-09-01 : un automate lexical dont l'état franchit les fins de ligne (code, chaîne, chaîne brute, caractère littéral, commentaire de ligne, commentaire de bloc imbriqué) rend pour chaque ligne son code hors commentaires — chaînes conservées, `table_name` y lisant le nom de la table — et le compte d'accolades hors chaîne et hors commentaire. Une seule passe ferme les trois trous que le fichier avait : les accolades de texte, mais aussi une entité et un module mis au rebut en commentaire de bloc, que le scan relevait comme réels. Quatre tests vus rouges, dont un montrant `sessions` rattachée au module `user` ; `cargo test --workspace` 1111 passés 0 échec, suite Docker `--ignored` en exit 0 (45 passés), clippy et fmt propres.
- [x] **61. [Sécu] Aucune contrainte de validation ne peut s'exprimer par `--fields` au-delà d'un `email` déduit du nom** — `templates/feature/dto.rs.jinja:16-19` n'émet `#[validate(email)]` que sur `field.valide_email` (`generate/fields.rs:272`) → tous les autres champs atterrissent dans un `#[derive(Validate)]` vide, et les `Create*` acceptent des chaînes de longueur arbitraire sur chaque route publique. *(Medium)* — Fait le 2026-09-01 : un `string` reçoit d'office `#[validate(length(max = 255))]`, et `max=<n>` — premier modificateur à valeur de la grammaire — surcharge la borne sur `string` comme sur `text`, qui n'en reçoit aucune par défaut puisque c'est le type qu'on choisit pour la dépasser. Refusé hors champ textuel et sur une valeur non strictement positive, chacun avec son message et son indice. `Field::validations()` rend les contraintes assemblées en un seul attribut : `validator` n'en lit qu'un par champ, et deux attributs empilés engendreraient un projet qui ne compile pas. Les quatre exemples ont suivi, le test de non-dérive faisant oracle (17/17) ; doc `cli/generate.md` EN et FR, `CHANGELOG` EN/FR. `cargo test --workspace` 1119 passés 0 échec, suite Docker `--ignored` en exit 0 (45 passés) — c'est elle qui prouve que l'attribut composé compile —, clippy et fmt propres.
- [x] **62. [Sécu] Pas de `Cache-Control: no-store` sur les réponses portant des jetons** — `templates/features/auth/dto.rs.jinja:30-36` : `TokenPair` est un `Json` nu → access et refresh tokens sont cacheables par tout intermédiaire. *(Easy)* — Fait le 2026-09-01 : `impl IntoResponse for TokenPair` pose `Cache-Control: no-store` et `Pragma: no-cache` (RFC 6749 §5.1). L'en-tête tient au type et non aux deux handlers : un troisième, ajouté plus tard dans le projet de l'utilisateur, le reçoit sans y penser, là où un tuple répété dans chaque handler l'aurait laissé passer sans bruit. Le fragment reçoit son test d'en-tête sur les deux routes. Première passe Docker **rouge** — le test comparait un `Option<Result<&str, ToStrError>>`, que rien n'implémente `PartialEq` : le code d'un fragment n'est compilé par rien avant Docker. Après correction : `cargo test --workspace` 1121 passés 0 échec, suite Docker `--ignored` en exit 0 (45 passés), dont `the_auth_tests_of_the_generated_project_pass` ; non-dérive 17/17, clippy et fmt propres.
- [x] **63. [Perf] Chaque fragment de feature est lu et alloué deux fois à l'installation** — `crates/rbs-cli/src/templates.rs:117-146` : `files()` et `manifest()` appellent tous deux `all()`, qui parcourt l'arborescence et copie chaque source en `String` (`:206-220`), et `add::plan_for` (`add/mod.rs:180`) enchaîne les deux → le fragment `auth` entier est matérialisé deux fois par feature de `rbs new --with`. *(Easy)* — Fait le 2026-09-01 : `Source::manifest_and_files()` fait la passe une fois et sépare le manifeste des fichiers à déposer ; `files()` s'y ramène, si bien que la règle « le manifeste n'est pas un fichier déposé » n'est plus écrite qu'à un endroit. `manifest()` n'avait plus d'appelant et a été supprimée plutôt que couverte d'un `allow(dead_code)`. Rien versé au CHANGELOG : le gain est interne et sans effet observable pour qui installe rbs. `cargo test --workspace` 1121 passés 0 échec, suite Docker `--ignored` en exit 0 (45 passés), clippy et fmt propres.
- [x] **64. [Perf] `doctor relations` relit chaque `model.rs` autant de fois qu'il porte d'entités** — `crates/rbs-cli/src/doctor/relations.rs:32-35` : `entities::scan` a déjà lu chaque fichier (`generate/entities.rs:41`), et la boucle refait un `read_to_string` par entité → un `model.rs` à trois entités est lu quatre fois. *(Easy)* — Fait le 2026-09-01 : les entités sont regroupées par fichier avant lecture, ce que le commentaire du contrôle annonçait déjà sans que le code le fasse — quatre lectures deviennent deux. La seconde est celle d'`entities::scan` : lui faire rendre les sources l'épargnerait, mais ses deux autres appelants n'en ont aucun usage et en porteraient la mémoire pour rien, ce que le code dit maintenant. Le gain est prouvé et non inspecté : la lecture passe par un paramètre et le test compte les appels — deux entités, une lecture, vu rouge à 2 d'abord. `cargo test --workspace` 1122 passés 0 échec, suite Docker `--ignored` en exit 0 (45 passés).
- [x] **65. [Perf] Les deux formateurs de logs allouent quatre à six fois par événement** — `crates/rbs-core/src/logs/pretty.rs:85-116` : `format!` pour le niveau et la cible, `paint` qui rend un `String` même sans ANSI (`:43-49`), un `Vec<String>` et un `fields.clone()` par span parent ; côté JSON, `timestamp()` alloue par événement (`json.rs:31`). Coût fixe sur chaque requête tracée. *(Medium)* — Fait le 2026-09-01, **volet `pretty` seulement**. `paint()` n'existe plus : rendre le *style* plutôt que le texte peint laisse `prefix`/`suffix` écrire droit dans le flux sans allouer, et n'écrire rien du tout quand la couleur est coupée. Le `Vec<String>`, les `clone()` de spans et le `join(" ")` deviennent une écriture à la file. Le chemin chaud n'alloue plus rien, la seule allocation restante étant dans le constructeur. Le rendu coloré est figé octet à octet par un test écrit *avant* la modification — les séquences ANSI doivent encadrer le champ déjà aligné, faute de quoi elles décaleraient les colonnes. Le volet JSON est **écarté et versé en tâche 103** : `timestamp()` y est une allocation parmi une douzaine que la construction d'une `Map<String, Value>` impose, et la retirer seule n'aurait rien changé de mesurable. `cargo test --workspace` 1124 passés 0 échec, suite Docker `--ignored` en exit 0 (45 passés).
- [x] **66. [UX] `rbs dev` ne dit jamais quel fichier a déclenché le redémarrage** — `crates/rbs-cli/src/dev/watch.rs:142-146` : le handler appelle `job.restart_with_signal(...)` sans rien afficher, alors que `action.paths()` porte le fichier → impossible de distinguer un redémarrage d'un crash. *(Easy)* — Fait le 2026-09-01 : `rbs dev` annonce « redémarrage : src/main.rs » ; sur un regroupement — un `cargo fmt` en touche vingt — le premier est nommé et les autres comptés, un chemin hors de la racine gardant sa forme entière. La mise en forme est une fonction pure, éprouvée sur les trois cas. `cargo test --workspace` 1127 passés 0 échec, suite Docker `--ignored` en exit 0 (45 passés).
- [x] **67. [Dette] Trois fragments posent `#![allow(dead_code)]` sur le module entier** — `templates/features/jobs/mod.rs.jinja:3`, `redis/mod.rs.jinja:3`, `mail/mod.rs.jinja:11`, alors que `auth/guard.rs.jinja:24` et `storage/mod.rs.jinja:32` savent le poser sur le seul item mort avec une instruction de retrait → sur `jobs`, l'allow couvre 98 lignes dont `Registry` et `registry()` : un handler oublié ne s'annonce plus. *(Easy)* — Fait le 2026-09-01 : les trois `#![allow(dead_code)]` de module tombent **sans qu'aucun `#[allow]` d'item les remplace** — sur un projet à `src/lib.rs`, un item public d'un module public reste joignable de l'extérieur du crate, et le lint ne le vise pas ; la permission datait du parc sans bibliothèque. Lint prouvé vivant par un `fn temoin_mort()` ajouté au projet engendré, qui échoue bien. `clippy -D warnings` à 0 sur `file-drop` et `newsletter-queue`, suite `--ignored` à 38 tests verts. Réserve : un projet antérieur au squelette à bibliothèque verra désormais les avertissements que la permission masquait.
- [x] **68. [Arch] `redis` expose un accesseur d'état, `mail` et `storage` laissent un champ public nu** — `templates/features/redis/mod.rs.jinja:187-191` ajoute `impl AppState { pub fn cache(&self) }` en justifiant que « l'accesseur arrive avec la feature et repart avec elle », mais `mail/feature.toml:36-40` et `storage/feature.toml:26-29` injectent leur champ sans accesseur, avec un `#[allow(dead_code)]` de compensation. *(Easy)* — Fait le 2026-09-01 : `mail` et `storage` reçoivent leur `impl AppState`, leur `state_champs` perd le `#[allow(dead_code)]` de compensation, et la sonde de `storage` passe par `state.storage()`. Prouvé par `the_probes_installed_by_two_fragments_compile_into_the_health_route`, qui compile réellement le projet engendré.
- [x] **69. [Dette] `ci/feature.toml` ne déclare rien et repose sur une convention muette** — `templates/features/ci/feature.toml:1-2` tient en une section `[feature] description`, quand les six autres énumèrent leurs `[[files]]` ; l'installation marche parce que `templates.rs:files()` copie tout sauf le manifeste → `ci` est la seule feature dont le manifeste ne dit pas qu'elle écrit `.github/workflows/ci.yml`. *(Easy)* — Fait le 2026-09-01 : le manifeste déclare `.github/workflows/ci.yml.jinja` → `.github/workflows/ci.yml` ; les dix fragments énumèrent désormais leurs fichiers. `the_ci_plan_creates_the_workflow_its_manifest_declares`, vu rouge d'abord — privé de son `.github/`, le `source` fait échouer l'installation sur `TemplateAbsente`. La preuve porte sur la résolution du manifeste par le plan, non sur une installation compilée.
- [x] **70. [Doc] Le commentaire du seed généré décrit une origine de l'`id` que la migration ne produit plus** — `templates/feature/seed.rs.jinja:21` dit que « `id`, `created_at` et `updated_at` tiennent leur valeur du défaut de la colonne », faux pour `id` depuis que `feature/migration.rs.jinja:15-19` pose la clé sans `.default(...)` et que `model.rs.jinja:60-66` la remplit dans `ActiveModelBehavior::new()` — choix verrouillé par le test `the_primary_key_leaves_its_default_to_the_application`. *(Easy)* — Fait le 2026-09-01 : le commentaire attribue l'`id` à `ActiveModelBehavior::new()` et les seuls `created_at`/`updated_at` au défaut de colonne. Les quatre seeds d'exemples suivis par diff entre deux générations ; non-dérive verte, 17 tests, 0 échec.
- [x] **71. [Dette] Sept tests n'assertent rien et tournent en CI** — `crates/rbs-cli/src/generate/{entity.rs:348,363, dto.rs:213, repository.rs:164, migration.rs:452, service.rs:197, controller.rs:365}` : six corps `#[ignore = "affichage pour revue humaine"]` sont un `println!` nu, et `.github/workflows/ci.yml:62` lance le lot `--ignored`. Séparément, `Plan::actions()` (`plan/mod.rs:50`) porte `#[allow(dead_code)]` : ses 25 appels sont tous dans `mod tests`. *(Easy)* — Fait le 2026-09-01 : les sept aperçus supprimés, chacun après vérification qu'un test assertif voisin couvrait déjà son cas — 38 ignorés contre 45. `Plan::actions()` passe sous `#[cfg(test)]` ; l'exemption ne disparaît pas du fichier mais se resserre sur le champ, en `expect(dead_code)` borné à `not(test)`, qui échouera de lui-même quand il deviendra inutile. PARTIEL sur ce seul point.
- [x] **72. [Doc] Les trois `README.md` de crates et d'`examples/` sont anglais seulement et échappent au contrôle de parité** — `examples/README.md` (214 lignes), `crates/rbs-cli/README.md` et `crates/rbs-core/README.md` n'ont pas de version française, et `docs/scripts/parite.mjs` ne balaie que `docs/` → le vérificateur ne les signalera jamais. *(Medium)* — Fait le 2026-09-01 : les trois `README.fr.md` écrits, `parite.mjs` étendu d'un troisième jeu de paires, et le renvoi croisé posé des deux côtés — sans lui la traduction n'était découvrable par personne. Vigilance prouvée par trois écarts introduits puis retirés (niveau de titre, langue de bloc, fichier manquant), chacun en exit 1. Au passage `IMPROVE.md` rejoint `RACINE_MONOLINGUE` : le contrôle était déjà rouge avant ce lot.
- [x] **73. [Dette] `sea-orm` impose ses features `with-chrono` et `with-uuid` à tous les consommateurs de `rbs-core`** — `Cargo.toml:38` les active au workspace, `rbs-core` en hérite, et `rg 'chrono|Uuid' crates/rbs-core/src` ne rend rien ; cargo les inline dans le manifeste publié. *(Easy)* — Fait le 2026-09-01 : `with-chrono` et `with-uuid` retirés du workspace — `rbs-core` n'en fait aucun usage et le projet engendré les déclare lui-même. Les quatre combinaisons de features de `rbs-core` compilent, `Cargo.lock` inchangé.

### Features — P3

- [x] **74. [Feature] `rbs generate crud --soft-delete`** — `generate/migration.rs` + `templates/feature/repository.rs.jinja:39-43` (`delete_by_id` en dur), en injectant un `deleted_at:datetime` par `generate/fields.rs:23`. Valeur : une suppression accidentelle par l'API est aujourd'hui définitive. *(Medium)* — Fait le 2026-09-02 : `--soft-delete` rend le `DELETE` logique — la ligne reste, sa colonne `deleted_at` datée — **sans que le contrat HTTP change** : 204 sur suppression, 404 sur une seconde, 404 sur lecture d'une ligne supprimée, disparition de `list` et de `filter`. La colonne est injectée et non déclarée : `--fields "deleted_at:…"` reste légitime hors du drapeau, et n'est refusé que sous lui. Un champ `unique` voit sa contrainte quitter la colonne pour un index restreint aux lignes vivantes, ce qui permet de se réinscrire avec une adresse qu'une ligne supprimée occupait. **MySQL n'a pas d'index partiel** — sea-query n'en protège pas, son builder y écrit le `WHERE` comme les autres — donc la migration branche à l'exécution et y garde une unicité globale : une valeur supprimée y reste réservée, ce que la documentation dit. Le repository reste la seule couche à construire une requête ; le service et son `Error::NotFound` sont intacts. **Le rebond après suppression est prouvé, non supposé** : gabarit amputé de son `and_where`, les bancs virent au rouge sur les deux moteurs avec la signature d'un index global, et repassent au vert restaurés. Trois bancs — PostgreSQL, SQLite, MySQL — appliquent la migration et lancent la suite HTTP engendrée. Relevés en passant et corrigés : le rendu de la migration n'était un point fixe de rustfmt sur aucun de **quatre** seuils de longueur combinée table+champ, dont un (`ColumnDef`, somme ≥ 58) **antérieur au drapeau et vrai sans lui** — le balayage de test ne faisait varier que le nom de feature, jamais celui du champ ; les trois blocs `--help` de `docs/docs/cli/generate.md` ne portaient aucun marqueur de transcript et pouvaient donc mentir, ce qu'ils faisaient. `cargo test --workspace` 1202 passés 0 échec 42 ignorés ; suite Docker `--no-fail-fast -- --ignored` en sortie 0, 42 passés dont les trois bancs ; `integration_examples --include-ignored` 17 passés aucune dérive ; `integration_docs` 11 passés ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie ; `parite.mjs` exit 0, 29/29.
- [x] **75. [Feature] Pagination par curseur dans `rbs-core`** — `crates/rbs-core/src/pagination.rs:73` : `Pagination` est déjà un `FromRequestParts`, l'ajout est purement additif sur une API figée, et l'`id` étant un UUIDv7 le curseur est l'`id` lui-même (`repository.rs.jinja:15-16`). Valeur : listes stables au-delà de quelques milliers de lignes, là où `offset()` dégrade. *(Medium)* — Fait le 2026-09-02 : `Cursor` et `CursorPage<T>` s'ajoutent à `rbs-core`, **opt-in** — le CRUD engendré garde `Pagination` et son `meta.total`, basculer l'aurait retiré de toute réponse déjà servie. `after` est exclusif, illisible il rend 400, quand un `per_page` hors bornes est ramené en silence : la même asymétrie que `Pagination`, et pour la même raison. La page ne porte ni `total` ni `total_pages` — le `COUNT(*)` est ce que le curseur existe pour ne pas payer — et `next` s'éteint dès que la page est plus courte que demandée, règle tenue par le constructeur plutôt que réécrite par chaque appelant. Aucun curseur sur un tri arbitraire : sur une colonne à valeurs égales la frontière serait ambiguë et la page suivante sauterait des lignes. Deux découvertes en chemin : `rbs-core` n'avait aucune dépendance UUID — `with-uuid` avait été retiré du workspace faute d'usage, il en a un désormais et la feature est réactivée sur cette seule crate, ce qui donne le type même des entités engendrées sans écart de version ; et le dérive `ToSchema` sur `Option<Uuid>` ne compilait pas sans la feature `uuid` d'utoipa, celle que les projets engendrés activent déjà. `cargo test -p rbs-core` 113 passés 0 échec ; `cargo semver-checks -p rbs-core --all-features` 195 contrôles 195 pass, « no semver update required » ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie ; `parite.mjs` exit 0, 29/29.
- [ ] **76. [Feature] `rbs add audit`** — moule de `templates/features/jobs/feature.toml` (migration + modèle + ancres) et extracteur `Identity` (`rbs-core/src/lib.rs:55`) pour l'auteur de l'écriture. Valeur : « qui a modifié quoi, quand », qu'aucun fragment ne couvre. *(Medium)*
- [ ] **77. [Feature] `rbs add scheduler`** — `templates/features/jobs/worker.rs.jinja` a déjà la boucle de réservation et de réessai, l'ancre `STARTUP` (`anchors.rs:140`) lance le tick. Valeur : les jobs ne s'enfilent aujourd'hui que sur événement, rien ne déclenche un nettoyage nocturne. *(Medium)*
- [x] **78. [Feature] `rbs generate crud --with-upload`** — trait `Storage` déjà dans l'`AppState` (`templates/features/storage/mod.rs.jinja`), route multipart à ajouter dans `templates/feature/controller.rs.jinja`. Valeur : l'exemple `file-drop` prouve le besoin, chaque projet recâble le multipart à la main. *(Medium)* — Fait le 2026-09-03 : `--with-upload` monte trois routes sur `/<ressource>/{id}/content` — `PUT` (204), `GET` (200 en octet-stream), `HEAD` (204/404) — adossées au trait `Storage` du fragment. **Deux écarts assumés avec l'énoncé ci-dessus** : le corps voyage en `application/octet-stream` et **non en multipart**, parce que l'exemple qui prouvait le besoin le fait déjà ainsi et qu'un base64 chargerait le fichier deux fois en mémoire ; et **aucune colonne n'est injectée**, la clé étant dérivée de l'`id` — `file-drop` porte bien `content_type` et `size`, mais aucun de ses handlers ne les lit. Sans le fragment `storage`, le drapeau est refusé avant tout écrit, en nommant `rbs add storage`. La borne de taille est un `DefaultBodyLimit` sur la seule route de dépôt. **Deux défauts trouvés par la revue de branche et corrigés, non différés** : les routes de contenu échappaient au garde de `--role` — `PATCH` exigeait le rôle quand `PUT …/content`, qui remplace la charge utile, était ouvert à tous, et l'aide clap comme la documentation énuméraient un ensemble de routes devenu faux ; et sous `--soft-delete` le contenu était détruit par une suppression pourtant logique, si bien qu'une ligne restaurée revenait vide — il est désormais conservé. **Ce qui est prouvé par exécution** : le projet engendré compile et passe `clippy -D warnings`, y compris dans la combinaison `--with-upload --soft-delete --role admin` qui n'avait jamais été exercée ; l'aller-retour dépôt → relecture est joué en HTTP contre le binaire lancé, sur le backend `fs`, avec comparaison octet à octet d'un contenu non-UTF8 ; un `PUT` au-delà de la borne rend 413 et le contenu antérieur reste intact ; le refus sans fragment n'écrit rien. **Ce qui ne l'est pas** : le backend `s3` ; les 401/403 de `put_content` contre un serveur réel ; la conservation du contenu sous suppression logique, prouvée au rendu seulement. Au passage, une fixture de rendu figée couvre les deux fichiers que la documentation cite et que la comparaison d'exemples exclut — le trou était une ligne vide *manquante*, qu'aucun balayage rustfmt ne peut voir. `cargo test --workspace` 1234 passés 0 échec 45 ignorés ; suite Docker `--no-fail-fast -- --ignored` en sortie 0, 45 passés ; `integration_examples --include-ignored` 17 passés aucune dérive ; `integration_docs` 11 passés dont les gardes de transcript ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie ; `parite.mjs` exit 0.
- [ ] **79. [Feature] `rbs generate client --lang ts`** — le document OpenAPI est déjà produit sans serveur démarré par `ApiDoc::openapi()` (`templates/project/src/openapi.rs.jinja:26-33`), et `plan/mod.rs` fournit le rituel plan → afficher → appliquer. Valeur : un client typé qui suit le backend, argument d'adoption fort. *(Hard)*
- [ ] **80. [Feature] `rbs add webhooks`** — l'enfilage transactionnel de `templates/features/jobs/queue.rs.jinja` fait déjà la livraison-avec-réessais ; reste la table des abonnements. Valeur : sortants signés HMAC sans rien réécrire de la file. *(Hard)*
- [ ] **81. [Feature] `rbs new --preset api|worker|full`** — `cli.rs:42-44` : `--with` accepte déjà une liste, et `new.rs:186-196` installe dans l'ordre alphabétique. Valeur réelle faible tant que `--with` existe : à ne faire qu'en dernier. *(Easy)*

---

- [x] **82. [Dette] Le bloc d'attributs `sea_orm` du modèle assemble une liste qui ne peut plus avoir qu'un élément** — `crates/rbs-cli/templates/feature/model.rs.jinja:10-14` joint jusqu'à trois attributs par `| select | join(", ")`, mais depuis le refus d'`unique`/`index` sur `text` aucune ligne de `--fields` n'en produit deux : `column_type_attribute` ne rend `Some` que pour `Text` (`generate/fields.rs:77`) et jamais pour une référence, un scalaire non-`text` ne peut cumuler `unique` et `index` (`RedundantIndex`), et une référence a `index = !unique`. Fix : simplifier le bloc, ou documenter l'invariant si l'on veut garder la souplesse. *(Easy)* — Fait le 2026-09-02 : le bloc passe en `if`/`elif`/`elif`, précédé d'un commentaire qui porte les trois raisons pour lesquelles un seul attribut suffit. L'identité du rendu est prouvée et non supposée : un harnais temporaire rendant 39 combinaisons acceptées de `--fields` donne le même MD5 (`6e4d6b674e56fb17bd6199c09829a9e4`) avant et après, `diff` vide — et aucun des 39 rendus ne portait deux entrées, ce qui confirme le diagnostic. Deux tests permanents figent le rendu, cas par cas puis sur une ligne complète. `cargo test --workspace` 1143 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql` et `each_engine_produces_a_project_whose_tests_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **83. [Bug] `doctor` tait la faute d'un manifeste illisible par une seconde porte** — `crates/rbs-cli/src/doctor/base.rs`, `pilote()` : le `.ok()?` y avale exactement l'erreur de parsing que `database_of` a cessé d'avaler. Sur un manifeste cassé, `ecart` s'exécute d'abord et ne rend rien, silencieusement. Fix : propager, comme sa voisine. *(Easy)* — Fait le 2026-09-02 : `pilote()` rend `Result<Option<Database>, metadata::Error>`, séparant le manifeste illisible (propagé, comme `database_of`) du manifeste sans `sea-orm`, qui n'est pas une faute ; `base::check` traite les deux portes par un `manifeste_fautif()` commun, donc même détail et même remède. Tests vus rouges avant correction. **La sortie utilisateur est inchangée**, et on sait maintenant pourquoi : `doctor::run` appelle `metadata::project_root` en premier, qui abandonne tout le diagnostic sur un manifeste cassé — la branche corrigée n'est atteignable qu'en test unitaire. Ce qui est fermé, c'est la dépendance du signalement à l'ordre des deux contrôles. `cargo test --workspace` 1143 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql` et `each_engine_produces_a_project_whose_tests_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **84. [Bug] `rbs add docker` écrit des identifiants vides quand l'URL du projet est illisible** — le fragment dépose désormais `POSTGRES_USER`/`POSTGRES_PASSWORD` dans le `.env` depuis l'URL parsée ; un `.env` dont l'URL porte un `/` ou un `?` non encodé donne `connexion == None`, donc deux clés vides et un service qui ne monte pas. L'URL du compose reste sûre. Le projet est déjà cassé en amont — sqlx refuserait la même URL — mais `add docker` ne le lui dit pas. Fix : refuser en nommant l'URL. *(Easy)* — Fait le 2026-09-02 : `add::Error::UrlIndecomposable` refuse dans `plan_for`, avant que le `plan::Builder` n'existe, en nommant `RBS_DATABASE__URL`, l'URL fautive et l'encodage à appliquer ; le refus est gardé par `a_un_serveur()`, SQLite restant installable. Rouge documenté : le plan portait littéralement `AjouterVariable { key: "POSTGRES_USER", value: "" }`. `new.rs:378` porte le même `.unwrap_or_default()` mais pas la même conséquence — `connexion == None` éteint `compose_utile()`, donc ni compose ni clé `POSTGRES_` n'atteignent un fichier ; un test le tient, et refuser là fermerait la porte aux URL à socket Unix, légitimes. `cargo test --workspace` 1143 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql` et `each_engine_produces_a_project_whose_tests_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **85. [Dette] Aucun test ne prouve que `GET /health` répond 503 quand la base pend** — `crates/rbs-core/src/health.rs` : la borne de 2 s est éprouvée sur le futur (`bounded_ping(std::future::pending())` sous horloge en pause), mais `DatabaseConnection` ne sait rendre qu'un pool connecté ou déconnecté, dont le `ping` répond immédiatement : le chemin HTTP de bout en bout n'est couvert par rien. Fix : un double de `ConnectionTrait`, ou un test d'intégration sur une base joignable mais muette. *(Medium)* — Fait le 2026-09-02 : un trait `Ping` rend le ping injectable, et `a_database_that_never_answers_gives_a_503_over_http` éprouve le 503 et son corps sur une réponse HTTP réelle, base muette sous horloge en pause ; `cargo test -p rbs-core` 105 passés, `cargo semver-checks -p rbs-core --all-features` 195/195 sans montée exigée.
- [x] **86. [Dette] Le rendu d'un `use` long diverge de ce que rustfmt écrirait** — `the_guarded_render_is_already_what_rustfmt_would_write` a dû être restreint à `tag` et `articles` : sur `administrative_documents`, la ligne `use super::dto::{…}` franchit 100 colonnes et c'est `format::format_batch` qui la reformate à l'écriture, non la template. Divergence préexistante, sans rapport avec le garde de rôle. Fix : émettre le `use` déjà éclaté au-delà d'un seuil. *(Easy)* — Fait le 2026-09-02 : le gabarit assemble la ligne et l'éclate lui-même au-delà du seuil, mesuré à **98 colonnes et non 100** — rustfmt bascule deux colonnes sous `max_width`. Test vu rouge sur `administrative_documents` (109 colonnes) avant correction, et couverture balayée de 3 à 39 caractères d'entité contre `bench::formatted` : conforme jusqu'à 23, le test montant désormais à `organizational_structures`. Au-delà de 24, rustfmt passe au remplissage glouton multi-lignes, régime non couvert et laissé à `format_batch` — la limite est écrite dans le gabarit et dans le doc-comment du test. `cargo test --workspace` 1143 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql` et `each_engine_produces_a_project_whose_tests_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.


---

**Relevés le 2026-09-01**, pendant l'exécution des vingt tâches P2 : constats faits en
passant, chacun revérifié dans le code avant d'être inscrit, aucun corrigé à la volée. La
priorité est portée sur la ligne, ce bloc étant hors des sections.

- [x] **87. [Arch] `P2` — Les tests CRUD engendrés sont désormais les seuls que `cargo test` lance sans base** — `crates/rbs-cli/templates/feature/tests.rs.jinja` ne porte aucun `#[ignore]` (grep à vide), alors que la tâche 48 vient d'aligner `auth` sur la convention que `jobs`, `redis` et `storage` suivaient déjà → `rbs new --with auth && cargo test` passe maintenant sans PostgreSQL, mais `rbs new && rbs generate crud … && cargo test` échoue toujours. L'incohérence a changé de camp. *(Easy)* — Fait le 2026-09-01 : `tests.rs.jinja` porte le bandeau d'`auth` et `#[ignore = "joint la base du projet"]` sur ses sept scénarios ; trois bancs qui lançaient `cargo test` sans `--include-ignored` cessaient dès lors de rien exécuter et ont été corrigés (`bench::test_of`, `integration_new`, `integration_crud`), leur garde-fou « aucun test n'a été exécuté » les ayant signalés. L'`AGENTS.md` engendré donne la commande dans les deux langues. `cargo test --workspace` 923 + 155, 0 échec ; suite Docker `-- --ignored` exit 0, 45 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql` et `each_engine_produces_a_project_whose_tests_pass` ; `integration_examples` vert après régénération des quatre exemples ; clippy et fmt sans sortie.
- [x] **88. [Bug] `P2` — Un champ `unique` non textuel fait échouer la seconde exécution des tests engendrés** — `crates/rbs-cli/src/generate/tests_http.rs:77-79` : le doc-commentaire de `value` énonce que le suffixe tiré au sort existe précisément pour qu'« un champ `unique` ne fasse pas échouer la seconde exécution des tests sur la première ligne restée en base », mais seuls `String` et `Text` en reçoivent un ; `FieldType::Int` rend `42`/`43` en dur (`:93`), les autres scalaires de même → `--fields "views:int:unique"` engendre un test vert une fois, rouge ensuite. Le commentaire dit l'invariant que le code ne tient pas. *(Easy)* — Fait le 2026-09-01 : un champ `unique` non textuel tire sa valeur par `unique_number()` (`int`, `float`, `datetime`), les champs ordinaires gardant leur `42` lisible ; `bool:unique` est refusé à l'analyse de `--fields` par `ErrorKind::UniqueOnBool`, une colonne booléenne unique n'admettant que deux lignes. Le défaut frappait dès la première exécution, les scénarios engendrés créant en parallèle sur la même base. `cargo test -p rbs-cli --lib` 923, 0 échec, dont `a_unique_number_is_drawn_at_each_call` et son témoin `an_ordinary_number_keeps_its_readable_value` ; clippy et fmt sans sortie.
- [x] **89. [Bug] `P3` — `rbs doctor | head -3` panique au lieu de s'arrêter** — `crates/rbs-cli/src/lib.rs:211,329` : le puits du rendu avale l'erreur d'écriture, mais la conclusion passe par `ui::success` et `println!`, qui paniquent sur EPIPE → la promesse d'une sortie exploitable par un pipe n'est tenue qu'à moitié. `rbs completions <shell> | head -3` panique de même, `clap_complete::generate` faisant un `expect` sur l'écriture — trois puits, un seul remède. *(Easy)* — Fait le 2026-09-02 : `ui::Tolerante<W>` avale l'erreur d'écriture en rendant la taille du tampon — rendre `Ok(0)` échangerait la panique contre la boucle sans fin de `write_all`. C'est la doctrine que `doctor::render::Texte` tenait pour son seul compte, remontée dans `ui` et branchée sur les trois puits : les dix `println!` de `lib.rs` (et non les deux cités) passent par `ui::line`, `completions::render` et `Texte` reçoivent `ui::stdout()`. Nouveau `tests/integration_pipe.rs` — 4 tests sans Docker, ~0,9 s, qui referment l'extrémité de lecture d'un tube plutôt que de courir avec le tampon de 64 Kio d'un `| head` : les quatre échouent sur le code d'avant, passent après. `cargo test --workspace` 1143 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql` et `each_engine_produces_a_project_whose_tests_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **90. [Dette] `P3` — `const CONFIG` existe en quatre exemplaires** — `crates/rbs-cli/src/doctor/{mod.rs:299,auth.rs:21,storage.rs:18,mail.rs:20}` déclarent la même `"config/default.toml"` ; les trois derniers pourraient prendre `super::CONFIG`. *(Easy)* — Fait le 2026-09-02 : `auth.rs`, `mail.rs` et `storage.rs` prennent `super::CONFIG`, comme `observability.rs` le faisait déjà ; aucun changement de visibilité n'est nécessaire, un module enfant voyant les items privés de son ancêtre. `cargo test --workspace` 1143 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql` et `each_engine_produces_a_project_whose_tests_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **91. [Perf] `P3` — `doctor` lit encore le `Cargo.toml` trois fois hors de la boucle des features** — `crates/rbs-cli/src/doctor/{agents,base,versions}.rs`, une lecture chacun. La tâche 35 a supprimé les six relectures de la boucle mais pas celles-ci : les fermer suppose de passer les métadonnées, ou leur `Result`, à ces trois contrôles. *(Easy)* — Fait le 2026-09-02 : `doctor::run` lit et analyse le manifeste une fois et le remet aux contrôles par une struct `Projet { root, config, manifeste }` — le mécanisme déjà en place pour `Config`, et non un second. **6 ouvertures du `Cargo.toml` par diagnostic → 3** ; `base.rs` passe bien de 2 à 1. Mesure statique, relevé du code de production avant/après, pas un compteur d'appels. Les trois restantes réclament le document TOML lui-même, que `metadata` n'expose pas. `cargo test --workspace` 1143 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql` et `each_engine_produces_a_project_whose_tests_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **92. [Dette] `P3` — Le répertoire de compilation partagé `target/rbs-integration` est instable sous une suite `--ignored` complète** — observé deux fois : `could not parse/generate dep info` et `failed to write .fingerprint/…`, sur des tests sans rapport entre eux, verts au rejeu isolé. Un échec qui n'est pas celui du code coûte plus cher qu'une compilation refaite. *(Medium)* — Fait le 2026-09-02 : `crates/rbs-cli/src/test_cible.rs`, partagé par `#[path]` entre le banc et `tests/common`, pose un verrou de fichier autour de chaque `cargo` et de chaque `rbs` — `File::lock`, stable depuis 1.89 donc sous le plancher 1.94, aucune dépendance ajoutée ; `cargo test --workspace --no-fail-fast -- --ignored` sous Docker : 39 passés, 0 échec, exit 0 sur 25 binaires.
- [x] **93. [Sécu] `P3` — `RUSTSEC-2026-0173` reste un avertissement toléré** — `proc-macro-error2` n'est plus maintenue ; `cargo audit` la signale sans faire échouer le job ajouté par la tâche 43. À suivre : elle deviendra bloquante le jour où quelqu'un durcit la configuration. *(Easy)* — Fait le 2026-09-02, autrement que prévu : `validator_derive` 0.20.1 a remplacé `proc-macro-error2` par `proc-macro-error3`, si bien qu'un `cargo update -p validator_derive` lève l'advisory au lieu de la tolérer. `.cargo/audit.toml` reste intact, la CI passe à `cargo audit --deny warnings` — exit 0, 536 dépendances, aucune advisory.
- [x] **94. [Dette] `P3` — Les tests de `doctor/` compilent la crate `migration` d'un projet temporaire** — environ deux minutes par exécution. C'est le coût que la tâche 33 vient d'annoncer à l'utilisateur ; il pèse aussi, et sans annonce, sur la suite de tests du dépôt. *(Medium)* — Fait le 2026-09-02, la mesure ayant corrigé la prémisse : sans PostgreSQL sur 5432 les tests de `doctor/` ne compilent rien (135 passés en 1,01 s) ; avec un moteur à ce port, sept d'entre eux lançaient `cargo run -p migration`, le contrôle `base` demandant sa version dès qu'il joint la base. Leur projet vise désormais le port 1, réservé et sans écouteur, comme les tests de `base` : 135 passés en 1,27 s, zéro invocation de cargo, base démarrée ou non, et aucune couverture déplacée en `--ignored`.
- [x] **95. [Dette] `P3` — `examples/blog-auth/src/posts/tests.rs` est écrit à la main en français quand le gabarit rend des noms anglais** — `appeler`, `requete`, `sans_corps` contre `call`, `request`, `without_body` → qui compare l'exemple au code engendré voit deux styles là où la documentation affirme que les exemples sont ce que le CLI produit. *(Easy)* — Fait le 2026-09-02 : helpers et noms de tests alignés sur `tests.rs.jinja` (`appeler` → `call`, `requete` → `request`, `sans_corps` → `without_body`…), les six helpers propres à `blog-auth` passés à l'anglais pour ne pas laisser deux styles dans un même fichier ; les marqueurs `// region:` gardent leurs noms français, que `docs/docs/guides/auth.md` cite en `region=`. Le piège des littéraux a mordu : `integration_examples.rs:584-585` figeait deux de ces noms de tests, corrigé dans le même commit. Les 39 tests de l'exemple ont été exécutés contre PostgreSQL 16, 0 échec. `cargo test --workspace` 1143 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql` et `each_engine_produces_a_project_whose_tests_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **96. [Dette] `P3` — Les tests d'exclusion des drapeaux n'assertent que `is_err()`** — `crates/rbs-cli/src/cli.rs:369-381` : `yes_is_accepted_only_by_new` et sa voisine vérifient qu'une commande est refusée, sans vérifier *pourquoi* → une faute de frappe dans le nom de la sous-commande ferait passer le test pour la mauvaise raison. *(Easy)* — Fait le 2026-09-02 : `ErrorKind::UnknownArgument` vérifié par une sonde sur les treize commandes des deux boucles, et non supposé, puis asserté avec le message nommant le drapeau, sur la forme de `doctor_force_is_refused_without_fix`. Preuve que le trou est fermé : en remplaçant `doctor` par `doctorr` dans la liste, le test échoue maintenant (`left: InvalidSubcommand, right: UnknownArgument`) là où `is_err()` le laissait passer. `cargo test --workspace` 1143 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql` et `each_engine_produces_a_project_whose_tests_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.

- [x] **97. [Doc] `P3` — L'`AGENTS.md` engendré annonce une liste de fragments arrêtée à sept** — `crates/rbs-cli/templates/agents/{en,fr}.md.jinja:22` énumérait « auth, ci, docker, jobs, mail, redis, storage », sans `cors` ni `rate-limit` depuis les tâches 53 et 54 → le guide écrit *pour un agent* lui cachait les fragments qu'il pourrait poser. *(Easy)* — Fait le 2026-09-01 : les dix noms énumérés, la ligne `rbs doctor` dit son `--fix`, les quatre `examples/AGENTS.md` suivent ; `integration_examples` 17 passés, 0 échec.

- [x] **100. [Dette] `P3` — Sept bancs écrivent à la main la liste des fichiers d'une feature** — `crates/rbs-cli/src/generate/{dto,controller,repository,service,tests_http}.rs` énumèrent chacun les fichiers qu'ils déposent dans le projet du banc, quand `command.rs:490` en tient déjà la liste. L'ajout de `filter.rs` a cassé quatre d'entre eux l'un après l'autre, chaque échec ne se voyant qu'après une compilation complète sous Docker. Fix : un helper qui rend la liste depuis `command.rs`, et les bancs choisissent ce qu'ils en gardent. *(Medium)* — Fait le 2026-09-02 : `command::fichiers` rend la liste, `bench::tous` et `bench::retenus` la servent aux six bancs qui l'énuméraient — le septième, `entity.rs`, ne dépose que `model.rs` depuis son propre helper et n'avait rien à énumérer. Un nom disparu échoue désormais avant la compilation ; les six bancs verts sous Docker dans la suite `--ignored`.

- [x] **101. [Dette] `P3` — `examples/blog-auth/src/posts/tests.rs` n'éprouve pas la route de filtrage** — le fichier est écrit à la main (harnais de jetons de la tâche 48) et échappe donc au test de non-dérive : les trois autres exemples ont reçu `the_filter_narrows_the_list`, celui-ci non. L'exemple qui illustre le garde de rôle est justement celui où il vaudrait la peine de montrer que filtrer reste ouvert sans jeton. *(Easy)* — Fait le 2026-09-02 : `the_filter_narrows_the_list` ajouté hors de la région `refus`, qu'`auth.md` annonce à trois tests, et exerçant ce que `blog-auth` a de propre plutôt que d'être recopié : création sous jeton `admin`, filtrage **sans jeton** attendu en 200, suppression sous jeton. Exécuté contre PostgreSQL 16 — `posts::tests::the_filter_narrows_the_list ... ok`, 39 passés, 0 échec. `cargo test --workspace` 1143 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql` et `each_engine_produces_a_project_whose_tests_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.

- [x] **102. [Doc] `P3` — Le corps du filtre est décrit comme un objet libre dans l'OpenAPI** — `templates/feature/filter.rs.jinja` pose `#[schema(value_type = Object)]` sur chaque condition : dériver `ToSchema` exigerait `ComposeSchema` sur `Comparison<T>`, que seule la macro d'utoipa produit, et `T: ToSchema` que ni `DateTimeUtc` ni `Uuid` ne satisfont. Le document dit donc « objet » là où il pourrait nommer `eq`, `gt`, `gte`, `lt`, `lte` et `is_null`. Fix : un type de documentation non générique dans `rbs-core`, cité par `value_type`. *(Medium)* — Fait le 2026-09-02 : `ComparisonSchema` et `TextMatchSchema` dans `rbs-core`, cités par `value_type` — deux types et non un, les colonnes textuelles portant `contains` et non `gt`/`lt` —, avec un import conditionnel pour qu'une entité sans texte ne casse pas un projet bâti sous `-D warnings`. Document engendré relu : les six conditions y sont nommées et `ArticleFilter.title` les référence par `$ref`.

- [x] **98. [Doc] `P3` — `docs/docs/compatibility.md` annonce dix ancres et en décrit une de moins que le projet n'en porte** — la page (et sa jumelle française) a été écrite quand le registre en comptait dix ; `layers` lui a échappé à la tâche 53 et `health_probes` à la tâche 55, si bien qu'`ANCRES` en porte douze quand la page en nomme dix. Une page de compatibilité fausse sur le compte des points d'insertion se lit pourtant comme un contrat. *(Easy)* — Fait le 2026-09-02 : quatre occurrences du compte corrigées dans chaque langue — dix → douze, « nine Rust ones » → « eleven » — avec `layers` et `health_probes` insérées, et la formule finale alignée sur celle de `cli/new.md`. Autorité prise sur `ANCRES`, fichier hôte de chaque ancre vérifié au registre. `parite.mjs` : 29/29 paires au même dernier commit, 0 écart structurel. `cargo test --workspace` 1143 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql` et `each_engine_produces_a_project_whose_tests_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.

- [x] **99. [Doc] `P3` — Les deux guides d'observabilité citent des blocs écrits à la main** — `docs/docs/guides/observability.md` et sa jumelle, quand `CLAUDE.md` pose que la documentation ne cite aucune ligne écrite à la main et tire ses extraits d'`examples/`. Aucun des quatre projets ne porte la feature : la fermer suppose un cinquième exemple, avec son étape de CI, son `README` et son test de non-dérive — d'où le report plutôt que le contournement. *(Medium)* — Fait le 2026-09-02 : `observability` installé sur `examples/newsletter-queue` par diff entre deux générations, les six blocs des deux guides tirés du code de l'exemple par `file=` ; `integration_examples` 17 passés, `npm run build` deux `[SUCCESS]`, `parite.mjs` 0 écart.

- [x] **103. [Perf] `P3` — `JsonFormat` construit une `Map<String, Value>` entière par événement** — `crates/rbs-core/src/logs/json.rs:62-72` : quatre clés `to_owned()`, trois valeurs `String`, un `to_string()` du niveau, un `Value::Object` rendu en chaîne, plus le `timestamp()` que la tâche 65 visait seul — le retirer isolément n'aurait rien changé de mesurable, d'où le report. Fix : sérialiser directement dans le `Writer`, sans objet intermédiaire. Concerne le formateur actif en production, là où `pretty` ne sert qu'au développement. *(Medium)* — Fait le 2026-09-02 : `format_event` sérialise au fil de l'eau, la déduplication tenant sur un `BTreeSet` de clés ; trois tests fixent le contrat — clé de l'événement prioritaire sur celle du span, span intérieur sur l'englobant, et l'ordre `ts`, `level`, `target`, `msg` que la `Map` reclassait alphabétiquement.

---

**Relevés le 2026-09-02**, pendant l'exécution des onze tâches P3 de dette : constats faits
en passant, chacun revérifié dans le code avant d'être inscrit, aucun corrigé à la volée.
La priorité est portée sur la ligne, ce bloc étant hors des sections.

- [x] **104. [Bug] `P2` — La faute d'un manifeste illisible reste inatteignable par le CLI** — `crates/rbs-cli/src/doctor/mod.rs` : `doctor::run` appelle `metadata::project_root` avant tout contrôle, et abandonne le diagnostic entier sur un `Cargo.toml` mal formé. Les branches « manifeste illisible » d'`agents`, `versions` et `base` — celles que la tâche 83 vient justement de rendre correctes — ne sont donc exerçables qu'en test unitaire. Or c'est le cas où `doctor` servirait le plus : rendre un rapport portant cette faute en `Check` vaut mieux qu'un abandon muet. *(Medium)* — Fait le 2026-09-02 : `metadata::racine` rend la racine **avec** le manifeste qu'elle a lu pour la reconnaître — ou la faute qui l'a arrêtée dessus —, et `doctor::run` la porte jusqu'aux contrôles ; `project_root` reste le garde des commandes qui écrivent, pour lesquelles un manifeste illisible demeure un refus. `RootError::Absent` reste un abandon : ce n'est pas un projet à diagnostiquer. La racine retenue est celle du manifeste fautif — la remontée s'y arrête précisément parce que c'est le projet visé. Une première version portant `root` dans `RootError::Illisible` a été écartée sur `clippy::result_large_err`. Deux tests vus rouges, le second ayant motivé `doctor::une_ligne` : le report de `toml_edit` fait cinq lignes et disloquait la colonne du rendu. Vérifié à la main sur un projet au `Cargo.toml` tronqué — six contrôles rendus, trois nommant la faute, sortie 1 ; depuis un répertoire sans projet, la commande refuse toujours. `cargo test --workspace` 1153 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql`, `each_engine_produces_a_project_whose_tests_pass` et `the_auth_tests_of_the_generated_project_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **105. [Bug] `P2` — `Config::read` avale un `config/default.toml` mal formé comme un fichier absent** — `crates/rbs-cli/src/doctor/mod.rs:348` : même famille que la tâche 83, mais sur l'autre fichier. Conséquence visible, elle : chaque contrôle de feature annonce alors « ne porte pas de section `[x]` » et propose d'ajouter une section qui est déjà là. Le remède ment sur la panne. *(Easy)* — Fait le 2026-09-02 : `Config` devient une énumération `Lu` / `Absent` / `Fautif { detail, remede }`, et `read` distingue `NotFound`, une autre faute d'accès et une faute de syntaxe. Le remède ne propose plus d'ajouter une section déjà présente : il nomme le fichier mal formé. `defaut_de_section` porte désormais ce constat pour `section_check` et pour les trois contrôles qui en dupliquaient la formulation (`auth.rs`, `mail.rs`, `storage.rs`). Trois tests vus rouges, tous sur le message menteur « ne porte pas de section `[jobs]` ». `cargo test --workspace` 1153 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql`, `each_engine_produces_a_project_whose_tests_pass` et `the_auth_tests_of_the_generated_project_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **106. [Dette] `P2` — `service.rs.jinja` est le seul générateur de feature sans garde `bench::formatted`** — `crates/rbs-cli/templates/feature/service.rs.jinja:7` porte la même ligne `use super::dto::{…}` que la tâche 86 vient de corriger côté contrôleur, et `crates/rbs-cli/src/generate/service.rs` n'a aucun garde là où `entity`, `filter`, `seed` et `controller` en ont un. Le garde posé à titre de mesure montre que la divergence dépasse largement le `use` : sur un nom court les signatures de `list` et `filter` sont éclatées quand rustfmt les replie, sur un nom long trois expressions débordent. `format_batch` couvre l'utilisateur à l'écriture ; c'est le gabarit qui ment sur ce qu'il produit. *(Medium)* — Fait le 2026-09-02 : la divergence était plus large que le `use`. Trois seuils de rustfmt traversent le fichier — `max_width` à 100 colonnes pour une signature, `chain_width` à 60 pour une chaîne d'appels, 98 pour un `use` — et le gabarit n'en connaissait aucun : sur un nom court rustfmt *repliait* les signatures de `list` et `update` qu'il éclatait, sur un nom long il éclatait `create`, la ligne des DTO, les deux `.into_iter().map(Into::into).collect()` et les deux `.await?.into()`. Deux macros minijinja portent la décision : `entete` mesure une signature, `chaine` mesure une chaîne, chacune choisissant la forme que rustfmt aurait choisie. Garde `bench::formatted` ajouté sur la forme de `controller.rs:324`, vu rouge sur `tag` dès le premier nom de la boucle. `examples/` ne bouge pas, et c'est prouvé plutôt que supposé : `generate` fait passer ce qu'il écrit par `format_batch`, donc les `service.rs` versionnés portaient déjà la sortie de rustfmt. `cargo test --workspace` 1153 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql`, `each_engine_produces_a_project_whose_tests_pass` et `the_auth_tests_of_the_generated_project_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **107. [Dette] `P3` — Le `use` des DTO du contrôleur diverge encore au-delà de 24 caractères d'entité** — la tâche 86 rend le gabarit point fixe de rustfmt jusqu'à `organizational_structures` (23 caractères) ; au-delà, rustfmt passe au remplissage glouton sur plusieurs lignes, régime que le gabarit ne sait pas écrire. `format::format_batch` le rattrape à l'écriture, donc rien de mal formé n'atteint l'utilisateur. Même nature que la tâche 106, à traiter avec elle. *(Medium)* — **PARTIEL le 2026-09-02** : le seuil est mesuré et le gabarit du service l'atteint, mais le régime au-delà reste non écrit. Relevé au rustfmt sur des entités de 1 à 32 caractères, le point fixe des deux gabarits — contrôleur et service — s'arrête à **23 caractères d'entité** (`organizational_structures`) : à 24, la ligne intérieure de `use super::dto::{…}` atteint 101 colonnes et rustfmt passe au remplissage glouton. `format::format_batch` le rattrape à l'écriture, donc rien de mal formé n'atteint l'utilisateur. Reproduire ce remplissage côté `generate` a été écarté : cela réimplanterait une règle de rustfmt qu'une montée de version peut déplacer. — **Fait le 2026-09-02** : le seuil n'est plus une prose, c'est l'intervalle `(24..=40)` que le garde du contrôleur et celui du service assertent par `bench::longueurs_divergentes`. L'extension du point fixe au-delà de vingt-trois reste **écartée** — reproduire le remplissage glouton de rustfmt réimplanterait une règle qu'une montée de version déplace — mais la frontière est désormais mesurée, et un déplacement affiche sa nouvelle valeur au lieu de faire échouer une liste de quatre noms. Le contrôleur a gagné au passage sa borne **basse** : à une ou deux lettres d'entité, la signature de `find` tient sous les cent colonnes et rustfmt la compactait, ce qu'aucun garde ne voyait.
- [x] **108. [UX] `P3` — `rbs new` reste muet sur une URL qu'il ne décompose pas** — `crates/rbs-cli/src/new.rs:378` : `connexion == None` éteint `compose_utile()`, si bien qu'aucun identifiant vide n'atteint un fichier — c'est ce qui a dispensé la tâche 84 d'y toucher. Mais le projet naît alors sans `docker-compose.yml`, sans un mot, et avec une URL que sqlx refusera au démarrage. Un avertissement, et non un refus : les URL à socket Unix (`postgres:///demo`) sont légitimes et ne se décomposent pas non plus. *(Easy)* — Fait le 2026-09-02 : `url_opaque` (fonction pure) et un avertissement rendu au niveau de la commande, `new::create` n'écrivant rien sur la sortie. Un avertissement et non un refus, comme le critère le demandait. Deux écarts assumés à la lettre du backlog : le message **ne cite pas l'URL**, qui porte souvent un mot de passe et finirait dans les journaux de CI — il renvoie à `RBS_DATABASE__URL` dans `.env` —, et il n'affirme pas que sqlx refusera l'URL, ce qui est faux pour une socket Unix que le critère déclare lui-même légitime ; il se borne au fait observable : pas d'hôte lu, pas de compose écrit. Rouge d'abord sur une implémentation muette. Sortie réelle vérifiée sur `postgres:///demo`, et inchangée sur une URL complète comme sur SQLite. `cli/new.md` EN et FR, `CHANGELOG` EN/FR. `cargo test --workspace` 1153 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql`, `each_engine_produces_a_project_whose_tests_pass` et `the_auth_tests_of_the_generated_project_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **109. [Doc] `P3` — Quatre pages citent des transcripts `rbs doctor` au compte d'ancres périmé** — `docs/docs/getting-started.md:390,403` (« les 11 points d'insertion sont en place », puis « ten of them here »), `docs/docs/cli/doctor.md:193,262,382`, leurs quatre jumelles françaises et le JSON `docs/i18n/fr/…/cli/doctor.md:154`. La tâche 98 n'a corrigé que `compatibility.md`, dont la prose n'était adossée à aucune sortie : ici les blocs sont copiés d'une exécution réelle, et corriger la prose seule la mettrait en contradiction avec son propre transcript. Le correctif est de rejouer `rbs doctor` sur un projet neuf et de reprendre transcript et prose ensemble — donc avec Docker. `docs/docs/cli/doctor.md:47` est en revanche déjà juste. *(Medium)* — Fait le 2026-09-02 : `rbs doctor` rejoué sous Docker sur trois projets — 12 ancres et 7 variables en PostgreSQL, 11 et 4 en SQLite. Cinq « 11 » → 12, un « 10 » → 11, deux « 4 variables » → 7, la prose du démarrage reprise avec son transcript, et `guides/agents.md`, qui portait le même écart, repris avec les autres dans les deux langues. Deux blocs passent sous garde `base`.
- [x] **110. [Dette] `P3` — `metadata::load` et `parse` sont privés, et `doctor` reconstruit leur erreur à la main** — `crates/rbs-cli/src/doctor/base.rs:155` : `pilote()` refait les trois lignes de `metadata::parse` pour obtenir le document TOML, que `metadata` n'expose pas. Les exposer en `pub(crate)` ramènerait le diagnostic de trois lectures du `Cargo.toml` à une seule — c'est ce qui a arrêté la tâche 91 à 3 sur 6. *(Easy)* — Fait le 2026-09-02 : `load`/`parse` **n'ont pas** été exposés — la lecture unique s'obtient mieux en amont. `metadata::Manifeste { document, metadonnees }` porte les deux, `metadata::racine` rend la racine avec le manifeste lu au passage, et `doctor::Manifeste` devient son `Result` ; les trois lignes de reconstruction d'erreur de `base.rs:175` disparaissent, `versions::core` lit le document reçu. Le critère de la tâche était la réduction des lectures : **4 sites d'ouverture du `Cargo.toml` → 1**, mesure statique sur le code de production. Un test supprimé, `a_broken_manifest_names_its_parse_error` : la troisième issue de `pilote` n'existe plus, et son constat est déjà vérifié par `an_unreadable_manifest_is_named_instead_of_being_read_as_the_default_engine` (`base.rs:579`). `cargo test --workspace` 1153 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql`, `each_engine_produces_a_project_whose_tests_pass` et `the_auth_tests_of_the_generated_project_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **111. [Dette] `P3` — Le gabarit d'`observability` nomme encore son helper en français** — `crates/rbs-cli/templates/features/observability/tests.rs.jinja:30` définit `appeler(...)` quand `templates/feature/tests.rs.jinja` rend `call`. Même dette que la tâche 95, mais côté gabarit, donc visible dans tout projet portant la feature. *(Easy)* — Fait le 2026-09-02 : `appeler` → `call`, et l'alignement va au-delà de la lettre — `registre()` → `registry()`, `REGISTRE` → `REGISTRY` : n'en changer qu'un aurait reconduit dans un même fichier les deux styles que la tâche 95 refusait. La prose des commentaires reste française, comme partout dans le code engendré. Les deux pièges vérifiés : `integration_observability.rs:19-21` fige bien deux littéraux, mais ce sont des *noms de tests*, non touchés ; et le gabarit ne portant aucune balise Jinja, il a été copié en `.rs` et passé à `rustfmt --edition 2024 --check` pour compenser le fait qu'aucune compilation ne le couvre avant Docker. Test de rendu ajouté (`add/mod.rs:1443`), vu rouge. `cargo test --workspace` 1153 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql`, `each_engine_produces_a_project_whose_tests_pass` et `the_auth_tests_of_the_generated_project_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **112. [Dette] `P3` — Un troisième test d'exclusion n'assert que `is_err()`** — `crates/rbs-cli/src/cli.rs:333` : `an_unknown_language_is_refused_by_the_parser` a échappé à la tâche 96, qui ne visait que les deux tests nommés au backlog. Motif attendu : `ErrorKind::InvalidValue`. *(Easy)* — Fait le 2026-09-02 : `ErrorKind::InvalidValue` **vérifié par une sonde** avant d'être asserté (`invalid value 'de' for '--lang <LANGUE>'`), et le message asserté nomme `--lang`, sur la forme des voisins. Preuve que le trou est fermé : en remplaçant `new` par `neww` — une faute d'un autre genre, que `is_err()` laissait passer — le test échoue désormais (`left: InvalidSubcommand, right: InvalidValue`). `cargo test --workspace` 1153 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql`, `each_engine_produces_a_project_whose_tests_pass` et `the_auth_tests_of_the_generated_project_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **113. [Doc] `P3` — `examples/README.md` énumère mal les éditions à la main de `blog-auth`** — la liste dit quels tests s'ajoutent au fichier engendré, mais `--role admin` met `creatable = false` (`crates/rbs-cli/src/generate/tests_http.rs:31`) : le gabarit ne rend que `an_unknown_id_returns_404` et `an_anonymous_write_returns_401`, si bien que le cycle de vie complet et le 400 sont eux aussi des ajouts que la liste ne mentionne pas. Vaut pour la jumelle française. *(Easy)* — Fait le 2026-09-02, **sur une prémisse corrigée** : la recette de `blog-auth` ne passe pas `--role admin` — ni `examples/README.md:42-48`, ni `integration_examples.rs:41-55` — le garde de rôle y étant une édition à la main de `controller.rs`, ce qui est le point même de l'exemple. Donc `creatable = true`, et le cycle de vie complet comme le 400 sont bien engendrés : ce ne sont pas des ajouts. Le vrai défaut de la liste était plus large : le troisième « ajout » annoncé — la route de filtrage — est engendré, et la prose taisait deux retraits et trois réécritures. Liste réelle établie en rejouant la recette dans un projet jetable puis en diffant contre le fichier versionné : **3 ajoutés** (401, 403, liste publique), **3 réécrits** (cycle de vie et filtre signés `admin`, 400 du corps illisible qui doit être signé — `Identity` court avant l'analyse du corps), **2 retirés** (que `hello-crud` porte intacts), **1 intact**. Jumelle française suivie dans le même commit. `parite.mjs` : 29/29 paires au même dernier commit, 0 écart structurel. `cargo test --workspace` 1153 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql`, `each_engine_produces_a_project_whose_tests_pass` et `the_auth_tests_of_the_generated_project_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.

---

**Relevés le 2026-09-02**, pendant l'exécution du lot P2 et des cinq Easy qui l'ont suivi :
constats faits en passant, chacun revérifié dans le code avant d'être inscrit, aucun
corrigé à la volée. La priorité est portée sur la ligne, ce bloc étant hors des sections.

- [x] **114. [Bug] `P2` — `rbs doctor --fix` abandonne encore sur un manifeste cassé** — `crates/rbs-cli/src/lib.rs:615` : `repair_anchors` appelle `metadata::project_root(...)` avant tout diagnostic, quand la tâche 104 vient de faire survivre `rbs doctor` à ce cas. Le CLI se contredit donc : il diagnostique le projet, et refuse de réparer ses ancres — alors que la réparation d'une ancre ne dépend en rien du manifeste. Non traité avec la 104 parce que c'est un chemin d'écriture, où un manifeste illisible peut légitimement valoir refus : c'est l'arbitrage à rendre. *(Easy)* — Fait le 2026-09-02 : `repair_anchors` désigne la racine par `metadata::racine` et non `project_root`. L'arbitrage, rendu par l'utilisateur, est que la réparation procède : reposer une ancre ne lit aucune donnée du manifeste et n'écrit que dans des fichiers source qui en portent déjà. `project_root` n'est pas assouplie — sa doctrine reste juste pour `add`, `generate`, `upgrade`, `migrate` et `seed`. Le défaut était pire que la lettre du constat : le `?` faisait tomber la commande entière, si bien que `--fix` n'affichait même plus le rapport que `doctor` seul rend. La faute du manifeste reste dite, par les contrôles `agents`, `versions` et `base` du même passage. Test vu rouge — `la réparation aboutit: Metadata(Syntaxe { … "unclosed table, expected `]`" … })`. `cargo test --workspace` 1156 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés, 0 échec, dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql`, `each_engine_produces_a_project_whose_tests_pass` et `the_auth_tests_of_the_generated_project_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **115. [Dette] `P2` — Deux générateurs de feature restent sans garde `bench::formatted`** — la tâche 106 disait `service.rs` « le seul » ; à la fermer on a constaté que `crates/rbs-cli/src/generate/repository.rs` et `dto.rs` n'en portent pas davantage, quand `entity`, `filter`, `seed`, `controller` et désormais `service` en ont un. La divergence qu'un tel garde a révélée sur `service` — trois seuils de rustfmt, pas seulement le `use` — n'a été mesurée sur aucun des deux. *(Medium)* — Fait le 2026-09-02, **la mesure ayant déplacé le diagnostic**. Le pilote n'est pas l'entité PascalCase mais le singulier snake_case, et le seuil qui mord en premier n'est pas les 100 colonnes de `max_width` retenues par la tâche 86 mais les 60 de `chain_width` : dès **treize** caractères de singulier, `{singulier}.insert(db).await.map_err(conflict_on_duplicate)` vaut 61 colonnes et rustfmt l'éclate en quatre. `repository.rs.jinja` reprend les deux macros de `service.rs.jinja` — recopiées et non importées, `Renderer::render` appelant `render_str` sans chargeur — et devient point fixe jusqu'au second seuil, vingt-trois caractères de singulier. `dto.rs.jinja` s'est révélé **déjà point fixe de deux à quarante** : le garde est posé et la mesure écrite, aucune correction fabriquée. Rouge reproduit hors du rapport de l'agent, sur `purchase_orders`. Trois constats versés au backlog, dont le plus lourd : le test de `command.rs` qui se disait garant de tous les gabarits compare rustfmt à lui-même, ce qui explique que ce seuil ait vécu invisible (tâche 122). `cargo test --workspace` 1156 passés, 0 échec, 38 ignorés ; suite Docker `--ignored --no-fail-fast` en sortie 0, 38 passés, 0 échec, dont `a_generated_crud_migrates_and_passes_its_tests_against_postgresql`, `each_engine_produces_a_project_whose_tests_pass` et `the_auth_tests_of_the_generated_project_pass` ; `integration_examples` 17 passés, aucune dérive ; clippy `-D warnings` et `cargo fmt --all --check` sans sortie.
- [x] **116. [Doc] `P2` — `cli/new.md` annonce deux cas et en énumère quatre** — `docs/docs/cli/new.md:314` (« Unless one of the **two** cases below applies ») introduit la liste que la ligne 357 compte elle-même « **Four** cases write nothing ». Jumelle française `docs/i18n/fr/.../cli/new.md:315`. La page se contredit à quarante lignes d'intervalle sur le nombre de cas où aucun compose n'est écrit. *(Easy)* — Fait le 2026-09-02 : « two » devient « four », en anglais comme en français ; la liste, quarante lignes plus bas, se comptait déjà elle-même. Corrigé dans la même passe que la tâche 117, dont il partage les deux pages.
- [x] **117. [Doc] `P2` — Le transcript SQLite de `cli/new.md` compte un fichier de moins que le CLI n'en écrit** — `docs/docs/cli/new.md:376` montre `✓ sqlite-demo créé — 18 fichiers`, et la prose du `:381` s'appuie dessus (« Eighteen files, not nineteen: the count is how you tell »). Vérifié : `rbs new sqlite-demo --database sqlite --yes` rend aujourd'hui **19 fichiers**. Le texte ment donc deux fois, et son raisonnement — reconnaître l'absence de compose au compte — repose sur le chiffre faux. Vaut pour la jumelle française. *(Easy)* — Fait le 2026-09-02, **sur un périmètre élargi après mesure, l'utilisateur ayant tranché**. Le transcript SQLite annonçait dix-huit fichiers quand le CLI en écrit dix-neuf, mais toute la page portait le même écart : `config/production.toml` manquait à la liste comme aux cinq autres comptes. Deux autres axes de péremption tenaient dans les mêmes blocs — `auth` dépose treize fichiers et non neuf, l'alignement des colonnes ayant suivi la largeur du nombre, et `rbs new --with` connaît dix features et non sept, `cors`, `observability` et `rate-limit` étant arrivées après la rédaction. `getting-started.md` portait le même décalage plus un plan de `generate crud` amputé de `filter.rs`, seize lignes au lieu de dix-sept et neuf fichiers au lieu de dix. Chaque bloc rejoué contre le binaire du dépôt plutôt que corrigé de tête ; la prose se lit « dix-neuf, et non vingt ». Le `version = "1.1.0"` du manifeste d'exemple n'est pas touché : c'est la version publiée, quand le dépôt travaille sur la suivante. Constat versé en tâche 123 : rien ne tient ces transcripts à jour. `node docs/scripts/parite.mjs` 29/29 paires, 0 écart structurel ; `npm run build` dans `docs/` en succès dans les deux langues.
- [x] **118. [Bug] `P3` — Une autorité sans hôte mais avec port passe pour décomposée** — `crates/rbs-cli/src/url.rs:88-91` : `postgres://:5432/demo` rend `Some(Connection { host: "", … })`, `rsplit_once(':')` acceptant une partie gauche vide. Aucun compose n'est écrit (`est_locale()` est faux sur `""`), mais l'avertissement posé par la tâche 108 ne se déclenche pas non plus, puisqu'il guette `None` : c'est un troisième cas d'URL incompréhensible qui reste silencieux, exactement le trou que la 108 fermait. *(Easy)* — Fait le 2026-09-02 : un hôte vide rend `None` dans les deux branches de la découpe ; deux tests unitaires et un test d'intégration qui lit la sortie d'erreur du binaire prouvent que l'avertissement de la tâche 108 se déclenche désormais.
- [x] **119. [Dette] `P3` — Le seuil de 23 caractères vit dans deux commentaires et dans aucun test** — `crates/rbs-cli/src/generate/{controller,service}.rs` bornent chacun leur garde à la même liste de quatre noms, et l'expliquent par la même prose. Le jour où une montée de rustfmt déplace ce seuil, les deux commentaires mentiront ensemble et seule la comparaison échouera, sans nommer la cause. Fix : un test qui nomme le seuil, ou la liste des noms tenue à un seul endroit. *(Easy)* — **Fait le 2026-09-02** : `bench::longueurs_divergentes` balaie les noms de 1 à 40 caractères et rend les longueurs divergentes ; les huit gardes assertent cet intervalle au lieu d'énumérer quatre noms. Le seuil vit dans un seul endroit, il est mesuré, et le test affiche l'ancien et le nouveau quand il bouge.
- [x] **120. [Dette] `P3` — La signature de `filter` déborde à trente caractères de module** — `let ({module}, total) = repository::filter(db, filtre, pagination).await?;` franchit les 100 colonnes bien au-delà du seuil de 23 où s'arrête le point fixe des gabarits, donc hors de la plage que le garde de la tâche 106 couvre. C'est un quatrième seuil, qu'une extension du point fixe au-delà de 23 devrait traiter — à joindre à la tâche 107. *(Easy)* — **Fait le 2026-09-02** : la signature de `filter` déborde à trente caractères de module, donc au-delà des vingt-trois où s'arrête le point fixe du service — l'intervalle `(24..=40)` que son garde asserte la couvre. Close par la mesure, sans retouche de gabarit.
- [x] **121. [Dette] `P3` — `Check.detail` n'a aucune garantie de tenir sur une ligne** — `crates/rbs-cli/src/doctor/render.rs:83` écrit `detail` brut, là où le même rendu indente les continuations d'un `remede` et d'une `annonce`. La tâche 104 a dû introduire `doctor::une_ligne` parce qu'un report de `toml_edit` sur cinq lignes disloquait la colonne ; le remède est appliqué aux trois sites connus, pas au rendu, qui reste sans garde-fou pour le prochain. *(Easy)* — Fait le 2026-09-02 : `constat` indente les continuations d'un `detail` comme celles d'un remède, `a_multi_line_detail_is_indented_like_a_remedy` rouge avant le correctif. Les trois appels à `une_ligne` restent en place, ils disent une intention.

---

**Relevés le 2026-09-02**, en fermant le second lot `P2` — les deux contradictions de la
documentation du CLI, le chemin d'écriture de `doctor --fix` et les deux générateurs sans
garde de mise en forme. Chacun revérifié dans le code avant d'être inscrit, aucun corrigé
à la volée. La priorité est portée sur la ligne, ce bloc étant hors des sections.

- [x] **122. [Dette] `P2` — Le test qui se dit garant de tous les gabarits compare rustfmt à lui-même** — `crates/rbs-cli/src/generate/command.rs:826` : `the_render_goes_through_rustfmt_without_a_diff_whatever_the_name_length` lit les fichiers *écrits* par `run()`, qui les a passés par `format::format_batch` (`command.rs:280`). Il compare donc la sortie de rustfmt à elle-même et ne peut pas échouer, alors que son nom et son doc-comment promettent de garder les gabarits. Preuve : ce test passait sur `main` en balayant `administrative_documents`, quand le rendu du dépôt divergeait dès treize caractères de singulier. Fix : lire le rendu avant `format_batch`, ou dire dans son nom et son commentaire qu'il garantit l'écriture et non le gabarit. *(Medium)* — **Fait le 2026-09-02** : le test lit `render()`, le rendu d'avant `format_batch`, et se nomme `the_rendered_templates_are_already_what_rustfmt_would_write`. Sur ce rendu il relevait douze fichiers divergents sur les six cas de son éventail — dont `tests.rs` et la migration, divergents dès trois caractères et seuls des neuf rendus sans garde. Les cinq gabarits sont corrigés, les deux gardes manquants ajoutés. `cargo test --workspace` : 1163 passés, 0 échec, 38 ignorés ; clippy et fmt à zéro sortie.
- [x] **123. [Doc] `P2` — Rien ne tient les transcripts de la documentation à jour** — quatre blocs de `docs/docs/getting-started.md` et `docs/docs/cli/new.md` ont vécu périmés sur trois axes à la fois — compte de fichiers du squelette, nombre de features connues, fichiers déposés par `auth` et par `generate crud` — et la prose raisonnait sur leurs chiffres. `docs/scripts/parite.mjs` ne voit que la structure et les liens, jamais les nombres ; `integration_examples` ne couvre que le code d'`examples/`, pas les sorties citées en prose. Fix : un test qui rejoue les commandes citées et compare le bloc, sur le modèle du test de non-dérive. *(Hard)* — Fait le 2026-09-02 : marqueur `{/* rbs:transcript cmd="…" */}` — un commentaire HTML ne compile pas en MDX et cassait `npm run build` —, extracteur et normalisation dans `crates/rbs-cli/tests/integration_docs.rs`, quatorze blocs marqués dans les deux langues. Preuve de morsure : un chiffre changé rougit en nommant page et ligne. 10 passés hors base, 1 sous Docker en 138,58 s.
- [x] **124. [Bug] `P3` — `bench::formatted` ne force pas `newline_style`** — `crates/rbs-cli/src/generate/bench.rs:436` lance rustfmt sans `--config newline_style=Unix`, quand `format::formatted` (`format.rs:47`) le passe et écrit pourquoi : le défaut « Auto » retombe sur le style de la plateforme. Les sept gardes `the_render_is_already_what_rustfmt_would_write` compareraient donc un rendu LF à une sortie CRLF sur une machine Windows, et tomberaient en bloc sur une fin de ligne plutôt que sur une mise en forme. *(Easy)* — **Fait le 2026-09-02** : `bench::formatted` passe `--config newline_style=Unix` comme `format::formatted`, et son doc-comment dit pourquoi.
- [x] **125. [Dette] `P3` — Un troisième seuil de rustfmt traverse `repository.rs.jinja`** — au-delà de vingt-six caractères d'entité, rustfmt éventaille les arguments de l'appel `filter(db, &{Entity}Filter::default(), pagination).await` que rend `list`. Le point fixe posé sur `create` et `update` s'arrête avant, et le seuil ne correspond à aucun réglage nommé de rustfmt — `format::format_batch` le rattrape à l'écriture. Même nature que les tâches 107 et 120, à traiter avec elles. *(Easy)* — **Fait le 2026-09-02** : le seuil de vingt-six est mesuré et asserté — le garde du repository porte l'intervalle `(27..=40)`. L'éventail des arguments au-delà reste laissé à `format_batch`, pour la même raison qu'en 107.
- [x] **126. [Dette] `P3` — Le point fixe des DTO n'est éprouvé que sur l'axe du nom d'entité** — `crates/rbs-cli/src/generate/dto.rs` : le gabarit est point fixe de deux à quarante caractères d'entité, mesuré ; l'axe qui finit par bouger est le nom d'un *champ* — à quarante caractères, `{champ}: model.{champ},` atteint 101 colonnes dans `impl From<Model>`. Aucun des sept gardes ne balaie cet axe-là. *(Easy)* — **Fait le 2026-09-02** : un second garde balaie l'axe du nom de champ, de 1 à 40 caractères. Il a trouvé la divergence annoncée à quarante — `{champ}: model.{champ},` atteint 101 colonnes et rustfmt casse l'accès — et le gabarit écrit désormais les deux formes : les deux axes sont point fixe sur toute la plage.

## Écarté / obsolète

**Vérifié sain, avec son témoin** — ces points ont été contrôlés et n'ont donné aucune tâche :

- `cargo fmt --all --check` et `cargo clippy --workspace --all-targets --all-features -- -D warnings` : **aucune sortie**, exit 0.
- `cargo test --workspace --all-features` : **774 + 159 tests passés, 0 échec** (23 ignorés, exercés en CI par l'étape `-- --ignored`).
- Parité FR/EN de la documentation : `docs/docs` et `docs/i18n/fr/…/current` portent **26 pages chacun**, `comm` sur les deux listes rend zéro écart, et `parite.mjs` confirme 26/26 au même dernier commit. Seuls les README hors `docs/` échappent au contrôle → tâche 72.
- Pagination bornée : `pagination.rs:44` fait `per_page.clamp(1, PAR_PAGE_MAX)`, testé par `per_page_beyond_the_maximum_is_capped_without_an_error`. Un `?per_page=999999` est ramené à 100.
- Pool de connexions entièrement configuré (`db.rs:65-75`), défauts explicites en `config.rs:197-202`.
- JWT : algorithme imposé et non lu de l'en-tête, `validate_exp` actif, tests sur `alg:none` et la confusion HS512 (`jwt.rs:75-87`). Argon2id à sel par appel (`hash.rs:19-46`), refresh tokens de 256 bits dont seule l'empreinte SHA-256 atteint la base (`token.rs:23-41`).
- Aucune fuite de la source SQL dans le corps HTTP : `error.rs:133-147`, test `the_database_error_detail_does_not_leak_into_the_response`.
- Traversée de chemin défaite dans `storage` : `normalize` parcourt `Path::components()` et rejette `RootDir`/`Prefix`/`ParentDir` en excès (`storage/mod.rs.jinja:116-135`). Noms de projet et de feature strictement validés (`new.rs:264-277`, `generate/name.rs:33-61`) — `rbs new ../../x` est refusé avant toute écriture.
- Aucun sous-processus ne passe par un shell : `Command::args` partout (`git.rs:14`, `dev/mod.rs:212`). Dockerfile généré en non-root uid 10001, `.env` exclu de l'image.
- SQL écrit à la main entièrement paramétré (`jobs/queue.rs.jinja:30-56`). `down()` réversible partout, y compris l'ordre des deux tables d'`auth`.
- Délimiteurs minijinja fidèles : `src/template.rs:19-22` et ses tests couvrent `format!("{{}}")` et `${{ secrets.X }}`.
- Les deux binaires `rbs` et `rbs-cli` sont tous deux justifiés et documentés (collision de `PATH` avec le `rbs` de Ruby) — aucun n'est mort.
- Aucun module orphelin ni déclaration sans fichier dans `generate/`, `doctor/`, `plan/`, `migrate/`, `add/`, `dev/`, `logs/`. `toml_edit::DocumentMut` n'est parsé qu'en un seul endroit (`metadata.rs:518`), `anchors::insert` n'a qu'une implémentation.
- Le CRUD généré ne fait ni `find().all()` non borné ni N+1 : `relations.rs` ne produit que des colonnes FK et des `impl Related`. Les colonnes FK reçoivent bien un index (`fields.rs:475`).
- CI : `fmt`, `clippy`, tests, tests d'intégration Docker, `semver-checks` sur `rbs-core`, compilation des quatre `examples/`, double verdict de la garde de version et `cargo publish --dry-run` — sur Linux, macOS et Windows.

**Déjà implémenté** — idées envisagées puis écartées, avec le fichier qui le prouve : templates utilisateur personnalisés (`cli.rs:18-20`, testé), MySQL et SQLite (`src/database.rs`), `rbs-core` en bibliothèque publiée à l'API figée, validation des DTO par `validator`, pagination offset, tests HTTP générés avec chaque CRUD, `rbs add docker` et `rbs add ci` pour le déploiement, compose de base à la création, guards de rôles, jobs avec réessais, `--lang fr|en`.

**Hors périmètre, jamais proposé** : GraphQL, multi-tenancy, WebSockets, gRPC, interface d'administration générée, gestion des paiements (`ROADMAP.md:111`).
